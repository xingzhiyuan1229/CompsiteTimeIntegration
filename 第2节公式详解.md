# 第 2 节（Formulation）公式逐一详解

> **依据文献**：Song, C. & Zhang, X. (2024). High-order composite implicit time integration schemes based on rational approximations for elastodynamics. *Computer Methods in Applied Mechanics and Engineering*, 418, 116473.  
> **解析范围**：原文第 2 节（Section 2）所有公式 (1) — (33)

---

## 引言与总体说明

第 2 节的核心任务是为后续高阶复合时间积分格式的构造奠定数学基础。该节分三个子节逐步建立理论框架：

- **2.1** 从物理运动方程出发，经过无量纲化、状态空间变换，将二阶 ODE 转化为可用矩阵指数精确求解的一阶 ODE，并引入外力多项式展开，得到一步精确解公式（公式 (1)–(14)）。
- **2.2** 以有理函数近似代替矩阵指数，将精确步进公式转化为可用多项式矩阵运算的代数递推格式（公式 (15)–(20)）。
- **2.3** 对单模态系统进行谱分析，定义衡量数值耗散与频散的关键指标（公式 (21)–(33)）。

三个子节之间的关系：**2.1 提供理论出发点，2.2 提出计算可行的近似方案，2.3 给出量化评估数值性能的工具**。

---

## 2.1 矩阵指数时间积分方法

### 公式 (1)：结构动力学运动方程

\[
\mathbf{M}\ddot{\mathbf{u}}(t) + \mathbf{C}\dot{\mathbf{u}}(t) + \mathbf{K}\mathbf{u}(t) = \mathbf{f}(t) \tag{1}
\]

**含义与背景**

这是结构动力学的基本控制方程，由 Hamilton 变分原理或 d'Alembert 原理通过有限元空间离散后得到。方程中各量的物理含义为：

- \(\mathbf{M} \in \mathbb{R}^{n \times n}\)：**质量矩阵**，正定对称，反映惯性效应；
- \(\mathbf{C} \in \mathbb{R}^{n \times n}\)：**阻尼矩阵**，正半定对称，反映能量耗散效应（黏性阻尼）；
- \(\mathbf{K} \in \mathbb{R}^{n \times n}\)：**刚度矩阵**，正定对称，反映弹性恢复力；
- \(\mathbf{f}(t) \in \mathbb{R}^{n}\)：**外力向量**，随时间变化；
- \(\mathbf{u}(t) \in \mathbb{R}^{n}\)：**位移向量**；
- \(\dot{\mathbf{u}}(t)\)：**速度向量**，\(\dot{\mathbf{u}} = \mathrm{d}\mathbf{u}/\mathrm{d}t\)；
- \(\ddot{\mathbf{u}}(t)\)：**加速度向量**，\(\ddot{\mathbf{u}} = \mathrm{d}^2\mathbf{u}/\mathrm{d}t^2\)。

方程 (1) 是一个关于时间 \(t\) 的 \(n\) 维二阶常微分方程组，其中 \(n\) 为有限元模型的总自由度数。它是整个时间积分方法的起点，后续所有公式均是在对此方程进行数值求解的过程中推导出来的。

---

### 公式 (2a)：初始条件

\[
\mathbf{u}(t=0) = \mathbf{u}_0,\quad \dot{\mathbf{u}}(t=0) = \dot{\mathbf{u}}_0 \tag{2a}
\]

**含义与推导背景**

方程 (1) 是二阶常微分方程组，其一般解包含 \(2n\) 个任意常数，需要 \(2n\) 个初始条件才能确定唯一解。初始条件给出时刻 \(t=0\) 处的初始位移 \(\mathbf{u}_0\) 和初始速度 \(\dot{\mathbf{u}}_0\)，构成一个**初值问题（IVP, Initial Value Problem）**。

与公式 (1) 合并，该问题被称为 Cauchy 问题，其解的存在唯一性由系数矩阵的连续性（矩阵 \(\mathbf{M}\), \(\mathbf{C}\), \(\mathbf{K}\) 均为常矩阵，\(\mathbf{f}(t)\) 通常为分段连续函数）保证。

**与后续公式的联系**：初始条件在无量纲化框架（公式 (3)–(4)）中将转化为状态向量（公式 (6)）的初始值，具体为 \(\mathbf{z}(s=0) = \{\Delta t\,\dot{\mathbf{u}}_0;\, \mathbf{u}_0\}\)（上半部分为无量纲初速度，下半为初位移），这是求解器初始化的依据。

---

### 公式 (3)：无量纲时间变量

\[
t(s) = t_{n-1} + s\,\Delta t,\quad 0 \le s \le 1 \tag{3}
\]

**含义与推导**

在数值时间积分中，需要在每个时步 \([t_{n-1},\, t_n]\) 上对方程 (1) 进行积分。令 \(\Delta t = t_n - t_{n-1}\) 为时步大小，公式 (3) 通过引入无量纲时间变量 \(s\)，将物理时间区间 \([t_{n-1}, t_n]\) **双射地映射到** \([0, 1]\)：

\[
s = \frac{t - t_{n-1}}{\Delta t}, \quad s \in [0,\,1]
\]

当 \(s = 0\) 时，\(t = t_{n-1}\)（时步起点）；当 \(s = 1\) 时，\(t = t_n\)（时步终点）。

**引入无量纲时间的动机**：在 \([0,1]\) 区间上分析方程，可以统一处理不同时步大小的情形，也便于后续利用 Legendre-Gauss-Lobatto（LGL）节点等标准正交积分点进行外力多项式采样（对应外力展开公式 (11)）。这一变换使得所有时步的分析在相同的数学框架下进行，与时步大小 \(\Delta t\) 解耦。

---

### 公式 (4)：速度与加速度的无量纲变换

\[
\dot{\mathbf{u}} = \frac{1}{\Delta t}\frac{\mathrm{d}\mathbf{u}}{\mathrm{d}s} = \frac{1}{\Delta t}\mathbf{u}^{\circ},\quad \ddot{\mathbf{u}} = \frac{1}{\Delta t^2}\frac{\mathrm{d}^2\mathbf{u}}{\mathrm{d}s^2} = \frac{1}{\Delta t^2}\mathbf{u}^{\circ\circ} \tag{4}
\]

**推导（链式法则）**

利用复合函数求导的链式法则，由公式 (3) 可知 \(\mathrm{d}s/\mathrm{d}t = 1/\Delta t\)，因此：

\[
\dot{\mathbf{u}} = \frac{\mathrm{d}\mathbf{u}}{\mathrm{d}t} = \frac{\mathrm{d}\mathbf{u}}{\mathrm{d}s}\cdot\frac{\mathrm{d}s}{\mathrm{d}t} = \frac{1}{\Delta t}\frac{\mathrm{d}\mathbf{u}}{\mathrm{d}s}
\]

\[
\ddot{\mathbf{u}} = \frac{\mathrm{d}}{\mathrm{d}t}\left(\frac{\mathrm{d}\mathbf{u}}{\mathrm{d}t}\right) = \frac{\mathrm{d}}{\mathrm{d}t}\left(\frac{1}{\Delta t}\frac{\mathrm{d}\mathbf{u}}{\mathrm{d}s}\right) = \frac{1}{\Delta t}\frac{\mathrm{d}}{\mathrm{d}s}\left(\frac{\mathrm{d}\mathbf{u}}{\mathrm{d}s}\right)\cdot\frac{\mathrm{d}s}{\mathrm{d}t} = \frac{1}{\Delta t^2}\frac{\mathrm{d}^2\mathbf{u}}{\mathrm{d}s^2}
\]

其中约定 \(\mathbf{u}^{\circ} \equiv \mathrm{d}\mathbf{u}/\mathrm{d}s\)（上标 \(\circ\) 表示对无量纲时间 \(s\) 求导，下文也记为 \(\dot{\mathbf{u}}^{\circ}\) 与 \(\ddot{\mathbf{u}}^{\circ\circ}\) 以区分对物理时间 \(t\) 的导数）。

**关键含义**：无量纲速度 \(\mathbf{u}^{\circ} = \Delta t\,\dot{\mathbf{u}}\) 是物理速度乘以 \(\Delta t\)；无量纲加速度 \(\mathbf{u}^{\circ\circ} = \Delta t^2\,\ddot{\mathbf{u}}\) 是物理加速度乘以 \(\Delta t^2\)。这解释了代码中对矩阵和初始速度所做的预处理：`v0 = dt*v0`、`K = dt^2*K`、`C = dt*C`、`F = dt^2*F`。

---

### 公式 (5)：无量纲时间下的运动方程

\[
\mathbf{M}\mathbf{u}^{\circ\circ} + \Delta t\,\mathbf{C}\,\mathbf{u}^{\circ} + \Delta t^2\mathbf{K}\,\mathbf{u} = \Delta t^2\mathbf{f} \tag{5}
\]

**推导**

将公式 (4) 代入公式 (1)：

\[
\mathbf{M}\cdot\frac{\mathbf{u}^{\circ\circ}}{\Delta t^2} + \mathbf{C}\cdot\frac{\mathbf{u}^{\circ}}{\Delta t} + \mathbf{K}\,\mathbf{u} = \mathbf{f}
\]

两端乘以 \(\Delta t^2\)，即得公式 (5)。

**关键理解**：在无量纲框架下，原来含 \(\Delta t\) 的不同幂次（$\Delta t^0$ 在惯性项，$\Delta t^{-1}$ 在阻尼项，$\Delta t^{-2}$ 在刚度项）被"吸收"到系数矩阵中。最终方程的结构形式为：

\[
\underbrace{\mathbf{M}}_{\text{质量}}\mathbf{u}^{\circ\circ} + \underbrace{\Delta t\mathbf{C}}_{\tilde{\mathbf{C}}}\mathbf{u}^{\circ} + \underbrace{\Delta t^2\mathbf{K}}_{\tilde{\mathbf{K}}}\mathbf{u} = \underbrace{\Delta t^2\mathbf{f}}_{\tilde{\mathbf{f}}}
\]

定义"缩放后的矩阵" \(\tilde{\mathbf{C}} = \Delta t\mathbf{C}\)，\(\tilde{\mathbf{K}} = \Delta t^2\mathbf{K}\)，\(\tilde{\mathbf{f}} = \Delta t^2\mathbf{f}\)，则公式 (5) 与公式 (1) 在形式上完全一致，但现在各项都是无量纲的（时间变量换为 \(s \in [0,1]\)）。这是后续推导状态空间方程（公式 (7)-(9)）的直接基础。

---

### 公式 (6)：状态空间向量

\[
\mathbf{z} = \begin{Bmatrix} \mathbf{u}^{\circ} \\ \mathbf{u} \end{Bmatrix} \tag{6}
\]

**含义与动机**

将二阶 ODE（公式 (5)）转化为一阶 ODE 系统的标准技巧是引入**状态向量（state vector）**，将系统的所有独立变量汇聚为一个向量。此处状态向量 \(\mathbf{z} \in \mathbb{R}^{2n}\) 的上半部分 \(\mathbf{u}^{\circ}\) 是无量纲速度，下半部分 \(\mathbf{u}\) 是位移。

状态向量的分块结构将在后续推导中反复使用：

\[
\mathbf{z} = \begin{Bmatrix} \mathbf{z}_1 \\ \mathbf{z}_2 \end{Bmatrix} \equiv \begin{Bmatrix} \mathbf{u}^{\circ} \\ \mathbf{u} \end{Bmatrix}
\]

其中上半部分 \(\mathbf{z}_1 = \mathbf{u}^{\circ}\) 将对应有效刚度矩阵方程的求解（公式 (88)），下半部分 \(\mathbf{z}_2 = \mathbf{u}\) 将由代数关系更新（公式 (89)）。

---

### 公式 (7)：一阶 ODE 系统

\[
\dot{\mathbf{z}} \equiv \frac{\mathrm{d}\mathbf{z}}{\mathrm{d}s} = \mathbf{A}\mathbf{z} + \mathbf{F} \tag{7}
\]

**推导**

对 \(\mathbf{z} = \{\mathbf{u}^{\circ};\,\mathbf{u}\}\) 关于 \(s\) 求导：

\[
\frac{\mathrm{d}\mathbf{z}}{\mathrm{d}s} = \begin{Bmatrix} \mathrm{d}\mathbf{u}^{\circ}/\mathrm{d}s \\ \mathrm{d}\mathbf{u}/\mathrm{d}s \end{Bmatrix} = \begin{Bmatrix} \mathbf{u}^{\circ\circ} \\ \mathbf{u}^{\circ} \end{Bmatrix}
\]

由公式 (5) 可解出 \(\mathbf{u}^{\circ\circ}\)：

\[
\mathbf{u}^{\circ\circ} = -\mathbf{M}^{-1}(\Delta t\mathbf{C}\,\mathbf{u}^{\circ} + \Delta t^2\mathbf{K}\,\mathbf{u}) + \Delta t^2\mathbf{M}^{-1}\mathbf{f}
= -\Delta t\mathbf{M}^{-1}\mathbf{C}\,\mathbf{z}_1 - \Delta t^2\mathbf{M}^{-1}\mathbf{K}\,\mathbf{z}_2 + \Delta t^2\mathbf{M}^{-1}\mathbf{f}
\]

因此：

\[
\frac{\mathrm{d}\mathbf{z}}{\mathrm{d}s}
= \underbrace{\begin{bmatrix} -\Delta t\mathbf{M}^{-1}\mathbf{C} & -\Delta t^2\mathbf{M}^{-1}\mathbf{K} \\ \mathbf{I} & \mathbf{0} \end{bmatrix}}_{\mathbf{A}}
\begin{Bmatrix} \mathbf{z}_1 \\ \mathbf{z}_2 \end{Bmatrix}
+ \underbrace{\begin{Bmatrix} \Delta t^2\mathbf{M}^{-1}\mathbf{f} \\ \mathbf{0} \end{Bmatrix}}_{\mathbf{F}}
\]

此即公式 (7)。这是一个关于 \(s\) 的**线性常系数非齐次一阶 ODE 系统**，其精确解可用矩阵指数表示（公式 (10)）。

> **注记**：将二阶 ODE 转化为一阶 ODE 系统是分析线性 ODE 的标准手段。一阶系统 \(\dot{\mathbf{z}} = \mathbf{A}\mathbf{z} + \mathbf{F}\) 的理论完备，精确解、稳定性条件（谱分析）等结论均可直接应用。

---

### 公式 (8)：状态矩阵 \(\mathbf{A}\)

\[
\mathbf{A} = \begin{bmatrix} -\Delta t\mathbf{M}^{-1}\mathbf{C} & -\Delta t^2\mathbf{M}^{-1}\mathbf{K} \\ \mathbf{I} & \mathbf{0} \end{bmatrix} \tag{8}
\]

> **推导细节**：见公式 (7) 的推导过程。\(\mathbf{A}\) 是 \(2n \times 2n\) 的常矩阵（对于给定的 \(\Delta t\)）。其（1,2）分块 \(-\Delta t^2\mathbf{M}^{-1}\mathbf{K}\) 和（1,1）分块 \(-\Delta t\mathbf{M}^{-1}\mathbf{C}\) 分别对应无量纲刚度和阻尼作用；（2,1）分块 \(\mathbf{I}\) 体现了位移是速度的积分这一运动学关系，（2,2）分块 \(\mathbf{0}\) 则说明位移方程不含位移本身（只含速度）。

**\(\mathbf{A}\) 的代数特性（为谱分析铺垫）**

矩阵 \(\mathbf{A}\) 是**反对称结构**的推广（对无阻尼情形）。对无阻尼自由振动（\(\mathbf{C}=\mathbf{0}\), \(\mathbf{f}=\mathbf{0}\)）：

\[
\mathbf{A} = \begin{bmatrix} \mathbf{0} & -\Delta t^2\mathbf{M}^{-1}\mathbf{K} \\ \mathbf{I} & \mathbf{0} \end{bmatrix}
\]

其特征值为纯虚数 \(\lambda = \pm i\omega\Delta t\)（见公式 (23) 的推导），反映了无阻尼振动的保守性。引入阻尼后，特征值实部变为负（稳定系统），导致振幅随时间衰减。

**与代码的对应**：由于 \(\mathbf{A}\) 的尺寸为 \(2n \times 2n\)（\(n\) 可达数万），代码中从不显式构造此矩阵，而是通过 `SolverPadeAx.m` 隐式地完成矩阵-向量乘积 \(\mathbf{A}\mathbf{x}\)：

```matlab
tmp = C*x(1:n) + K*x(n+1:end);   % C̃·z₁ + K̃·z₂
y   = [-dM\tmp ; x(1:n)];         % y = A·x = {-M⁻¹(C̃z₁+K̃z₂); z₁}
```

其中 `C` = \(\Delta t\mathbf{C}\)，`K` = \(\Delta t^2\mathbf{K}\)，`dM` 为 \(\mathbf{M}\) 的 LU 分解。

---

### 公式 (9)：非齐次项 \(\mathbf{F}\)

\[
\mathbf{F} = \begin{Bmatrix} \Delta t^2\mathbf{M}^{-1}\mathbf{f} \\ \mathbf{0} \end{Bmatrix} \tag{9}
\]

**推导**

由公式 (7) 的推导可知，\(\mathbf{F}\) 是外力项 \(\Delta t^2\mathbf{f}\) 经 \(\mathbf{M}^{-1}\) 作用后置于状态向量上半部分的结果：

\[
\mathbf{F}(s) = \begin{Bmatrix} \Delta t^2\mathbf{M}^{-1}\mathbf{f}(t(s)) \\ \mathbf{0} \end{Bmatrix}
\]

下半部分为零，因为位移分量 \(\mathbf{z}_2 = \mathbf{u}\) 的演化方程 \(\mathrm{d}\mathbf{u}/\mathrm{d}s = \mathbf{u}^{\circ}\) 不含外力直接驱动项。

代码中预计算 `Fb = M \ F`（即 \(\mathbf{M}^{-1}(\Delta t^2\mathbf{F}_0)\)，其中 `F` = \(\Delta t^2\mathbf{F}_0\)），对应公式 (9) 的上半部分，用于后续加速度计算和力贡献矩阵的构造。

---

### 公式 (10)：矩阵指数精确解（常数变易公式）

\[
\mathbf{z}(s) = e^{\mathbf{A}s}\mathbf{z}_{n-1} + e^{\mathbf{A}s}\int_0^s e^{-\mathbf{A}\tau}\mathbf{F}(\tau)\,\mathrm{d}\tau \tag{10}
\]

**推导（常数变易法/格林函数法）**

对线性非齐次 ODE 系统 \(\mathrm{d}\mathbf{z}/\mathrm{d}s = \mathbf{A}\mathbf{z} + \mathbf{F}(s)\），其精确解为：

**第一步**：齐次方程 \(\mathrm{d}\mathbf{z}/\mathrm{d}s = \mathbf{A}\mathbf{z}\) 的基础解矩阵为 \(e^{\mathbf{A}s}\)，其中**矩阵指数**定义为：

\[
e^{\mathbf{A}s} = \sum_{k=0}^{\infty} \frac{(\mathbf{A}s)^k}{k!} = \mathbf{I} + \mathbf{A}s + \frac{\mathbf{A}^2 s^2}{2!} + \cdots
\]

**第二步**：假设解的形式为 \(\mathbf{z}(s) = e^{\mathbf{A}s}\mathbf{c}(s)\)（常数变易），代入原方程：

\[
\frac{\mathrm{d}}{\mathrm{d}s}(e^{\mathbf{A}s}\mathbf{c}) = \mathbf{A}e^{\mathbf{A}s}\mathbf{c} + e^{\mathbf{A}s}\dot{\mathbf{c}} = \mathbf{A}(e^{\mathbf{A}s}\mathbf{c}) + \mathbf{F}
\]

化简得 \(e^{\mathbf{A}s}\dot{\mathbf{c}} = \mathbf{F}(s)\)，即 \(\dot{\mathbf{c}} = e^{-\mathbf{A}s}\mathbf{F}(s)\)。

**第三步**：积分（初始条件 \(\mathbf{c}(0) = \mathbf{z}_{n-1}\)）：

\[
\mathbf{c}(s) = \mathbf{z}_{n-1} + \int_0^s e^{-\mathbf{A}\tau}\mathbf{F}(\tau)\,\mathrm{d}\tau
\]

**第四步**：回代得公式 (10)：

\[
\mathbf{z}(s) = e^{\mathbf{A}s}\mathbf{z}_{n-1} + e^{\mathbf{A}s}\int_0^s e^{-\mathbf{A}\tau}\mathbf{F}(\tau)\,\mathrm{d}\tau
\]

**物理解释**：公式 (10) 将解分为两部分：

1. \(e^{\mathbf{A}s}\mathbf{z}_{n-1}\)：**自由响应**，即初始状态在矩阵指数算子作用下的传播；
2. \(e^{\mathbf{A}s}\int_0^s e^{-\mathbf{A}\tau}\mathbf{F}(\tau)\,\mathrm{d}\tau\)：**强迫响应**，即外力通过 Duhamel 积分的卷积贡献。

**实用局限**：公式 (10) 虽然精确，但对大规模系统（\(n\) 很大）计算矩阵指数 \(e^{\mathbf{A}}\) 的代价极高（\(O(n^3)\) 甚至更高），且矩阵 \(\mathbf{A}\) 为 \(2n \times 2n\)，更不实用。这是引入有理近似（公式 (15)）的根本动机。

---

### 公式 (11)：外力多项式展开

\[
\mathbf{F}_n(s) = \sum_{k=0}^{p_t} \mathbf{F}_{mn}^{(k)} (s-0.5)^k \tag{11}
\]

**含义与推导**

为了对公式 (10) 中的 Duhamel 积分进行分析求解，需要对被积函数中的外力 \(\mathbf{F}(s)\) 进行函数近似。此处采用**以时步中点 \(s = 0.5\) 为展开中心的 \(p_t\) 阶多项式**近似：

\[
\mathbf{F}_n(s) \approx \mathbf{F}_{mn}^{(0)} + \mathbf{F}_{mn}^{(1)}(s-0.5) + \mathbf{F}_{mn}^{(2)}(s-0.5)^2 + \cdots + \mathbf{F}_{mn}^{(p_t)}(s-0.5)^{p_t}
\]

其中系数 \(\mathbf{F}_{mn}^{(k)}\) 为矢量，由时步内外力的采样值通过最小二乘或插值确定（详见代码中的 `ForceSeries` 函数，使用 LGL 采样点和 Vandermonde 矩阵的伪逆）。

**选择中点展开的优势**：以 \(s = 0.5\) 为展开中心，使得奇偶项贡献对称分布，有利于误差控制。对比以端点为中心的展开，中点展开具有更好的数值稳定性（条件数更小）。

**精度阶次与多项式度次的关系**：外力展开阶次 \(p_t\) 将等于分子多项式 \(\mathbf{P}\) 的次数 \(N_p\)（后文说明），确保强迫响应的处理精度与自由响应的精度一致。

---

### 公式 (12)：时步终点的精确解

\[
\mathbf{z}_n = e^{\mathbf{A}}\mathbf{z}_{n-1} + \sum_{k=0}^{p_t} \mathbf{B}_k \mathbf{F}_{mn}^{(k)} \tag{12}
\]

**推导**

将公式 (11) 代入公式 (10)，取 \(s = 1\)（时步终点）：

\[
\mathbf{z}(s=1) = e^{\mathbf{A}}\mathbf{z}_{n-1} + e^{\mathbf{A}}\int_0^1 e^{-\mathbf{A}\tau}\left(\sum_{k=0}^{p_t} \mathbf{F}_{mn}^{(k)}(\tau-0.5)^k\right)\mathrm{d}\tau
\]

交换积分与求和顺序（有限和）：

\[
= e^{\mathbf{A}}\mathbf{z}_{n-1} + \sum_{k=0}^{p_t} \left(e^{\mathbf{A}}\int_0^1 (\tau-0.5)^k e^{-\mathbf{A}\tau}\,\mathrm{d}\tau\right)\mathbf{F}_{mn}^{(k)}
\]

定义积分算子：

\[
\mathbf{B}_k \equiv e^{\mathbf{A}}\int_0^1 (\tau-0.5)^k e^{-\mathbf{A}\tau}\,\mathrm{d}\tau \tag{见(13)}
\]

即得公式 (12)。

**物理解释**：\(\mathbf{z}_n = \mathbf{z}(t_n)\) 是第 \(n\) 步末的状态。公式 (12) 将一个时步的传播分解为：

1. 自由响应的传播 \(e^{\mathbf{A}}\mathbf{z}_{n-1}\)：上一步状态经矩阵指数传播；
2. 强迫响应的累积 \(\sum_k \mathbf{B}_k \mathbf{F}_{mn}^{(k)}\)：外力各阶展开系数通过矩阵 \(\mathbf{B}_k\) 的加权贡献。

---

### 公式 (13)：积分算子 \(\mathbf{B}_k\) 的递推公式

\[
\mathbf{B}_k = e^{\mathbf{A}}\int_0^1 (\tau-0.5)^k e^{-\mathbf{A}\tau}\,\mathrm{d}\tau = \mathbf{A}^{-1}\!\left(k\mathbf{B}_{k-1} + (-0.5)^k\bigl(e^{\mathbf{A}} - (-1)^k\mathbf{I}\bigr)\right);\quad k=1,2,\ldots,p_t \tag{13}
\]

**推导（分部积分）**

定义 \(\mathbf{I}_k \equiv \int_0^1 (\tau-0.5)^k e^{-\mathbf{A}\tau}\,\mathrm{d}\tau\)，则 \(\mathbf{B}_k = e^{\mathbf{A}}\mathbf{I}_k\)。

对 \(\mathbf{I}_k\) 进行分部积分，令：

\[
u(\tau) = (\tau-0.5)^k, \quad \mathrm{d}v = e^{-\mathbf{A}\tau}\,\mathrm{d}\tau
\]

则 \(\mathrm{d}u = k(\tau-0.5)^{k-1}\,\mathrm{d}\tau\)，\(v = -\mathbf{A}^{-1}e^{-\mathbf{A}\tau}\)（注意 \(\mathbf{A}\) 与 \(e^{\mathbf{A}\tau}\) 的矩阵函数关系使得积分公式与标量情形相同）。

分部积分公式 \(\int u\,\mathrm{d}v = [uv]_0^1 - \int_0^1 v\,\mathrm{d}u\)：

\[
\mathbf{I}_k = \left[(\tau-0.5)^k \cdot (-\mathbf{A}^{-1}e^{-\mathbf{A}\tau})\right]_{\tau=0}^{\tau=1} + k\mathbf{A}^{-1}\int_0^1 (\tau-0.5)^{k-1}e^{-\mathbf{A}\tau}\,\mathrm{d}\tau
\]

计算边界项：

\[
\left[(\tau-0.5)^k(-\mathbf{A}^{-1}e^{-\mathbf{A}\tau})\right]_0^1
= (0.5)^k(-\mathbf{A}^{-1}e^{-\mathbf{A}}) - (-0.5)^k(-\mathbf{A}^{-1}\mathbf{I})
= -(0.5)^k \mathbf{A}^{-1}e^{-\mathbf{A}} + (-0.5)^k\mathbf{A}^{-1}
\]

因此：

\[
\mathbf{I}_k = -(0.5)^k\mathbf{A}^{-1}e^{-\mathbf{A}} + (-0.5)^k\mathbf{A}^{-1} + k\mathbf{A}^{-1}\mathbf{I}_{k-1}
\]

两端左乘 \(e^{\mathbf{A}}\)（利用 \(e^{\mathbf{A}}\mathbf{A}^{-1} = \mathbf{A}^{-1}e^{\mathbf{A}}\)，即矩阵函数对同一矩阵可交换）：

\[
\mathbf{B}_k = e^{\mathbf{A}}\mathbf{I}_k = -(0.5)^k\mathbf{A}^{-1} + (-0.5)^k\mathbf{A}^{-1}e^{\mathbf{A}} + k\mathbf{A}^{-1}\mathbf{B}_{k-1}
\]

整理（注意 \((-0.5)^k(-1)^k = (-1)^{2k}(0.5)^k = (0.5)^k\)）：

\[
\mathbf{B}_k = \mathbf{A}^{-1}\!\left((-0.5)^k e^{\mathbf{A}} - (0.5)^k\mathbf{I} + k\mathbf{B}_{k-1}\right)
= \mathbf{A}^{-1}\!\left(k\mathbf{B}_{k-1} + (-0.5)^k(e^{\mathbf{A}} - (-1)^k\mathbf{I})\right)
\]

此即公式 (13)。最后一步利用了 \((0.5)^k = (-0.5)^k(-1)^k\)，即 \(-(0.5)^k = -(-0.5)^k(-1)^k = (-0.5)^k \cdot (-(-1)^k)\)，故 \(-(-0.5)^k(-1)^k\mathbf{I} = (-0.5)^k(-(-1)^k\mathbf{I})\)，从而 \((-0.5)^k e^{\mathbf{A}} - (0.5)^k\mathbf{I} = (-0.5)^k(e^{\mathbf{A}} - (-1)^k\mathbf{I})\)。

**递推意义**：公式 (13) 建立了从 \(\mathbf{B}_{k-1}\) 计算 \(\mathbf{B}_k\) 的递推关系，避免了逐次计算多维积分，将计算量降低为仅需矩阵多项式的乘法。

---

### 公式 (14)：\(\mathbf{B}_0\) 的初始值

\[
\mathbf{B}_0 = e^{\mathbf{A}}\int_0^1 e^{-\mathbf{A}\tau}\,\mathrm{d}\tau = \mathbf{A}^{-1}(e^{\mathbf{A}} - \mathbf{I}) \tag{14}
\]

**推导**

公式 (14) 是公式 (13) 在 \(k=0\) 时的特殊情况（也可直接积分验证）。

**直接积分法**：

\[
\mathbf{I}_0 = \int_0^1 e^{-\mathbf{A}\tau}\,\mathrm{d}\tau = \left[-\mathbf{A}^{-1}e^{-\mathbf{A}\tau}\right]_0^1 = -\mathbf{A}^{-1}e^{-\mathbf{A}} + \mathbf{A}^{-1} = \mathbf{A}^{-1}(\mathbf{I} - e^{-\mathbf{A}})
\]

\[
\mathbf{B}_0 = e^{\mathbf{A}}\mathbf{I}_0 = e^{\mathbf{A}}\mathbf{A}^{-1}(\mathbf{I} - e^{-\mathbf{A}}) = \mathbf{A}^{-1}(e^{\mathbf{A}} - \mathbf{I})
\]

**递推法验证**：在公式 (13) 中令 \(k=0\)（定义 \(\mathbf{B}_{-1}\) 可认为不出现）：

\[
\mathbf{B}_0 = \mathbf{A}^{-1}(0\cdot\mathbf{B}_{-1} + (-0.5)^0(e^{\mathbf{A}} - (-1)^0\mathbf{I})) = \mathbf{A}^{-1}(e^{\mathbf{A}} - \mathbf{I}) \quad \checkmark
\]

**物理解释**：\(\mathbf{B}_0\) 对应外力均匀分布（常数力）时的 Duhamel 积分算子，其结果 \(\mathbf{A}^{-1}(e^{\mathbf{A}}-\mathbf{I})\) 正是矩阵指数与单位矩阵之差再乘以 \(\mathbf{A}^{-1}\)，类比标量情形 \(\int_0^1 e^{a\tau}\,\mathrm{d}\tau = (e^a-1)/a = a^{-1}(e^a-1)\)。

---

## 2.2 矩阵指数的有理函数近似

### 公式 (15)：有理近似总纲

\[
e^{\mathbf{A}} \approx \mathbf{R} = \frac{\mathbf{P}}{\mathbf{Q}} \tag{15}
\]

**含义与动机**

公式 (12) 虽然精确，但包含矩阵指数 \(e^{\mathbf{A}}\)，对 \(n\) 自由度系统其计算代价正比于 \((2n)^3 = 8n^3\)，对大规模问题（\(n \sim 10^4\)–\(10^6\)）完全不可行。

公式 (15) 提出用**有理函数**（两个矩阵多项式之商）来近似矩阵指数：

\[
e^{\mathbf{A}} \approx \mathbf{R}(\mathbf{A}) = \mathbf{Q}(\mathbf{A})^{-1}\mathbf{P}(\mathbf{A}) = \mathbf{P}(\mathbf{A})\mathbf{Q}(\mathbf{A})^{-1}
\]

注意：由于 \(\mathbf{P}\) 和 \(\mathbf{Q}\) 均为 \(\mathbf{A}\) 的多项式，它们相互可交换（polynomials in the same matrix commute），因此左除和右除相同：\(\mathbf{Q}^{-1}\mathbf{P} = \mathbf{P}\mathbf{Q}^{-1}\)。

这一近似的计算可行性来自于：多项式矩阵乘法不需要完整的矩阵存储（可以利用稀疏结构），且 \(\mathbf{Q}\mathbf{z}_n = \cdots\) 形式的线性方程组可以通过高效稀疏直接求解器求解（只需分解一次，后续各步重复使用）。

---

### 公式 (16a)(16b)：分子与分母多项式展开

\[
\mathbf{P} = \sum_{i=0}^{N_p} p_i \mathbf{A}^i = p_0\mathbf{I} + p_1\mathbf{A} + \cdots + p_{N_p}\mathbf{A}^{N_p} \tag{16a}
\]

\[
\mathbf{Q} = \sum_{i=0}^{N_q} q_i \mathbf{A}^i = q_0\mathbf{I} + q_1\mathbf{A} + \cdots + q_{N_q}\mathbf{A}^{N_q} \tag{16b}
\]

**含义**

\(\mathbf{P}(\mathbf{A})\) 和 \(\mathbf{Q}(\mathbf{A})\) 是关于矩阵 \(\mathbf{A}\) 的多项式，其系数 \(p_i, q_i \in \mathbb{R}\) 为实数标量。

**归一化条件**：为保证 \(e^{\mathbf{0}} = \mathbf{I}\)（当 \(\mathbf{A} = \mathbf{0}\) 时，\(e^{\mathbf{A}} = \mathbf{I}\)，此时 \(\mathbf{P}(\mathbf{0}) = p_0\mathbf{I}\)，\(\mathbf{Q}(\mathbf{0}) = q_0\mathbf{I}\)，要求 \(p_0/q_0 = 1\)），因此 \(p_0 = q_0\)（可不妨设为 1）。

**次数的含义**：
- \(N_p\)：分子多项式 \(\mathbf{P}\) 的次数。
- \(N_q\)：分母多项式 \(\mathbf{Q}\) 的次数。
- 精度阶次（对 Padé 展开）等于 \(N_p + N_q\)；对本文构造的特殊有理近似，精度阶次为 \(N_q = M\)（M 格式）或 \(M+1\)（(M+1) 格式）。

**与 Padé 展开的联系**：经典 Padé 展开也是公式 (16) 的特例，其中分子和分母系数均由最大精度条件唯一确定（阶次 \(N_p + N_q\) 阶精确）。本文的构造则在精度和数值耗散之间寻求平衡，允许用户通过 \(\rho_\infty\) 参数调节阻尼量。

---

### 公式 (17)：稳定性条件

\[
N_q \ge N_p \tag{17}
\]

**含义与推导**

对于高频振动（\(\omega \to \infty\)，即 \(\lambda = i\omega\Delta t \to i\infty\)），放大因子的行为决定了数值稳定性：

\[
R(\lambda) = \frac{P(\lambda)}{Q(\lambda)} \approx \frac{p_{N_p}\lambda^{N_p}}{q_{N_q}\lambda^{N_q}} = \frac{p_{N_p}}{q_{N_q}}\lambda^{N_p - N_q}
\]

为使高频分量不被放大（必要稳定条件：\(|R(\lambda)| \le 1\) 对所有纯虚数 \(\lambda\) 成立），需要：

- 若 \(N_q > N_p\)：\(|R(\lambda)| \to 0\)（高频完全耗散，无条件稳定）；
- 若 \(N_q = N_p\)：\(|R(\lambda)| \to |p_{N_p}/q_{N_q}| = \rho_\infty \le 1\)（受控耗散，可调节）；
- 若 \(N_q < N_p\)：\(|R(\lambda)| \to \infty\)（**无界增长，不稳定**）。

因此，无条件稳定性要求 \(N_q \ge N_p\)，即公式 (17)。这也对应公式 (33) 的前两种情况（\(N_q > N_p\) 和 \(N_q = N_p\) 均稳定）。

---

### 公式 (18)：有理近似时步方程

\[
\mathbf{Q}\mathbf{z}_n = \mathbf{P}\mathbf{z}_{n-1} + \sum_{k=0}^{p_t} \mathbf{C}_k \mathbf{F}_{mn}^{(k)} \tag{18}
\]

**推导**

将公式 (15)（\(e^{\mathbf{A}} \approx \mathbf{P}/\mathbf{Q} = \mathbf{Q}^{-1}\mathbf{P}\)）代入公式 (12)，并将 \(\mathbf{B}_k\) 乘以 \(\mathbf{Q}\) 定义为 \(\mathbf{C}_k \equiv \mathbf{Q}\mathbf{B}_k\)：

\[
\mathbf{z}_n \approx \mathbf{Q}^{-1}\mathbf{P}\,\mathbf{z}_{n-1} + \sum_{k=0}^{p_t} \mathbf{B}_k \mathbf{F}_{mn}^{(k)}
\]

两端左乘 \(\mathbf{Q}\)：

\[
\mathbf{Q}\mathbf{z}_n = \mathbf{P}\mathbf{z}_{n-1} + \sum_{k=0}^{p_t} \underbrace{\mathbf{Q}\mathbf{B}_k}_{\mathbf{C}_k} \mathbf{F}_{mn}^{(k)}
\]

即公式 (18)。

**关键意义**：公式 (18) 是整个时步算法的核心方程。通过左乘 \(\mathbf{Q}\)，将原本含矩阵指数逆运算的 \(\mathbf{z}_n = \mathbf{Q}^{-1}(\cdots)\) 转化为**多项式矩阵方程** \(\mathbf{Q}\mathbf{z}_n = \cdots\)，即求解关于 \(\mathbf{z}_n\) 的线性代数方程组。由于 \(\mathbf{Q}(\mathbf{A})\) 是 \(\mathbf{A}\) 的多项式，其作用在 \(\mathbf{z}_n\) 上不需要显式构造矩阵，可以通过递推分解（Section 4）高效实现。

**截断误差阶次**：近似误差来自于 \(e^{\mathbf{A}} \approx \mathbf{Q}^{-1}\mathbf{P}\) 的截断。对 M 格式（\(N_q = N_p = M\)），精度为 \(M\) 阶；对 (M+1) 格式，精度为 \(M+1\) 阶。外力展开的截断阶次 \(p_t = N_p\) 与格式精度匹配，不降低整体精度阶次。

---

### 公式 (19)：外力贡献矩阵 \(\mathbf{C}_k\) 的递推公式

\[
\mathbf{C}_k = \mathbf{Q}\mathbf{B}_k = \mathbf{A}^{-1}\!\left(k\mathbf{C}_{k-1} + (-0.5)^k(\mathbf{P} - (-1)^k\mathbf{Q})\right);\quad k=1,2,\ldots,p_t \tag{19}
\]

**推导**

由定义 \(\mathbf{C}_k = \mathbf{Q}\mathbf{B}_k\) 和公式 (13)：

\[
\mathbf{C}_k = \mathbf{Q}\mathbf{B}_k = \mathbf{Q}\cdot\mathbf{A}^{-1}\!\left(k\mathbf{B}_{k-1} + (-0.5)^k(e^{\mathbf{A}} - (-1)^k\mathbf{I})\right)
\]

利用 \(\mathbf{Q}\mathbf{A}^{-1} = \mathbf{A}^{-1}\mathbf{Q}\)（同一矩阵的多项式可交换）以及 \(\mathbf{Q}\mathbf{B}_{k-1} = \mathbf{C}_{k-1}\)：

\[
\mathbf{C}_k = \mathbf{A}^{-1}\!\left(k\mathbf{Q}\mathbf{B}_{k-1} + (-0.5)^k(\mathbf{Q}e^{\mathbf{A}} - (-1)^k\mathbf{Q})\right)
= \mathbf{A}^{-1}\!\left(k\mathbf{C}_{k-1} + (-0.5)^k(\mathbf{Q}e^{\mathbf{A}} - (-1)^k\mathbf{Q})\right)
\]

由有理近似 \(e^{\mathbf{A}} \approx \mathbf{Q}^{-1}\mathbf{P}\)，有 \(\mathbf{Q}e^{\mathbf{A}} \approx \mathbf{P}\)，代入：

\[
\mathbf{C}_k \approx \mathbf{A}^{-1}\!\left(k\mathbf{C}_{k-1} + (-0.5)^k(\mathbf{P} - (-1)^k\mathbf{Q})\right)
\]

即公式 (19)。

**递推操作的实现**：在代码中 `TimeIntgCoeffForce.m` 实现了此递推，注意 \(\mathbf{A}^{-1}\) 的作用是对系数多项式降阶（要求 \(\mathbf{C}_k|_{\mathbf{A}=\mathbf{0}} = \mathbf{0}\)，即常数项为零，可以从系数向量中"去掉"最低次项），避免了实际求 \(\mathbf{A}^{-1}\)。

---

### 公式 (20)：\(\mathbf{C}_0\) 的初始值

\[
\mathbf{C}_0 = \mathbf{Q}\mathbf{B}_0 = \mathbf{A}^{-1}(\mathbf{P} - \mathbf{Q}) \tag{20}
\]

**推导**

将公式 (14) 代入 \(\mathbf{C}_0 = \mathbf{Q}\mathbf{B}_0\)：

\[
\mathbf{C}_0 = \mathbf{Q}\cdot\mathbf{A}^{-1}(e^{\mathbf{A}} - \mathbf{I}) = \mathbf{A}^{-1}(\mathbf{Q}e^{\mathbf{A}} - \mathbf{Q}) \approx \mathbf{A}^{-1}(\mathbf{P} - \mathbf{Q})
\]

此处同样使用了近似 \(\mathbf{Q}e^{\mathbf{A}} \approx \mathbf{P}\)。

**也可由公式 (19) 令 \(k=0\) 验证**：

\[
\mathbf{C}_0 = \mathbf{A}^{-1}(0\cdot\mathbf{C}_{-1} + (-0.5)^0(\mathbf{P} - (-1)^0\mathbf{Q})) = \mathbf{A}^{-1}(\mathbf{P} - \mathbf{Q}) \quad \checkmark
\]

**物理意义**：\(\mathbf{C}_0\) 对应外力在时步内均匀分布时的贡献矩阵，是"等效静力"贡献的度量。当外力为常数时，\(\mathbf{C}_0\mathbf{F}_{mn}^{(0)}\) 给出力的积分效应。\(\mathbf{P} - \mathbf{Q}\) 的零常数项（因为 \(p_0 = q_0\)）保证了 \(\mathbf{A}^{-1}(\mathbf{P}-\mathbf{Q})\) 的良定义（无需 \(\mathbf{A}\) 可逆的强条件，只需多项式系数的结构）。

**截断阶次与外力展开的匹配**（\(p_t = N_p\)）：外力展开最高阶项 \(\mathbf{F}_{mn}^{(N_p)}\) 对应的积分矩阵 \(\mathbf{C}_{N_p}\) 由公式 (19) 给出，精度阶次与 \(\mathbf{P}\) 的次数一致，从而整体精度不因外力处理而降低。

---

## 2.3 数值耗散与频散特性

### 公式 (21)：结构固有频率特征值问题

\[
\mathbf{K}\boldsymbol{\phi} = \omega^2\mathbf{M}\boldsymbol{\phi} \tag{21}
\]

**含义与推导**

对无阻尼自由振动情形（\(\mathbf{C}=\mathbf{0}\), \(\mathbf{f}=\mathbf{0}\)），公式 (1) 化为：

\[
\mathbf{M}\ddot{\mathbf{u}} + \mathbf{K}\mathbf{u} = \mathbf{0}
\]

设谐波解 \(\mathbf{u}(t) = \boldsymbol{\phi}\,e^{i\omega t}\)，代入得广义特征值问题：

\[
-\omega^2\mathbf{M}\boldsymbol{\phi} + \mathbf{K}\boldsymbol{\phi} = \mathbf{0} \quad \Rightarrow \quad \mathbf{K}\boldsymbol{\phi} = \omega^2\mathbf{M}\boldsymbol{\phi}
\]

其中 \(\omega > 0\) 为**固有圆频率**（natural angular frequency），\(\boldsymbol{\phi}\) 为对应的**模态振型**（mode shape）。对 \(\mathbf{M}\) 正定、\(\mathbf{K}\) 正半定的系统，存在 \(n\) 对实特征对 \((\omega_i^2, \boldsymbol{\phi}_i)\)。

**谱分析的意义**：时间积分格式的精度和稳定性可以通过对单个模态系统（scalar \(\omega\)）的分析来完整描述，因为系统的模态叠加原理允许将多自由度问题解耦为一系列单自由度问题。

---

### 公式 (22)：状态矩阵 \(\mathbf{A}\) 的特征值问题

\[
\mathbf{A}\boldsymbol{\psi} = \lambda\boldsymbol{\psi} \tag{22}
\]

**含义**

对状态矩阵 \(\mathbf{A}\)（公式 (8)）求解特征值问题，寻找标量 \(\lambda\) 和 \(2n\) 维向量 \(\boldsymbol{\psi}\) 使得 \(\mathbf{A}\boldsymbol{\psi} = \lambda\boldsymbol{\psi}\)。

由于 \(\mathbf{A}\) 不是实对称矩阵（对无阻尼情形是"反对称型"），其特征值一般为**复数**。了解 \(\mathbf{A}\) 的特征值是进行谱分析的基础——矩阵指数 \(e^{\mathbf{A}}\) 在 \(\mathbf{A}\) 的特征基下的作用完全由 \(e^{\lambda_i}\) 描述，因此时间积分方案对每个模态的放大因子为 \(R(\lambda_i)\)（公式 (26)）。

---

### 公式 (23)：\(\mathbf{A}\) 的特征值与特征向量

\[
\lambda = \pm\,i\omega\Delta t, \quad \boldsymbol{\psi} = \begin{Bmatrix} \pm(i\omega\Delta t)\boldsymbol{\phi} \\ \boldsymbol{\phi} \end{Bmatrix} \tag{23}
\]

**推导**

对无阻尼自由振动，\(\mathbf{A} = \begin{bmatrix} \mathbf{0} & -\Delta t^2\mathbf{M}^{-1}\mathbf{K} \\ \mathbf{I} & \mathbf{0} \end{bmatrix}\)。

设 \(\boldsymbol{\psi} = \{\boldsymbol{\psi}_1;\,\boldsymbol{\psi}_2\}\)，则 \(\mathbf{A}\boldsymbol{\psi} = \lambda\boldsymbol{\psi}\) 写成分块形式：

\[
\begin{cases}
-\Delta t^2\mathbf{M}^{-1}\mathbf{K}\,\boldsymbol{\psi}_2 = \lambda\boldsymbol{\psi}_1 \\
\boldsymbol{\psi}_1 = \lambda\boldsymbol{\psi}_2
\end{cases}
\]

由第二式 \(\boldsymbol{\psi}_1 = \lambda\boldsymbol{\psi}_2\) 代入第一式：

\[
-\Delta t^2\mathbf{M}^{-1}\mathbf{K}\,\boldsymbol{\psi}_2 = \lambda^2\boldsymbol{\psi}_2
\quad \Rightarrow \quad
\mathbf{K}\,\boldsymbol{\psi}_2 = -\frac{\lambda^2}{\Delta t^2}\mathbf{M}\,\boldsymbol{\psi}_2
\]

与公式 (21) \(\mathbf{K}\boldsymbol{\phi} = \omega^2\mathbf{M}\boldsymbol{\phi}\) 比较：

\[
-\frac{\lambda^2}{\Delta t^2} = \omega^2 \quad \Rightarrow \quad \lambda^2 = -\omega^2\Delta t^2 \quad \Rightarrow \quad \lambda = \pm\,i\omega\Delta t
\]

因此 \(\boldsymbol{\psi}_2 = \boldsymbol{\phi}\)，\(\boldsymbol{\psi}_1 = \lambda\boldsymbol{\phi} = \pm\,i\omega\Delta t\,\boldsymbol{\phi}\)，即：

\[
\boldsymbol{\psi} = \begin{Bmatrix} \pm(i\omega\Delta t)\boldsymbol{\phi} \\ \boldsymbol{\phi} \end{Bmatrix}
\]

**物理解释**：\(\mathbf{A}\) 的每对特征值 \(\pm\,i\omega\Delta t\) 对应无阻尼振动的频率 \(\omega\)。在 \(\Delta t \to 0\) 的极限下，\(e^{\lambda} = e^{\pm i\omega\Delta t} \approx 1 \pm i\omega\Delta t\)（无耗散，保幅），与精确解一致。有耗散时，\(e^{\lambda}\) 的绝对值小于 1，但数值方案的放大因子 \(R(\lambda)\) 也应近似 \(e^{\lambda}\)（精确解），其模 \(\rho = |R(\lambda)|< 1\) 反映了数值方案引入的额外（人工）耗散。

---

### 公式 (24)：谱分析中选取的特征值

\[
\lambda = i\omega\Delta t \tag{24}
\]

**含义**

由于 \(\lambda = \pm\,i\omega\Delta t\) 是共轭对（公式 (23)），且放大因子满足 \(R(\bar{\lambda}) = \overline{R(\lambda)}\)（系数为实数时），因此只需分析 \(\lambda = +i\omega\Delta t\)（正频率分支），放大因子为 \(R(i\omega\Delta t)\)，其共轭情形自动给出。

这是谱分析的**标准约定**，也是绘制谱半径 \(\rho\) 和相位误差随 \(\Delta t/T\)（时间步与周期之比）变化曲线的基础。

---

### 公式 (25)：用周期 \(T\) 表示特征值

\[
\lambda = i\,2\pi\,\frac{\Delta t}{T} \tag{25}
\]

**推导**

利用圆频率与周期的关系 \(\omega = 2\pi/T\)，代入公式 (24)：

\[
\lambda = i\omega\Delta t = i\cdot\frac{2\pi}{T}\cdot\Delta t = i\,2\pi\,\frac{\Delta t}{T}
\]

**意义**：公式 (25) 将谱分析的参数统一为**无量纲频率比** \(\Delta t/T\)（时步大小与周期之比），这是时间积分方法谱性质的标准度量。

- 当 \(\Delta t/T \ll 1\)（细时步）：\(\lambda \approx 0\)，精确积分，误差小；
- 当 \(\Delta t/T \sim 1\) 或更大：\(\lambda\) 较大，有理近似误差显著，数值耗散和频散不可忽略；
- 当 \(\Delta t/T \to \infty\)（极粗时步，高频模态）：\(|\lambda| \to \infty\)，此时 \(\rho = |R(\lambda)| \to \rho_\infty\)（公式 (33)）。

---

### 公式 (26)：标量放大因子

\[
R(\lambda) = \frac{P(\lambda)}{Q(\lambda)} \tag{26}
\]

**推导**

将公式 (16) 中的矩阵 \(\mathbf{A}\) 替换为标量特征值 \(\lambda\)（对无阻尼单模态系统，\(\mathbf{A}\) 的特征空间对应方程的一维化）：

\[
P(\lambda) = \sum_{i=0}^{N_p} p_i\lambda^i, \quad Q(\lambda) = \sum_{i=0}^{N_q} q_i\lambda^i
\]

则 \(R(\lambda) = P(\lambda)/Q(\lambda)\) 是矩阵有理近似 \(\mathbf{R} = \mathbf{P}/\mathbf{Q}\) 在特征值 \(\lambda\) 处的标量版本。

**物理意义**：\(R(\lambda)\) 是时间积分格式对频率为 \(\omega\)（\(\lambda = i\omega\Delta t\)）的单模态系统，在一个时步 \(\Delta t\) 内的**放大因子**（amplification factor）。精确解的放大因子为 \(e^\lambda = e^{i\omega\Delta t}\)（模为 1，无耗散）。数值格式的 \(R(\lambda)\) 偏离 \(e^\lambda\) 的程度反映了截断误差；\(|R(\lambda)|\) 偏离 1 的程度反映了数值耗散。

时步方程 (18) 在单模态情形下退化为标量递推 \(z_n = R(\lambda)\,z_{n-1}\)（忽略外力项），经过 \(N\) 步后 \(z_N = R(\lambda)^N z_0\)。

---

### 公式 (27)：谱半径（数值耗散度量）

\[
\rho = |R| \tag{27}
\]

**含义**

\(\rho\) 是放大因子 \(R(\lambda)\) 的**模（绝对值）**，刻画了每个时步的幅值变化：

- \(\rho = 1\)：**无数值耗散**，幅值不变；
- \(\rho < 1\)：**数值耗散**，每步幅值衰减因子为 \(\rho\)；
- \(\rho > 1\)：**数值增长**（不稳定）。

**稳定性条件**：对所有 \(\lambda = i\omega\Delta t\)（\(\omega > 0\)，\(\Delta t > 0\)），要求 \(\rho = |R(i\omega\Delta t)| \le 1\)——这是无条件稳定性的谱条件。

**可控的高频耗散**：对高频模态（\(\Delta t/T \gg 1\)），\(\rho \to \rho_\infty\)（公式 (33)）。选取 \(\rho_\infty < 1\) 可确保高频分量快速衰减，而对低频模态（\(\Delta t/T \ll 1\)），\(\rho \approx 1\)（几乎无耗散）。这种特性正是本文方法的核心设计目标。

---

### 公式 (28)：相位角（数值频散度量）

\[
\bar{\Omega} = \arg(R) \tag{28}
\]

**含义**

\(\bar{\Omega}\) 是放大因子 \(R\) 的**辐角（相位角）**，即复数 \(R\) 在复平面上的角度：

\[
\bar{\Omega} = \arg(R) = \mathrm{Im}(\ln R) = \arctan\!\left(\frac{\mathrm{Im}(R)}{\mathrm{Re}(R)}\right)
\]

精确解对应 \(\bar{\Omega}_{\text{exact}} = \mathrm{Im}(\lambda) = \omega\Delta t\)（单个时步的相位推进量）。数值方案的 \(\bar{\Omega}\) 偏离 \(\omega\Delta t\) 反映了**数值频散（numerical dispersion）**——不同频率分量以不同速度传播的现象。

---

### 公式 (29)：放大因子的极坐标形式

\[
R = \rho\,e^{i\bar{\Omega}} \tag{29}
\]

**含义与推导**

任意复数都可以用极坐标形式 \(re^{i\theta}\) 表示。将公式 (27) 的模 \(\rho = |R|\) 和公式 (28) 的辐角 \(\bar{\Omega} = \arg(R)\) 组合：

\[
R = |R|\,e^{i\,\arg(R)} = \rho\,e^{i\bar{\Omega}}
\]

**意义**：极坐标形式将放大因子的耗散性质（\(\rho\)）和频散性质（\(\bar{\Omega}\)）清晰分离。每个时步，解的幅值乘以 \(\rho\)，相位增加 \(\bar{\Omega}\)。这是分别分析数值耗散（公式 (27)）和数值频散（公式 (28)–(32)）的数学基础。

---

### 公式 (30)：数值角频率

\[
\bar{\omega} = \frac{\bar{\Omega}}{\Delta t} \tag{30}
\]

**推导**

精确解在一个时步 \(\Delta t\) 内的相位推进量为 \(\omega\Delta t\)（物理角频率乘以时步），对应物理角频率 \(\omega\)。数值格式在一个时步内相位推进 \(\bar{\Omega}\)，因此数值方案隐含的**数值角频率**为：

\[
\bar{\omega} = \frac{\bar{\Omega}}{\Delta t}
\]

**意义**：\(\bar{\omega}\) 是数值方案"感知"到的振动频率，与物理频率 \(\omega\) 的偏差 \(\bar{\omega} - \omega\) 衡量了频率失真的绝对量。通常用相对量（相对周期误差，公式 (32)）衡量。

---

### 公式 (31)：数值周期

\[
\bar{T} = \frac{2\pi}{\bar{\omega}} = \frac{2\pi\Delta t}{\bar{\Omega}} \tag{31}
\]

**推导**

由 \(\bar{T} = 2\pi/\bar{\omega}\)（圆频率与周期的关系），代入公式 (30)：

\[
\bar{T} = \frac{2\pi}{\bar{\Omega}/\Delta t} = \frac{2\pi\Delta t}{\bar{\Omega}}
\]

**对比精确周期**：物理周期 \(T = 2\pi/\omega\)，数值周期 \(\bar{T} = 2\pi\Delta t/\bar{\Omega}\)。精确情形下 \(\bar{\Omega} = \omega\Delta t\)，则 \(\bar{T} = 2\pi\Delta t/(\omega\Delta t) = 2\pi/\omega = T\)（无误差）。数值格式使 \(\bar{\Omega}\) 偏离 \(\omega\Delta t\)，导致 \(\bar{T} \ne T\)。

---

### 公式 (32)：相对周期误差

\[
\frac{\bar{T} - T}{T} = \frac{\omega\Delta t}{\bar{\Omega}} - 1 \tag{32}
\]

**推导**

\[
\frac{\bar{T} - T}{T} = \frac{\bar{T}}{T} - 1 = \frac{2\pi\Delta t/\bar{\Omega}}{2\pi/\omega} - 1 = \frac{\omega\Delta t}{\bar{\Omega}} - 1
\]

**物理解释**：

- 若 \(\bar{\Omega} < \omega\Delta t\)（数值方案的相位推进量偏小）：\(\bar{\omega} < \omega\)，即 \(\bar{T} > T\)，相对误差为正，称为**周期延长（period elongation）**，是隐式格式的典型行为；
- 若 \(\bar{\Omega} > \omega\Delta t\)：周期缩短；
- 误差随 \(\Delta t/T\) 增大而增大；高阶格式的误差随 \((\Delta t/T)^{2M}\) 衰减（\(M\) 阶格式），故高阶方法在给定精度要求下允许使用更大的时步。

**数值频散对波传播的影响**：在多自由度波传播问题中，不同频率的波以不同的数值速度传播（因 \(\bar{\omega}/\omega\) 随 \(\omega\) 变化），导致波形的畸变。相对周期误差越小，数值频散越弱，波形保真度越高。

---

### 公式 (33)：高频极限谱半径

\[
\rho_\infty = \begin{cases}
0, & \text{当 } N_q > N_p \\[4pt]
\left|\dfrac{p_{N_p}}{q_{N_q}}\right|, & \text{当 } N_q = N_p \\[4pt]
\infty, & \text{当 } N_q < N_p
\end{cases} \tag{33}
\]

**推导**

当 \(\omega \to \infty\)（即 \(\lambda = i\omega\Delta t \to i\cdot\infty\)，\(|\lambda| \to \infty\)），放大因子趋于：

\[
R(\lambda) = \frac{P(\lambda)}{Q(\lambda)} = \frac{p_0 + p_1\lambda + \cdots + p_{N_p}\lambda^{N_p}}{q_0 + q_1\lambda + \cdots + q_{N_q}\lambda^{N_q}}
\approx \frac{p_{N_p}\lambda^{N_p}}{q_{N_q}\lambda^{N_q}} = \frac{p_{N_p}}{q_{N_q}}\cdot\lambda^{N_p-N_q}
\]

对 \(\lambda = i\omega\Delta t\)，\(|\lambda| = \omega\Delta t \to \infty\)：

- \(N_q > N_p\)：\(|R| \sim \frac{|p_{N_p}|}{|q_{N_q}|}|\lambda|^{N_p-N_q} \to 0\)，故 \(\rho_\infty = 0\)；
- \(N_q = N_p\)：\(|R| \to \left|\frac{p_{N_p}}{q_{N_q}}\right|\)，故 \(\rho_\infty = |p_{N_p}/q_{N_q}|\)；
- \(N_q < N_p\)：\(|R| \to \infty\)（不稳定），故 \(\rho_\infty = \infty\)。

结合稳定性条件 (17)（\(N_q \ge N_p\)），实际只用前两种情形。

**关键设计参数**：\(\rho_\infty\) 是本文方法最重要的用户参数，直接控制高频模态的数值耗散：

- **\(\rho_\infty = 0\)**（最大耗散）：高频分量在单个时步内被完全压制（幅值变为零），对刚性问题最安全；
- **\(\rho_\infty = 1\)**（无耗散）：无数值耗散，保持精度，但高频寄生振荡不被压制；
- **\(0 < \rho_\infty < 1\)**：可调节的耗散量，在耗散与精度之间折中。

**与分子多项式系数的关系**（\(N_q = N_p = M\) 时）：\(\rho_\infty = |p_{N_p}| = |p_M|\)（当 \(q_{N_q} = 1\) 时），这直接对应代码中 `pcoe(end)` 的绝对值（`InitSchemeRExpn.m` 的第 27 行：`disp(['Selected rhoInfty = ', num2str(abs(pcoe(end)), '%.5f')])`）。

---

## 公式间的内在联系与推导路径总结

### 主推导链（2.1 → 2.2）

公式之间的逻辑推导链如下图所示：

```
(1) 运动方程（二阶 ODE）
 │
 ├── (2a) 初始条件
 │
 ↓
(3) 无量纲时间 s ∈ [0,1]
 │
 ↓
(4) 链式法则：u̇ = u°/Δt, ü = u°°/Δt²
 │
 ↓
(5) 无量纲运动方程（M u°° + ΔtC u° + Δt²K u = Δt²f）
 │
 ↓
(6) 状态向量 z = {u°; u}
 │
 ↓
(7) 一阶 ODE 系统（ż = Az + F）
 │
 ├── (8) 状态矩阵 A = [[-ΔtM⁻¹C, -Δt²M⁻¹K]; [I, 0]]
 └── (9) 外力项 F = {Δt²M⁻¹f; 0}
 │
 ↓
(10) 矩阵指数精确解（常数变易公式）
 │
 ↓（截断级数 + 分部积分）
(11) 外力多项式展开（以 s=0.5 为中心，p_t 阶）
 │
 ↓（代入 (10), 取 s=1）
(12) 时步精确解（z_n = e^A z_{n-1} + ΣB_k F_mn^(k)）
 │
 ├── (14) B_0 = A⁻¹(e^A - I)（初始值）
 └── (13) B_k 递推（分部积分得到递推式）
 │
 ↓（有理近似替换 e^A ≈ P/Q，Q 左乘，C_k = Q·B_k）
(15) 有理近似总纲
 │
 ├── (16a,b) 多项式 P, Q 展开
 └── (17) 稳定条件 N_q ≥ N_p
 │
 ↓
(18) 时步方程（Q·z_n = P·z_{n-1} + ΣC_k F_mn^(k)）
 │
 ├── (20) C_0 = A⁻¹(P - Q)（初始值）
 └── (19) C_k 递推（来自 B_k 递推 × Q）
```

### 谱分析链（2.3）

```
(21) K φ = ω² M φ（物理固有频率）
 │
 ↓（代入 A 的特征值问题）
(22) A ψ = λψ
 │
 ↓（求解分块特征方程）
(23) λ = ±iωΔt（A 的特征值），ψ = {±iωΔt·φ; φ}
 │
 ↓（选正频率分支）
(24) λ = iωΔt
 │
 ↓（代入 ω = 2π/T）
(25) λ = i2π(Δt/T)
 │
 ↓（将 A 标量化为 λ，放大因子为 P(λ)/Q(λ)）
(26) R(λ) = P(λ)/Q(λ)（标量放大因子）
 │
 ├── (27) ρ = |R|（谱半径 → 耗散）
 │
 └── (28) Ω̄ = arg(R)（相位角 → 频散）
      │
      ↓（极坐标合并）
      (29) R = ρ·e^{iΩ̄}
      │
      ↓（Ω̄/Δt）
      (30) ω̄ = Ω̄/Δt（数值角频率）
      │
      ↓（2π/ω̄）
      (31) T̄ = 2πΔt/Ω̄（数值周期）
      │
      ↓（(T̄-T)/T）
      (32) 相对周期误差
 │
 ↓（|λ| → ∞ 极限）
(33) ρ_∞ = |p_{Np}/q_{Nq}|（高频极限谱半径）
```

### 三个子节的桥接关系

| 要素 | 来源 | 在后续章节的角色 |
|---|---|---|
| 公式 (15)–(17)：有理近似框架 | 2.2 节 | 第 3 节的设计目标（构造满足精度和 \(\rho_\infty\) 要求的 \(P\), \(Q\)） |
| 公式 (18)：时步代数方程 | 2.2 节 | 第 4 节的算法基础（将方程分解为复合子步） |
| 公式 (33)：\(\rho_\infty\) 的含义 | 2.3 节 | 第 3 节的约束条件（\(p_{N_p} = \pm\rho_\infty\) 确定根 \(r\)，见公式 (42)） |
| 公式 (32)：相对周期误差 | 2.3 节 | 第 3 节中选择"最优"根 \(r\) 的判据（最小化低频区域的相对周期误差） |
| 公式 (26)–(27)：谱半径 | 2.3 节 | 第 3 节的稳定性验证（要求所有 \(\lambda = i\omega\Delta t\) 时 \(\rho \le 1\)） |

---

### 关键近似的误差控制

从精确解（公式 (12)）到近似时步方程（公式 (18)），引入了两处近似：

1. **外力多项式截断**（公式 (11)）：外力展开至 \(p_t = N_p\) 阶，引入 \(O(\Delta t^{N_p+1})\) 的力处理误差，与格式本身精度 \(O(\Delta t^{N_q+N_p})\)（Padé）或 \(O(\Delta t^M)\)（本文 M 格式）相匹配，不降低整体精度。

2. **矩阵指数有理近似**（公式 (15)）：引入 \(O(\Delta t^{N_p+N_q+1})\) 或 \(O(\Delta t^{M+1})\) 的截断误差，由精度阶次和 \(\rho_\infty\) 值共同决定。

这两处近似的阶次对齐是本文格式设计的精髓——它保证了"外力处理的精度不拖累格式精度"的优良性质（论文观察第 1 条）。

---

*本文档覆盖了第 2 节中全部 33 个公式的详细推导与解释，以及公式间的完整逻辑关联图。*
