# 综合总结报告

> **文献来源**：Chongmin Song & Xiaoran Zhang (2024). "High-order composite implicit time integration schemes based on rational approximations for elastodynamics." *Computer Methods in Applied Mechanics and Engineering*, 418, 116473.  
> DOI: https://doi.org/10.1016/j.cma.2023.116473

---

## 第一部分：摘要简明概括

本文提出了一种基于矩阵指数有理函数近似的高阶复合隐式时间积分格式新构造方法，用于求解弹性动力学中的结构动力方程。核心思路是：选取分母具有**单一重根**的有理函数来近似矩阵指数，使得所有子步共享**同一有效刚度矩阵**，从而每个子步的隐式方程与梯形法具有相同形式，可高效求解大规模稀疏系统。由此产生两类格式：**M 格式**（M 个子步达到 M 阶精度，用户可通过参数 \(\rho_{\infty}\) 调节数值耗散量）和 **\((M+1)\) 格式**（M 个子步达到 \((M+1)\) 阶精度，耗散量内置、不可调）。两类格式的精度均不受外部激励和物理阻尼的影响。数值算例验证：高阶复合格式在引入数值耗散、允许大时步方面对波传播问题具有显著优势。

---

## 第二部分：问题、核心公式、求解方法与代码对应

### 2.1 问题背景与核心挑战

结构动力学有限元计算的时间积分格式需同时满足以下要求（Hilber & Hughes, 1978）：

1. **无条件稳定性**；
2. **每步只需求解一组隐式方程**；
3. **至少二阶精度**；
4. **高频模态的可控数值耗散**；
5. **自启动性**。

现有高阶格式（如 Padé 展开格式）虽然精度高，但每个递推步的**有效刚度矩阵各不相同**（甚至为复数矩阵），导致直接求解器必须对多个不同矩阵分别进行 LU 分解，内存占用大、效率低。

**本文要解决的核心问题**：如何构造高阶（≥3 阶）隐式复合时间积分格式，使其在保持高精度和可控数值耗散的同时，所有子步使用**同一个实数有效刚度矩阵**？

---

### 2.2 问题的数学起点：运动方程

结构动力学离散化后的运动方程为：

\[
\mathbf{M}\ddot{\mathbf{u}}(t) + \mathbf{C}\dot{\mathbf{u}}(t) + \mathbf{K}\mathbf{u}(t) = \mathbf{f}(t) \tag{1}
\]

其中 \(\mathbf{M}\)、\(\mathbf{C}\)、\(\mathbf{K}\) 分别为质量、阻尼、刚度矩阵，\(\mathbf{u}\)、\(\dot{\mathbf{u}}\)、\(\ddot{\mathbf{u}}\) 为位移、速度和加速度向量，\(\mathbf{f}(t)\) 为外力向量。

**公式解读**：这是一个 \(n\) 维二阶常微分方程组（\(n\) 为有限元自由度数），时间积分的本质就是数值求解此方程的初值问题。由于 \(n\) 可达数百万，计算效率至关重要。

---

### 2.3 无量纲化与状态空间变换

引入无量纲时间 \(s \in [0,1]\)：

\[
t(s) = t_{n-1} + s\Delta t \tag{3}
\]

速度和加速度的无量纲变换（链式法则）：

\[
\dot{\mathbf{u}} = \frac{1}{\Delta t}\mathbf{u}^{\circ},\quad \ddot{\mathbf{u}} = \frac{1}{\Delta t^2}\mathbf{u}^{\circ\circ} \tag{4}
\]

其中上标 \(\circ\) 表示对无量纲时间 \(s\) 求导。代入公式 (1) 并乘以 \(\Delta t^2\)，得无量纲运动方程：

\[
\mathbf{M}\mathbf{u}^{\circ\circ} + \Delta t\mathbf{C}\,\mathbf{u}^{\circ} + \Delta t^2\mathbf{K}\,\mathbf{u} = \Delta t^2\mathbf{f} \tag{5}
\]

引入**状态向量**将二阶方程化为一阶：

\[
\mathbf{z} = \begin{Bmatrix} \mathbf{u}^{\circ} \\ \mathbf{u} \end{Bmatrix} \tag{6}
\]

由此得到一阶 ODE 系统：

\[
\dot{\mathbf{z}} \equiv \frac{\mathrm{d}\mathbf{z}}{\mathrm{d}s} = \mathbf{A}\mathbf{z} + \mathbf{F} \tag{7}
\]

其中**状态矩阵**为：

\[
\mathbf{A} = \begin{bmatrix} -\Delta t\mathbf{M}^{-1}\mathbf{C} & -\Delta t^2\mathbf{M}^{-1}\mathbf{K} \\ \mathbf{I} & \mathbf{0} \end{bmatrix} \tag{8}
\]

**非齐次项**为：

\[
\mathbf{F} = \begin{Bmatrix} \Delta t^2\mathbf{M}^{-1}\mathbf{f} \\ \mathbf{0} \end{Bmatrix} \tag{9}
\]

**公式解读**：状态矩阵 \(\mathbf{A}\) 是 \(2n \times 2n\) 的矩阵，其特征值对无阻尼自由振动为纯虚数 \(\lambda = \pm i\omega\Delta t\)，反映系统振动的本质。式 (7) 的精确解由矩阵指数给出（见公式 (10)），但对大规模问题计算 \(e^{\mathbf{A}}\) 极为昂贵，这正是本文用有理函数近似代替的动机。

**代码对应**（`TimeSolverRExpn.m` 第 18–25 行）：

```matlab
K = dt*dt*sparse(K);   % 对应 Δt²K（公式 5 中的 Δt²K）
C = dt*sparse(C);       % 对应 ΔtC
M = sparse(M);          % 质量矩阵
F = dt*dt*F;            % 对应 Δt²f
v0 = dt*v0;             % 无量纲初速度 u°₀ = Δt·u̇₀（公式 4）
```

注意代码从不显式构造矩阵 \(\mathbf{A}\)，而是通过 `SolverPadeAx.m` 计算矩阵-向量乘积 \(\mathbf{A}\mathbf{x}\)：

```matlab
% SolverPadeAx.m
tmp = C*x(1:n) + K*x(n+1:end);   % ΔtC·z₁ + Δt²K·z₂
y   = [-dM\tmp ; x(1:n)];         % y = Ax = {-M⁻¹(ΔtC·z₁+Δt²K·z₂); z₁}
```

---

### 2.4 矩阵指数精确解与外力多项式展开

公式 (7) 的精确解（常数变易法）为：

\[
\mathbf{z}(s) = e^{\mathbf{A}s}\mathbf{z}_{n-1} + e^{\mathbf{A}s}\int_0^s e^{-\mathbf{A}\tau}\mathbf{F}(\tau)\,\mathrm{d}\tau \tag{10}
\]

将外力在时步中点 \(s=0.5\) 展开为 \(p_t\) 阶多项式：

\[
\mathbf{F}_n(s) = \sum_{k=0}^{p_t} \mathbf{F}_{mn}^{(k)} (s-0.5)^k \tag{11}
\]

取 \(s=1\)（时步终点），精确解化为：

\[
\mathbf{z}_n = e^{\mathbf{A}}\mathbf{z}_{n-1} + \sum_{k=0}^{p_t} \mathbf{B}_k \mathbf{F}_{mn}^{(k)} \tag{12}
\]

**公式解读**：公式 (12) 是整个格式推导的起点。第一项是自由响应（历史状态的矩阵指数传播），第二项是强迫响应（Duhamel 积分的多项式近似）。外力展开系数 \(\mathbf{F}_{mn}^{(k)}\) 由 LGL 采样点和 Vandermonde 矩阵插值确定（对应 `ForceSeries.m`）。

**代码对应**（`ForceSeries.m`）：

```matlab
pf = order + 1;           % 多项式阶数 p_t+1
s  = forceSamplingPoints(np);    % LGL 采样点（以 0.5 为中心）
T  = transMtxPointsToPoly(s,pf); % Vandermonde 矩阵的逆（插值矩阵）
ft = f'*T;                % ft(is,:) 即各时步的 F_mn^(k)（k=0,...,p_t）
```

---

### 2.5 核心近似：有理函数代替矩阵指数

为使格式计算可行，用有理函数近似矩阵指数：

\[
e^{\mathbf{A}} \approx \mathbf{R} = \frac{\mathbf{P}}{\mathbf{Q}} \tag{15}
\]

其中 \(\mathbf{P} = \sum_{i=0}^{N_p} p_i\mathbf{A}^i\) 和 \(\mathbf{Q} = \sum_{i=0}^{N_q} q_i\mathbf{A}^i\) 为矩阵多项式，要求 \(N_q \ge N_p\) 保证稳定性（公式 (17)）。

将公式 (15) 代入公式 (12)，两端左乘 \(\mathbf{Q}\)，得**时步核心方程**：

\[
\mathbf{Q}\mathbf{z}_n = \mathbf{P}\mathbf{z}_{n-1} + \sum_{k=0}^{p_t} \mathbf{C}_k \mathbf{F}_{mn}^{(k)} \tag{18}
\]

其中外力贡献矩阵 \(\mathbf{C}_k = \mathbf{Q}\mathbf{B}_k\) 由递推关系确定：

\[
\mathbf{C}_0 = \mathbf{A}^{-1}(\mathbf{P} - \mathbf{Q}) \tag{20}
\]

\[
\mathbf{C}_k = \mathbf{A}^{-1}\!\left(k\mathbf{C}_{k-1} + (-0.5)^k(\mathbf{P} - (-1)^k\mathbf{Q})\right),\quad k=1,2,\ldots,p_t \tag{19}
\]

**公式解读**：公式 (18) 是纯多项式矩阵方程，不再含矩阵指数，计算可行。但仍需具体确定 \(\mathbf{P}\) 和 \(\mathbf{Q}\) 的系数，以及如何高效分解公式 (18) 为子步形式——这是第 3 节（有理函数设计）和第 4 节（子步算法）要回答的。

**代码对应**（`TimeIntgCoeffForce.m`）：

```matlab
M   = length(q) - 1;
tmp = p - q;              % 对应 P - Q（公式 20 的分子）
C(1,:) = tmp(2:end);      % C_0 = A^{-1}(P-Q)（去掉常数项=降阶操作）
for k = 1:M
    tmp = ((-1/2)^k)*(p-((-1)^k)*q);  % (-0.5)^k * (P - (-1)^k Q)
    tmp(1:M) = tmp(1:M) + k*C(k,:);   % + k*C_{k-1}
    C(k+1,:) = tmp(2:end);             % A^{-1} 作用（降阶）
end
```

---

### 2.6 关键设计：分母选单一重根

**本文最关键的设计选择**是将分母多项式限制为具有**单一重根 \(r\) 的 \(M\) 次多项式**：

\[
e^{\mathbf{A}} \approx \mathbf{R} = \frac{\mathbf{P}}{\mathbf{Q}} = \frac{p_0\mathbf{I} + p_1\mathbf{A} + \cdots + p_L\mathbf{A}^L}{(r\mathbf{I} - \mathbf{A})^M},\quad M \ge L \tag{34}
\]

对应的标量放大因子为：

\[
e^{\lambda} \approx R(\lambda) = \frac{P(\lambda)}{Q(\lambda)} = \frac{p_0 + p_1\lambda + \cdots + p_L\lambda^L}{(r - \lambda)^M} \tag{35}
\]

**M 格式**的构造：将 \((r-\lambda)^M e^\lambda\) 展开为 Taylor 级数，匹配到 \(M\) 阶，再用用户指定的 \(\rho_\infty\) 约束最高阶系数确定根 \(r\)：

\[
p_M(r) = \sum_{j=0}^{M} (-1)^j \frac{M!}{j!(M-j)!} \frac{r^{M-j}}{(M-j)!} = \pm\rho_\infty \tag{42}
\]

各系数由公式 (40) 给出：

\[
p_i(r) = \sum_{j=0}^{\min(i,M)} (-1)^j \frac{M!}{j!(M-j)!} \frac{r^{M-j}}{(i-j)!} \tag{40}
\]

**高频极限谱半径**：

\[
\rho_\infty = \begin{cases}
0, & N_q > N_p \\
\left|\dfrac{p_{N_p}}{q_{N_q}}\right|, & N_q = N_p \\
\infty, & N_q < N_p
\end{cases} \tag{33}
\]

**公式解读**：这里蕴含了两个核心设计决策：

① **分母单一重根**：\(\mathbf{Q} = (r\mathbf{I} - \mathbf{A})^M\) 意味着所有子步面对同一矩阵 \(r\mathbf{I} - \mathbf{A}\)，从而有效刚度矩阵在所有子步相同（见公式 (88)），只需一次 LU 分解；

② **\(\rho_\infty\) 约束**：通过控制最高阶系数 \(p_M = \pm\rho_\infty\)，用户可以在精确性（\(\rho_\infty = 1\)，无耗散）和最大耗散（\(\rho_\infty = 0\)，L-稳定）之间自由调节。

**代码对应**（`MschemeRoot.m`，求根 \(r\)）：

```matlab
% MschemeRoot.m：根据 M 和 rhoInfty 求根 r（对应公式 42）
RHS   = [1,1,-1,1,-1,-1]*rhoInfty;   % 选择 +ρ∞ 或 -ρ∞（见 Table 1）
j     = 0:M;
% 系数向量对应 p_M(r) 关于 r 的多项式（公式 40 在 i=M 的特例）
pMcoe = ((-1).^j).*factorial(M)./factorial(j)./(factorial(M-j).^2);
pMcoe(end) = pMcoe(end) - RHS(M);    % 令 p_M(r) - ±ρ∞ = 0
rs = sort(roots(pMcoe));              % 求方程的所有根
r  = rs(ir(M));                       % 按 Table 1 选取最优根
```

**代码对应**（`pCoefficients.m`，计算分子多项式系数）：

```matlab
% pCoefficients.m：计算 p_i(r)（对应公式 40）
for ii = 0:M
    j = 0:ii;
    p = ((-1).^j).*factorial(M)./factorial(M-j)./factorial(j)./factorial(ii-j);
    pcoe(ii+1) = p*(r.^(M-j))';   % p_i(r) = Σ_j (-1)^j C(M,j) r^{M-j}/(i-j)!
end
```

---

### 2.7 核心子步算法：将单一时步分解为 M 次递推

引入辅助矩阵 \(\mathbf{A}_r = r\mathbf{I} - \mathbf{A}\)，以及将多项式从 \(\mathbf{A}\) 变量基变换到 \(\mathbf{A}_r\) 基：

\[
\mathbf{A}_r^M\,\mathbf{z}_n = \mathbf{P}_r\,\mathbf{z}_{n-1} + \sum_{k=0}^{p_t}\mathbf{C}_k\mathbf{F}_{mn}^{(k)} \tag{82}
\]

引入辅助变量 \(\mathbf{z}^{(k)} = \mathbf{A}_r\mathbf{z}^{(k-1)}\)，方程 (82) 分解为 \(M\) 个递推子步：

\[
\begin{aligned}
\mathbf{A}_r\,\mathbf{z}^{(1)} &= \mathbf{z}^{(0)} \\
\mathbf{A}_r\,\mathbf{z}^{(2)} &= \mathbf{z}^{(1)} \\
&\vdots \\
\mathbf{A}_r\,\mathbf{z}_n &= \mathbf{z}^{(M-1)}
\end{aligned} \tag{85}
\]

其中初始右端项为：

\[
\mathbf{z}^{(0)} = \mathbf{P}_r\,\mathbf{z}_{n-1} + \sum_{k=0}^{p_t}\mathbf{C}_k\mathbf{F}_{mn}^{(k)} \tag{84}
\]

每个子步展开为块方程，对上半部分（无量纲速度 \(\mathbf{z}_1^{(k)}\)）求解**有效刚度矩阵方程**：

\[
\underbrace{\left(r^2\mathbf{M} + r\Delta t\mathbf{C} + \Delta t^2\mathbf{K}\right)}_{\mathbf{K}_{\mathrm{eff}}}\mathbf{z}_1^{(k)} = r\mathbf{M}\,\mathbf{z}_1^{(k-1)} - \Delta t^2\mathbf{K}\,\mathbf{z}_2^{(k-1)} \tag{88}
\]

对下半部分（位移 \(\mathbf{z}_2^{(k)}\)）用纯向量运算更新：

\[
\mathbf{z}_2^{(k)} = \frac{1}{r}\left(\mathbf{z}_1^{(k)} + \mathbf{z}_2^{(k-1)}\right) \tag{89}
\]

**加速度的计算**不需要矩阵-向量乘积，利用最后一个子步的结果：

\[
\dot{\mathbf{z}}_n = r\,\mathbf{z}_n - \mathbf{z}^{(M-1)} + \mathbf{F}_n(s=1) \tag{93}
\]

**公式解读**：

- 公式 (88) 中，**有效刚度矩阵 \(\mathbf{K}_{\mathrm{eff}} = r^2\mathbf{M} + r\Delta t\mathbf{C} + \Delta t^2\mathbf{K}\) 在所有 \(M\) 个子步中完全相同**，只需在时步开始前做一次 LU 分解（`dKd = decomposition(Kd)`），之后每个子步只需回代（\(O(n)\) 操作），极大节省计算量。
- 公式 (89) 是纯向量运算，无需求解方程组，计算代价可忽略。
- 公式 (93) 同样为纯向量运算，避免了矩阵-向量乘积 \(\mathbf{A}\mathbf{z}_n\)。

**代码对应**（`TimeSolverRExpn.m` 时步推进循环，第 40–80 行）：

```matlab
% --- 预计算阶段（仅一次）---
Kd  = sparse((r*r)*M + r*C + K);  % K_eff = r²M + rΔtC + Δt²K（公式 88 左侧）
dKd = decomposition(Kd);           % 仅做一次 LU 分解！

% --- 预计算 P_r（从 A 基变换到 A_r 基，公式 81a）---
prcoe = shiftPolycoe(pcoe, r);     % 变换系数，对应公式 81a

% --- 初始化状态辅助量（对应公式 84-85 的 z^(M-k) 初始化）---
z(:,1) = [v0; u0];
for ii = 1:p
    z(:,ii+1) = r*z(:,ii) - SolverPadeAx(dM,K,C,z(:,ii));  % A_r·z^(k)
end

% --- 每个时步的推进（主循环）---
for it = 2:ns
    % 步骤 1：计算右端项 z^(0)（公式 84）
    z(:,p+1) = z(:,1:p+1)*prcoe' + Pf*ft(is,:)';

    % 步骤 2：M 个子步的递推（公式 85，从 k=p 到 k=1，即从 z^(M-1) 到 z_n）
    for ip = p:-1:1
        % 公式 88：求解有效刚度方程
        z(1:ndof,ip) = dKd \ (r*(M*z(1:ndof,ip+1)) - K*z(ndof+1:2*ndof,ip+1));
        % 公式 89：位移的向量更新
        z(ndof+1:2*ndof,ip) = (z(1:ndof,ip) + z(ndof+1:2*ndof,ip+1))/r;
    end

    % 步骤 3：输出加速度（公式 93）
    acc(it,:) = r*z(pDOF,1) - z(pDOF,2) + fn*Fb(pDOF);
end

% 步骤 4：反变换（公式 4）
vel = vel/dt;    % u̇ = u°/Δt
acc = acc/(dt*dt); % ü = u°°/Δt²
```

---

### 2.8 格式的精度阶次与 Bathe 方法的等价关系

M 格式在 \(M=2\) 时，有效刚度矩阵除以 \(r^2\) 得：

\[
\left(\mathbf{M} + \frac{\Delta t}{r}\mathbf{C} + \left(\frac{\Delta t}{r}\right)^2\mathbf{K}\right)\mathbf{z}_1^{(k)} = \frac{1}{r}\mathbf{M}\,\mathbf{z}_1^{(k-1)} - \left(\frac{\Delta t}{r}\right)^2\mathbf{K}\,\mathbf{z}_2^{(k-1)} \tag{94}
\]

与 Bathe 方法的时步分割比 \(\gamma\) 对应关系为：

\[
\gamma = \frac{2}{r} \tag{95}
\]

代入 M=2 格式的根 \(r = 2 + \sqrt{2+2\rho_\infty}\)（公式 60），得到与 \(\rho_\infty\)-Bathe 方法完全相同的分割比：

\[
\gamma = \frac{2}{2 + \sqrt{2+2\rho_\infty}} = \frac{2 - \sqrt{2+2\rho_\infty}}{1 - \rho_\infty} \tag{96}
\]

**公式解读**：这证明了本文 M=2 格式是 \(\rho_\infty\)-Bathe 方法的自然推广起点。对 \(M \ge 3\) 的高阶格式，本文方法提供了 Bathe 方法框架下无法达到的阶次，同时保留其"单一有效刚度矩阵"的计算优势。

---

### 2.9 谱分析指标

通过单模态分析（特征值 \(\lambda = i\omega\Delta t = i2\pi\Delta t/T\)）评估格式性能：

\[
R(\lambda) = \frac{P(\lambda)}{Q(\lambda)} \tag{26}
\]

- **谱半径**（数值耗散）：\(\rho = |R|\)；精确值为 \(|e^\lambda| = 1\)（无耗散）。
- **相位角**（数值频散）：\(\bar{\Omega} = \arg(R)\)；精确值为 \(\omega\Delta t\)。
- **相对周期误差**：

\[
\frac{\bar{T} - T}{T} = \frac{\omega\Delta t}{\bar{\Omega}} - 1 \tag{32}
\]

- **高频极限谱半径**（可控耗散量）：\(\rho_\infty = |p_M / q_M|\)（公式 33）。

**公式解读**：谱半径曲线 \(\rho(\Delta t/T)\) 应在低频端接近 1（保精度），在高频端趋向 \(\rho_\infty\)（控耗散）。相对周期误差曲线 \((\bar{T}-T)/T\) 随 \(\Delta t/T\) 增大而增大，高阶格式的误差斜率更小（即相同 \(\Delta t\) 下误差更小），允许使用更大时步。

---

## 第三部分：创新点

### 创新点一：单一重根有理近似——"同一有效刚度矩阵"

**现有问题**：已有的基于 Padé 展开的高阶时间积分方法（如 [46,47]）对矩阵指数的分母多项式有多个不同的根，导致每次递推步的有效刚度矩阵不同，部分甚至为**复数矩阵**，内存占用成倍增加，大规模计算时无法共享 LU 分解。

**本文创新**：通过将分母多项式特别设计为具有**单一重根 \(r\) 的 \(M\) 次多项式**（公式 34）：

\[
\mathbf{Q}(\mathbf{A}) = (r\mathbf{I} - \mathbf{A})^M \tag{34（分母部分）}
\]

所有 \(M\) 个子步的有效刚度矩阵均为（公式 88）：

\[
\mathbf{K}_{\mathrm{eff}} = r^2\mathbf{M} + r\Delta t\mathbf{C} + \Delta t^2\mathbf{K}
\]

该矩阵只含 **\(\mathbf{M}\)、\(\mathbf{C}\)、\(\mathbf{K}\)** 三个原始稀疏矩阵的实数线性组合，始终为**实数稀疏矩阵**，且在所有子步完全相同——只需一次 LU 分解（代码中的 `dKd = decomposition(Kd)`）。与 Padé 展开需要 \(M\) 次不同（甚至复数）LU 分解相比，计算代价大幅降低，内存占用减少约 \(M\) 倍。

**具体体现**：代码 `TimeSolverRExpn.m` 中，有效刚度矩阵仅做一次分解（第 41 行），时步循环的内层（第 69–72 行）只有回代（\(\backslash\) 运算），每个子步的计算量等同于梯形法的单步。

---

### 创新点二：两族格式的统一构造框架（M 格式与 (M+1) 格式）

**M 格式**（用户可控耗散）：

- M 个子步，M 阶精度。
- 通过公式 (42) 以用户指定的 \(\rho_\infty\) 确定根 \(r\)，从 \(\rho_\infty = 0\)（最大耗散，L-稳定）到 \(\rho_\infty = 1\)（无耗散）连续可调。
- 本文证明 M=2 格式与 \(\rho_\infty\)-Bathe 方法数值等价（公式 (95)–(96)），将著名的 Bathe 方法纳入统一框架。
- M 可取 1 到 6（M > 6 时无法保证稳定性，见 Table 1）。

**\((M+1)\) 格式**（内置耗散，更高精度）：

- M 个子步，\((M+1)\) 阶精度（多一阶）。
- 根 \(r\) 由 \(p_{M+1}(r) = 0\)（公式 67）唯一确定，\(\rho_\infty\) 内置、不可调。
- 适用子步数：\(M = 2, 3, 5\)（其余无法保证 \(\rho \le 1\)）。
- 本文证明 \((M+1)\) 格式在 M=2 时分割比 \(\gamma = 1+\sqrt{3}/3\) 与三阶 Bathe 方法 [33] 的推荐值完全一致（公式 (74)–(77)），再次验证统一框架的完备性。

**创新意义**：同一套代数框架（公式 (34)–(44) 和 (66)–(69)）下，仅通过改变根 \(r\) 的确定方式，即可在"精度阶次"与"耗散可控性"之间灵活切换，给工程用户提供了清晰的选择指南。

---

### 创新点三：精度阶次不受外力与物理阻尼影响

**现有问题**：部分传统时间积分方法（如某些 Runge-Kutta 格式）在有非零外力时精度阶次会降低（称为"阶次折减"现象）；另一些方法要求外力时程为特定函数形式。

**本文创新**：通过将外力展开阶次设定为与分子多项式次数相同（\(p_t = N_p\)，公式 (11) 和 (16a)），并在递推公式 (19)–(20) 中显式包含外力贡献，使得：

1. 对**任意时程**的外力，格式精度阶次均能达到理论值（M 阶或 M+1 阶）。
2. 存在**物理阻尼**时，精度阶次同样不降低（矩阵 \(\mathbf{A}\) 的阻尼项被完整处理）。

**具体体现**：数值算例（Section 5.1，单自由度系统）在不同 \(\rho_\infty\) 和外力形式下均表现出最优收敛率（误差随 \(\Delta t^M\) 收敛），验证了这一性质。代码中，外力插值（`ForceSeries.m`）和外力贡献矩阵（`TimeIntgCoeffForce.m`）的设计保证了力处理精度与格式精度的一致性。

---

### 创新点四：高阶格式在波传播问题中的实际效率优势

**现有问题**：二阶 \(\rho_\infty\)-Bathe 方法（M=2）虽然稳定，但对波传播问题所需的 CFL 数较小（约 1.2–2），大时步下高频振荡明显。

**本文创新**：三阶（M=3）及更高阶格式允许使用显著更大的 CFL 数：

| 子步数 \(M\) | 推荐 CFL 数 | 相对 M=2 的计算加速比（估算） |
|:---:|:---:|:---:|
| 2 | ~1.2 | 基准 |
| 3 | ~5   | ~2.7× |
| 4 | ~8   | ~3.3× |
| 5 | ~12  | ~4×   |
| 6 | ~16  | ~4.4× |

这意味着**实际计算时间随阶次提高而减少**，尽管每步子步数增多（计算代价线性增加），但更大 CFL 数带来的时步增大（超线性节约）使总效率显著提升。大规模实算（Lamb 波问题，~1570 万自由度）证实三子步方案比二子步快约 1.64 倍，四子步快约 2 倍。

**公式支撑**：加速比估算公式为：

\[
\text{加速比} \approx \frac{\mathrm{CFL}_M}{\mathrm{CFL}_2} \cdot \frac{2}{M}
\]

其中分子为 CFL 提升（更大时步）、分母为子步数增加（更多计算），两者之积反映实际效率收益。

---

### 创新点五：递推辅助变量避免矩阵-向量乘积

公式 (91) 表明，推进到下一时步 \(n+1\) 时，\(\mathbf{P}_r\mathbf{z}_n\) 的计算不需要矩阵-向量乘积，仅需利用当前时步各子步中已经计算并存储的辅助变量 \(\mathbf{z}^{(M-1)}, \ldots, \mathbf{z}^{(0)}\)（即代码中的 `z(:,2:p+1)`）：

\[
\mathbf{P}_r\,\mathbf{z}_n = p_{r0}\mathbf{z}_n + p_{r1}\mathbf{z}^{(M-1)} + \cdots + p_{rM}\mathbf{z}^{(0)} \tag{91}
\]

**代码对应**（`TimeSolverRExpn.m` 第 68 行）：

```matlab
z(:,p+1) = z(:,1:p+1)*prcoe';  % P_r·z_n = 加权求和（无矩阵-向量乘积！）
```

这一设计使得"右端项预处理"的计算量仅为 \(O((M+1) \cdot n)\) 次向量加法，相比原本需要计算 \(\mathbf{A}\) 的各次方再乘 \(\mathbf{z}\) 的代价（\(O(M \cdot n^2)\)），对大规模问题效率大幅提升。

---

*本报告涵盖了文献的摘要概括、核心问题与公式推导、代码对应关系，以及五项创新点的详细阐述。*
