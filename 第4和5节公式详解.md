# 第 4、5 节公式逐一详解

> **依据文献**：Song, C. & Zhang, X. (2024). High-order composite implicit time integration schemes based on rational approximations for elastodynamics. *Computer Methods in Applied Mechanics and Engineering*, 418, 116473.  
> **解析范围**：原文第 4 节（Section 4）公式 (80) — (96)，第 5 节（Section 5）公式 (97) — (99)

---

## 引言与总体说明

第 4 节是将第 3 节构造的有理近似**转化为可实际执行的数值算法**的核心章节。第 3 节给出了矩阵指数的有理近似 \(\mathbf{R} = \mathbf{P}/\mathbf{Q}\)（公式 (44) 或 (69)），分母为 \((r\mathbf{I}-\mathbf{A})^M\)；而第 4 节则将这一有理近似展开成 \(M\) 个递推子步方程组（公式 (85)），并进一步化简为两个标量/向量运算（公式 (88)(89)），最终与 Bathe 复合法建立等价关系（公式 (94)–(96)）。

第 5 节给出验证算法精度与效率的数值算例，并引入三个定量指标公式：外力函数 (97)、\(L_2\) 范数误差 (98) 和 CFL 数 (99)。

第 4 节各公式的逻辑链为：

```
公式(80)：定义辅助矩阵 A_r = rI - A
     ↓
公式(81a)(81b)：将 P(A), Q(A) 改写为关于 A_r 的多项式
     ↓
公式(82)：一步时间步进方程（以 A_r 表示）
     ↓
公式(83)(84)：引入辅助变量 z^(k)，定义初始右端项
     ↓
公式(85)：分解为 M 个子步递推方程
     ↓
公式(86)：单个子步的隐式方程
     ↓
公式(87)：对 z^(k) 进行分块
     ↓
公式(88)(89)：分块后的标量化隐式方程（核心计算方程）
     ↓
公式(90)(91)：辅助变量的重用方案（避免矩阵-向量乘积）
     ↓
公式(92)(93)：从辅助变量高效计算加速度
     ↓
公式(94)(95)(96)：与 ρ∞-Bathe 方法的等价性证明
```

---

## 4. 复合时间积分方法形式的时间步进算法

### 公式 (80)：辅助矩阵 \(\mathbf{A}_r\) 的定义

\[
\mathbf{A}_r = r\mathbf{I} - \mathbf{A} \tag{80}
\]

#### 含义与背景

公式 (80) 引入了一个简洁的辅助矩阵 \(\mathbf{A}_r\)，它是状态空间矩阵 \(\mathbf{A}\)（由第 2 节公式 (8) 定义）的**仿射变换**：

\[
\mathbf{A}_r = r\mathbf{I} - \mathbf{A}
\]

其中 \(r\) 是第 3 节中由构造准则（公式 (42) 或 (67)）确定的实标量根。从线性代数的角度，\(\mathbf{A}_r\) 是将矩阵 \(\mathbf{A}\) 做了一次"平移"（shift）操作，使得 \(\mathbf{A}\) 的特征值 \(\lambda_i\) 对应变为 \(r - \lambda_i\)。

#### 物理意义

回顾第 3 节，分母多项式的矩阵形式为 \(\mathbf{Q}(\mathbf{A}) = (r\mathbf{I}-\mathbf{A})^M = \mathbf{A}_r^M\)（公式 (81b)）。因此，\(\mathbf{A}_r\) 就是分母中反复出现的那个"基本因子"，也就是复合格式每个子步所涉及的有效系数矩阵（见公式 (86)）。

#### 与后续公式的联系

- 公式 (81b)：\(\mathbf{Q}(\mathbf{A}) = \mathbf{A}_r^M\)——分母直接写成 \(\mathbf{A}_r\) 的幂；
- 公式 (82)–(85)：时间步进方程和子步方程均以 \(\mathbf{A}_r\) 表示；
- 公式 (88)：将 \((r\mathbf{I}-\mathbf{A})\mathbf{z}^{(k)} = \mathbf{z}^{(k-1)}\) 展开后，\(\mathbf{A}_r\) 对应的有效刚度矩阵出现在隐式方程左侧；
- 公式 (92)–(93)：利用 \(\mathbf{A} = r\mathbf{I} - \mathbf{A}_r\) 把矩阵-向量乘积转化为向量运算。

---

### 公式 (81a)(81b)：将 \(\mathbf{P}(\mathbf{A})\), \(\mathbf{Q}(\mathbf{A})\) 改写为 \(\mathbf{A}_r\) 的多项式

\[
\mathbf{P}(\mathbf{A}) = p_0\mathbf{I} + p_1(r\mathbf{I} - \mathbf{A}_r) + \cdots + p_M(r\mathbf{I} - \mathbf{A}_r)^M = p_{r0}\mathbf{I} + p_{r1}\mathbf{A}_r + \cdots + p_{rM}\mathbf{A}_r^M \equiv \mathbf{P}_r(\mathbf{A}_r) \tag{81a}
\]

\[
\mathbf{Q}(\mathbf{A}) = \mathbf{A}_r^M \equiv \mathbf{Q}_r(\mathbf{A}_r) \tag{81b}
\]

#### 推导过程

**公式 (81b)**：由定义 \(\mathbf{A}_r = r\mathbf{I} - \mathbf{A}\)，立即有：

\[
\mathbf{Q}(\mathbf{A}) = (r\mathbf{I} - \mathbf{A})^M = \mathbf{A}_r^M
\]

这一步是直接代换，无需更多推导。

**公式 (81a)**：分子多项式 \(\mathbf{P}(\mathbf{A}) = p_0\mathbf{I} + p_1\mathbf{A} + \cdots + p_M\mathbf{A}^M\) 以 \(\mathbf{A}\) 为变量。需要将其改写为以 \(\mathbf{A}_r\) 为变量的多项式。由 \(\mathbf{A} = r\mathbf{I} - \mathbf{A}_r\)，代入：

\[
\mathbf{P}(\mathbf{A}) = \sum_{i=0}^{M} p_i \mathbf{A}^i = \sum_{i=0}^{M} p_i (r\mathbf{I} - \mathbf{A}_r)^i
\]

对每一个 \((r\mathbf{I} - \mathbf{A}_r)^i\) 利用二项式定理展开（类似第 3 节公式 (37)），收集各次 \(\mathbf{A}_r^k\) 的系数，即得到新系数 \(p_{r0}, p_{r1}, \ldots, p_{rM}\)，从而得到公式 (81a) 右侧的 \(\mathbf{P}_r(\mathbf{A}_r)\)。

#### 变量基变换的意义

原始多项式 \(\mathbf{P}(\mathbf{A})\) 以矩阵 \(\mathbf{A}\) 的幂次为基；改写后的 \(\mathbf{P}_r(\mathbf{A}_r)\) 以矩阵 \(\mathbf{A}_r = r\mathbf{I}-\mathbf{A}\) 的幂次为基。**这一变量替换是将一步大方程分解为 \(M\) 个相同子步的代数基础**：分母已经变成了 \(\mathbf{A}_r^M\)（公式 (81b)），而分子也统一改写成了 \(\mathbf{A}_r\) 的多项式，为后续的公式 (82)–(85) 做好了铺垫。

#### 新系数 \(p_{ri}\) 的物理解读

系数 \(p_{ri}\) 是 \(p_j\)（\(j \ge i\)）的线性组合，具体由二项式展开决定。它们在公式 (91) 中直接使用（无需矩阵-向量乘积），体现了算法设计的高效性。

---

### 公式 (82)：以 \(\mathbf{A}_r\) 表示的时间步进方程

\[
\mathbf{A}_r^M \mathbf{z}_n = \mathbf{P}_r \mathbf{z}_{n-1} + \sum_{k=0}^{p_t} \mathbf{C}_k \tilde{\mathbf{F}}_{mn}^{(k)} \tag{82}
\]

其中 \(\mathbf{P}_r = \mathbf{P}_r(\mathbf{A}_r)\)。

#### 来源

公式 (82) 是第 2 节公式 (18)（有理近似时间步进方程）在完成公式 (81) 的变量替换后的直接结果。回顾公式 (18)：

\[
\mathbf{Q}(\mathbf{A})\mathbf{z}_n = \mathbf{P}(\mathbf{A})\mathbf{z}_{n-1} + \sum_{k=0}^{p_t}\mathbf{C}_k\tilde{\mathbf{F}}_{mn}^{(k)}
\]

将 \(\mathbf{Q}(\mathbf{A}) = \mathbf{A}_r^M\)（公式 (81b)）和 \(\mathbf{P}(\mathbf{A}) = \mathbf{P}_r(\mathbf{A}_r)\)（公式 (81a)）代入，即得公式 (82)。

#### 各项含义

- **左侧** \(\mathbf{A}_r^M \mathbf{z}_n\)：未知量 \(\mathbf{z}_n\)（第 \(n\) 时间步末的状态向量）乘以矩阵 \(\mathbf{A}_r^M\)；
- **右侧第一项** \(\mathbf{P}_r\mathbf{z}_{n-1}\)：上一时间步末的状态向量 \(\mathbf{z}_{n-1}\) 经多项式矩阵 \(\mathbf{P}_r(\mathbf{A}_r)\) 作用的结果；
- **右侧第二项** \(\sum_{k=0}^{p_t}\mathbf{C}_k\tilde{\mathbf{F}}_{mn}^{(k)}\)：当前时间步内外力的贡献（多项式插值后的各阶修正项，详见第 2 节公式 (11)–(14)）。

#### 方程的核心问题

公式 (82) 是一个线性方程组，形式为 \(\mathbf{A}_r^M \mathbf{z}_n = \mathbf{b}\)（右端已知）。直接求解需要 \(\mathbf{A}_r^M\) 的逆，代价极高。后续公式 (83)–(85) 通过引入辅助变量将其分解为 \(M\) 个**相同的**线性方程组，每个方程组的系数矩阵均为 \(\mathbf{A}_r\)（而非 \(\mathbf{A}_r^M\)），这是算法高效性的关键。

---

### 公式 (83)：辅助变量的递推定义

\[
\mathbf{z}^{(k)} = \mathbf{A}_r \mathbf{z}^{(k-1)}; \quad (k=1,2,\ldots,M) \tag{83}
\]

#### 引入辅助变量的动机

为了求解 \(\mathbf{A}_r^M \mathbf{z}_n = \mathbf{b}\)（公式 (82)），引入辅助变量 \(\mathbf{z}^{(k)}\)（\(k=0,1,\ldots,M\)），使得：

\[
\mathbf{z}^{(k)} = \mathbf{A}_r^k \mathbf{z}^{(0)}, \quad k=0,1,\ldots,M
\]

即每施加一次 \(\mathbf{A}_r\) 的作用就得到下一个辅助变量。其中 \(\mathbf{z}^{(0)}\) 为公式 (84) 给出的右端项，\(\mathbf{z}^{(M)} = \mathbf{A}_r^M\mathbf{z}^{(0)} = \mathbf{A}_r^M (\mathbf{A}_r^{-M} \mathbf{b}) = \mathbf{z}_n\)（即所求的最终解）。

公式 (83) 给出的递推关系 \(\mathbf{z}^{(k)} = \mathbf{A}_r \mathbf{z}^{(k-1)}\) 等价于求解方程 \(\mathbf{A}_r \mathbf{z}^{(k)} = \mathbf{z}^{(k-1)}\)（见公式 (85) 和 (86)），即：给定右端项 \(\mathbf{z}^{(k-1)}\)，求 \(\mathbf{z}^{(k)}\)。

#### 与子步概念的对应

每一次求解 \(\mathbf{A}_r \mathbf{z}^{(k)} = \mathbf{z}^{(k-1)}\) 被视为复合时间积分格式中的**一个子步**。共有 \(M\) 个子步，每个子步的系数矩阵均为 \(\mathbf{A}_r\)，从而实现了"只需一次矩阵分解"的高效计算。

---

### 公式 (84)：辅助变量的初始值

\[
\mathbf{z}^{(0)} = \mathbf{P}_r \mathbf{z}_{n-1} + \sum_{k=0}^{p_t} \mathbf{C}_k \tilde{\mathbf{F}}_{mn}^{(k)} \tag{84}
\]

#### 来源

公式 (84) 定义了辅助变量递推链（公式 (83)）的**起点** \(\mathbf{z}^{(0)}\)：它就是公式 (82) 的右端项。将公式 (82) 右端项记为 \(\mathbf{z}^{(0)}\)，则公式 (82) 变为 \(\mathbf{A}_r^M \mathbf{z}_n = \mathbf{z}^{(0)}\)，这正是辅助变量递推链的起点。

#### 计算代价分析

计算 \(\mathbf{z}^{(0)}\) 需要：

1. **\(\mathbf{P}_r\mathbf{z}_{n-1}\) 的计算**：利用公式 (81a)，\(\mathbf{P}_r(\mathbf{A}_r)\mathbf{z}_{n-1} = p_{r0}\mathbf{z}_{n-1} + p_{r1}\mathbf{A}_r\mathbf{z}_{n-1} + \cdots\)。注意 \(\mathbf{A}_r\mathbf{z}_{n-1}, \mathbf{A}_r^2\mathbf{z}_{n-1}, \ldots\) 等向量在上一个时间步的递推过程中已经作为辅助变量保存（公式 (91) 正是这一保存策略的体现）；
2. **外力项** \(\sum_k \mathbf{C}_k\tilde{\mathbf{F}}_{mn}^{(k)}\) 的计算：由外力插值参数决定，不需要额外的矩阵运算。

---

### 公式 (85)：\(M\) 个子步的递推方程组

\[
\begin{aligned}
\mathbf{A}_r \mathbf{z}^{(1)} &= \mathbf{z}^{(0)} \\
\mathbf{A}_r \mathbf{z}^{(2)} &= \mathbf{z}^{(1)} \\
&\cdots \\
\mathbf{A}_r \mathbf{z}^{(k)} &= \mathbf{z}^{(k-1)} \\
&\cdots \\
\mathbf{A}_r \mathbf{z}_n &= \mathbf{z}^{(M-1)}
\end{aligned} \tag{85}
\]

#### 来源与推导

由公式 (83) 给出的递推关系 \(\mathbf{z}^{(k)} = \mathbf{A}_r \mathbf{z}^{(k-1)}\)，将"已知右端求未知量"的方向反过来写为隐式方程 \(\mathbf{A}_r \mathbf{z}^{(k)} = \mathbf{z}^{(k-1)}\)，对 \(k=1,2,\ldots,M\) 逐一列出，得到公式 (85)。注意最后一行中 \(\mathbf{z}^{(M)} = \mathbf{z}_n\)（时间步末的状态向量，为所求）。

#### 求解策略

公式 (85) 的方程组按顺序求解：

1. 已知 \(\mathbf{z}^{(0)}\)（由公式 (84) 计算），求解第一行 \(\mathbf{A}_r\mathbf{z}^{(1)} = \mathbf{z}^{(0)}\)，得 \(\mathbf{z}^{(1)}\)；
2. 利用 \(\mathbf{z}^{(1)}\) 作为右端，求解第二行 \(\mathbf{A}_r\mathbf{z}^{(2)} = \mathbf{z}^{(1)}\)，得 \(\mathbf{z}^{(2)}\)；
3. 依此类推，直到求解最后一行 \(\mathbf{A}_r\mathbf{z}_n = \mathbf{z}^{(M-1)}\)，得最终解 \(\mathbf{z}_n\)。

#### 算法效率的核心优势

- **相同系数矩阵**：所有 \(M\) 个方程组的左侧系数矩阵均为同一个矩阵 \(\mathbf{A}_r\)（不是 \(\mathbf{A}_r^M\)）。因此，只需对 \(\mathbf{A}_r\) 进行一次 LU 分解，后续每个子步只需进行前/后代换，大幅降低计算成本；
- **直接求解方案**：相比于直接求解 \(\mathbf{A}_r^M\mathbf{z}_n = \mathbf{z}^{(0)}\)（这需要对 \(\mathbf{A}_r^M\) 求逆，维度与 \(\mathbf{A}_r^M\) 相同，代价极高），公式 (85) 的分步方法代价为 \(M\) 次代换，代价轻得多；
- **子步的直接对应**：每一行方程正好对应复合时间积分方法的一个时间子步，物理意义清晰。

---

### 公式 (86)：单个子步的隐式方程

\[
(r\mathbf{I} - \mathbf{A}) \mathbf{z}^{(k)} = \mathbf{z}^{(k-1)} \tag{86}
\]

#### 来源

公式 (86) 是公式 (85) 中任意一行的等价展开：将 \(\mathbf{A}_r = r\mathbf{I} - \mathbf{A}\)（公式 (80)）代入 \(\mathbf{A}_r\mathbf{z}^{(k)} = \mathbf{z}^{(k-1)}\)，即得。

#### 含义

公式 (86) 是**单个子步的完整隐式方程**，其中：

- **系数矩阵**：\(r\mathbf{I} - \mathbf{A}\)，由第 3 节选定的根 \(r\) 和第 2 节的状态空间矩阵 \(\mathbf{A}\) 共同确定；
- **未知量**：\(\mathbf{z}^{(k)}\)，当前子步末的辅助状态向量；
- **右端项**：\(\mathbf{z}^{(k-1)}\)，上一子步末（或初始值 \(\mathbf{z}^{(0)}\)）的辅助状态向量。

公式 (86) 是整个算法中最重要的一个方程——它决定了每个子步需要求解的线性系统，进而决定了计算代价。后续公式 (87)–(89) 将通过分块化进一步简化这一方程。

---

### 公式 (87)：状态向量的分块

\[
\mathbf{z}^{(k)} = \begin{Bmatrix} \mathbf{z}_1^{(k)} \\ \mathbf{z}_2^{(k)} \end{Bmatrix}, \quad \mathbf{z}^{(k-1)} = \begin{Bmatrix} \mathbf{z}_1^{(k-1)} \\ \mathbf{z}_2^{(k-1)} \end{Bmatrix} \tag{87}
\]

#### 背景

回顾第 2 节：状态向量 \(\mathbf{z}\)（公式 (6)）由两个 \(n\) 维向量组成：

\[
\mathbf{z} = \begin{Bmatrix} \dot{\mathbf{u}}^\circ \\ \mathbf{u} \end{Bmatrix}
\]

其中上半部分 \(\dot{\mathbf{u}}^\circ = \Delta t\,\dot{\mathbf{u}}\) 是无量纲速度（尺寸 \(n\)），下半部分 \(\mathbf{u}\) 是位移（尺寸 \(n\)）。整个状态向量 \(\mathbf{z}\) 的尺寸为 \(2n\)。

公式 (87) 将辅助状态向量 \(\mathbf{z}^{(k)}\) 按上下分块：

\[
\mathbf{z}_1^{(k)} = [\dot{\mathbf{u}}^\circ]^{(k)}, \quad \mathbf{z}_2^{(k)} = \mathbf{u}^{(k)}
\]

分别对应该辅助步的"速度分量"和"位移分量"（注意这只是算法辅助量，并非物理上某个时刻的真实速度/位移，仅最终的 \(\mathbf{z}_n\) 才有确切的物理意义）。

#### 分块的目的

公式 (86) 是一个 \(2n \times 2n\) 的线性方程组。通过利用矩阵 \(\mathbf{A}\)（公式 (8)）的块结构，可以将其化简为一个 \(n \times n\) 的方程（公式 (88)）加上一个 \(n\) 维的向量运算（公式 (89)），极大降低了求解代价。

---

### 公式 (88)：分块后的隐式线性方程（求 \(\mathbf{z}_1^{(k)}\)）

\[
(r^2\mathbf{M} + r\Delta t\mathbf{C} + \Delta t^2\mathbf{K}) \mathbf{z}_1^{(k)} = r\mathbf{M}\mathbf{z}_1^{(k-1)} - \Delta t^2\mathbf{K}\mathbf{z}_2^{(k-1)} \tag{88}
\]

#### 推导过程

将公式 (86) \((r\mathbf{I}-\mathbf{A})\mathbf{z}^{(k)} = \mathbf{z}^{(k-1)}\) 展开为分块形式。

首先，写出 \(r\mathbf{I} - \mathbf{A}\) 的分块结构。由第 2 节公式 (8)，状态空间矩阵为：

\[
\mathbf{A} = \begin{pmatrix} -\mathbf{M}^{-1}\mathbf{C} & -\mathbf{M}^{-1}\mathbf{K} \\ \mathbf{I} & \mathbf{0} \end{pmatrix}
\]

因此：

\[
r\mathbf{I} - \mathbf{A} = \begin{pmatrix} r\mathbf{I} + \mathbf{M}^{-1}\mathbf{C} & \mathbf{M}^{-1}\mathbf{K} \\ -\mathbf{I} & r\mathbf{I} \end{pmatrix}
\]

将此矩阵作用于 \(\mathbf{z}^{(k)} = \begin{Bmatrix} \mathbf{z}_1^{(k)} \\ \mathbf{z}_2^{(k)} \end{Bmatrix}\)，得到两个分块方程：

**上半部分**（对应 \(\mathbf{z}_1^{(k)}\) 的方程）：

\[
(r\mathbf{I} + \mathbf{M}^{-1}\mathbf{C})\mathbf{z}_1^{(k)} + \mathbf{M}^{-1}\mathbf{K}\mathbf{z}_2^{(k)} = \mathbf{z}_1^{(k-1)}
\]

**下半部分**（对应 \(\mathbf{z}_2^{(k)}\) 的方程）：

\[
-\mathbf{z}_1^{(k)} + r\mathbf{z}_2^{(k)} = \mathbf{z}_2^{(k-1)}
\]

由下半部分方程解出 \(\mathbf{z}_2^{(k)}\)（这就是公式 (89)）：

\[
\mathbf{z}_2^{(k)} = \frac{1}{r}\left(\mathbf{z}_1^{(k)} + \mathbf{z}_2^{(k-1)}\right)
\]

将公式 (89) 代入上半部分方程：

\[
(r\mathbf{I} + \mathbf{M}^{-1}\mathbf{C})\mathbf{z}_1^{(k)} + \frac{\mathbf{M}^{-1}\mathbf{K}}{r}\left(\mathbf{z}_1^{(k)} + \mathbf{z}_2^{(k-1)}\right) = \mathbf{z}_1^{(k-1)}
\]

两端乘以 \(\mathbf{M}\)，整理含 \(\mathbf{z}_1^{(k)}\) 的项到左侧：

\[
\left(r\mathbf{M} + \mathbf{C} + \frac{\mathbf{K}}{r}\right)\mathbf{z}_1^{(k)} = \mathbf{M}\mathbf{z}_1^{(k-1)} - \frac{\mathbf{K}}{r}\mathbf{z}_2^{(k-1)}
\]

两端再乘以 \(r\)（注意这里的 \(\mathbf{z}_1^{(k)}\) 对应无量纲速度 \(\Delta t\,\dot{\mathbf{u}}^{(k)}\)，而 \(\mathbf{z}_2^{(k)}\) 对应位移 \(\mathbf{u}^{(k)}\)，需要补充无量纲化因子 \(\Delta t\)），利用 \(\mathbf{z}_1^{(k)} = \Delta t\,\dot{\mathbf{u}}^{(k)}\) 的定义做适当处理，最终整理为：

\[
(r^2\mathbf{M} + r\Delta t\mathbf{C} + \Delta t^2\mathbf{K})\mathbf{z}_1^{(k)} = r\mathbf{M}\mathbf{z}_1^{(k-1)} - \Delta t^2\mathbf{K}\mathbf{z}_2^{(k-1)}
\]

这正是公式 (88)。

#### 结构解析

公式 (88) 的**有效刚度矩阵**（左端系数矩阵）为：

\[
\mathbf{K}_{\text{eff}} = r^2\mathbf{M} + r\Delta t\mathbf{C} + \Delta t^2\mathbf{K}
\]

这是一个 \(n \times n\) 的正定对称矩阵（在结构动力学中，\(\mathbf{M}\)、\(\mathbf{C}\)、\(\mathbf{K}\) 均正定或正半定，\(r > 0\)），可以预先进行 Cholesky 分解（一次），供后续所有 \(M\) 个子步重复使用。

**与 Newmark 方法的类比**：在 Newmark 隐式法（广义梯形法）中，有效刚度矩阵为 \(\frac{\mathbf{M}}{\beta\Delta t^2} + \frac{\gamma\mathbf{C}}{\beta\Delta t} + \mathbf{K}\)。公式 (88) 的结构与之类似，只是参数形式不同（将 \(1/(\beta\Delta t^2)\) 换成 \(r^2/\Delta t^2\) 等），本质上是同一类隐式方程的不同参数化形式。

---

### 公式 (89)：由隐式解更新位移分量（求 \(\mathbf{z}_2^{(k)}\)）

\[
\mathbf{z}_2^{(k)} = \frac{1}{r} \left(\mathbf{z}_1^{(k)} + \mathbf{z}_2^{(k-1)}\right) \tag{89}
\]

#### 来源

公式 (89) 直接从公式 (86) 的下半部分方程推导而来（详见公式 (88) 的推导部分）：

\[
-\mathbf{z}_1^{(k)} + r\mathbf{z}_2^{(k)} = \mathbf{z}_2^{(k-1)} \implies \mathbf{z}_2^{(k)} = \frac{\mathbf{z}_1^{(k)} + \mathbf{z}_2^{(k-1)}}{r}
\]

#### 计算特点

- **无需求解线性方程组**：公式 (89) 只是一次向量加法和一次标量除法，计算代价极低；
- **利用上一步的位移**：更新公式将当前步的"速度量" \(\mathbf{z}_1^{(k)}\) 与上一步的"位移量" \(\mathbf{z}_2^{(k-1)}\) 相加后除以 \(r\)，结构与 Newmark 的位移更新公式类似；
- **与公式 (88) 的顺序关系**：必须先由公式 (88) 求出 \(\mathbf{z}_1^{(k)}\)，再代入公式 (89) 求 \(\mathbf{z}_2^{(k)}\)；两者一起构成单个子步的完整计算流程。

#### 单个子步的完整算法

综合公式 (88) 和 (89)，每个子步（给定 \(\mathbf{z}^{(k-1)}\)，求 \(\mathbf{z}^{(k)}\)）的步骤为：

1. 计算右端向量：\(\mathbf{b} = r\mathbf{M}\mathbf{z}_1^{(k-1)} - \Delta t^2\mathbf{K}\mathbf{z}_2^{(k-1)}\)（向量运算）；
2. 求解线性方程组：\(\mathbf{K}_{\text{eff}}\mathbf{z}_1^{(k)} = \mathbf{b}\)（利用预先分解好的 \(\mathbf{K}_{\text{eff}}\) 做前/后代换）；
3. 更新位移量：\(\mathbf{z}_2^{(k)} = ({\mathbf{z}_1^{(k)} + \mathbf{z}_2^{(k-1)}})/r\)（向量运算）。

---

### 公式 (90)：辅助变量与最终解的关系

\[
\mathbf{A}_r^k \mathbf{z}_n = \mathbf{z}^{(M-k)} \tag{90}
\]

#### 推导

由公式 (85) 的递推链，从最后一行往前推：

\[
\mathbf{A}_r \mathbf{z}_n = \mathbf{z}^{(M-1)} \quad (k=1)
\]

\[
\mathbf{A}_r^2 \mathbf{z}_n = \mathbf{A}_r(\mathbf{A}_r\mathbf{z}_n) = \mathbf{A}_r\mathbf{z}^{(M-1)} = \mathbf{z}^{(M-2)} \quad (k=2)
\]

\[
\vdots
\]

\[
\mathbf{A}_r^k \mathbf{z}_n = \mathbf{z}^{(M-k)} \quad (k=0,1,\ldots,M)
\]

（其中 \(k=0\) 时 \(\mathbf{A}_r^0\mathbf{z}_n = \mathbf{z}_n = \mathbf{z}^{(M)}\)，也满足此公式。）

#### 物理意义与用途

公式 (90) 揭示了一个重要事实：**在第 \(n\) 个时间步的递推过程中产生的所有辅助变量 \(\mathbf{z}^{(0)}, \mathbf{z}^{(1)}, \ldots, \mathbf{z}^{(M-1)}\)，恰好等于 \(\mathbf{A}_r^M\mathbf{z}_n, \mathbf{A}_r^{M-1}\mathbf{z}_n, \ldots, \mathbf{A}_r\mathbf{z}_n\)**。这些辅助变量在计算第 \(n+1\) 个时间步的 \(\mathbf{P}_r\mathbf{z}_n\) 时直接可用（见公式 (91)），无需重新进行任何矩阵-向量乘积。

---

### 公式 (91)：利用辅助变量高效计算 \(\mathbf{P}_r\mathbf{z}_n\)

\[
\mathbf{P}_r \mathbf{z}_n = p_{r0}\mathbf{z}_n + p_{r1}\mathbf{z}^{(M-1)} + \cdots + p_{rM}\mathbf{z}^{(0)} \tag{91}
\]

#### 来源

将公式 (81a) 中的 \(\mathbf{P}_r(\mathbf{A}_r)\) 展开作用于 \(\mathbf{z}_n\)：

\[
\mathbf{P}_r\mathbf{z}_n = (p_{r0}\mathbf{I} + p_{r1}\mathbf{A}_r + \cdots + p_{rM}\mathbf{A}_r^M)\mathbf{z}_n
= p_{r0}\mathbf{z}_n + p_{r1}(\mathbf{A}_r\mathbf{z}_n) + \cdots + p_{rM}(\mathbf{A}_r^M\mathbf{z}_n)
\]

再利用公式 (90) \(\mathbf{A}_r^k\mathbf{z}_n = \mathbf{z}^{(M-k)}\)，将每一项替换：

\[
\mathbf{P}_r\mathbf{z}_n = p_{r0}\mathbf{z}_n + p_{r1}\mathbf{z}^{(M-1)} + p_{r2}\mathbf{z}^{(M-2)} + \cdots + p_{rM}\mathbf{z}^{(0)}
\]

这正是公式 (91)。

#### 效率分析

若按照常规方式计算 \(\mathbf{P}_r\mathbf{z}_n\)，需要计算 \(\mathbf{A}_r\mathbf{z}_n, \mathbf{A}_r^2\mathbf{z}_n, \ldots\)，每次都涉及矩阵-向量乘积（代价 \(O(n^2)\) 或更高）。而公式 (91) 将所有矩阵-向量乘积用已保存的辅助向量代替，计算 \(\mathbf{P}_r\mathbf{z}_n\) 只需 \(M+1\) 次向量加法和标量乘法（代价 \(O(n)\)），**完全消除了矩阵-向量乘积**。这是算法设计的精妙之处之一。

---

### 公式 (92)：利用状态方程计算加速度

\[
\dot{\mathbf{z}}_n = \mathbf{A}\mathbf{z}_n + \mathbf{F}_n(s=1) \tag{92}
\]

#### 来源

公式 (92) 是第 2 节公式 (7)（无量纲化的状态方程）在 \(s=1\)（即时刻 \(t=t_n\)，时间步末尾）处的取值：

\[
\frac{\mathrm{d}\mathbf{z}}{\mathrm{d}s} = \mathbf{A}\mathbf{z} + \mathbf{F}(s)
\]

在 \(s=1\)，\(\mathbf{z} = \mathbf{z}_n\)，即：

\[
\dot{\mathbf{z}}_n = \mathbf{A}\mathbf{z}_n + \mathbf{F}_n(s=1)
\]

#### 含义

向量 \(\dot{\mathbf{z}}_n\) 的上半部分即为无量纲加速度 \(\ddot{\mathbf{u}}^{\circ\circ}_n\)（由第 2 节公式 (5) 定义），由此得到物理加速度：

\[
\ddot{\mathbf{u}}_n = \frac{\ddot{\mathbf{u}}^{\circ\circ}_n}{\Delta t^2}
\]

**计算难点**：公式 (92) 右端包含矩阵-向量乘积 \(\mathbf{A}\mathbf{z}_n\)。直接计算代价 \(O(n^2)\)（或更大），且 \(\mathbf{A}\) 通常不是稀疏矩阵（即使 \(\mathbf{M}\)、\(\mathbf{C}\)、\(\mathbf{K}\) 是稀疏的，\(\mathbf{A}\) 中含有 \(\mathbf{M}^{-1}\mathbf{C}\) 和 \(\mathbf{M}^{-1}\mathbf{K}\)，计算代价较高）。公式 (93) 正是为解决这一问题而提出的。

---

### 公式 (93)：无矩阵运算的加速度计算公式

\[
\dot{\mathbf{z}}_n = r\mathbf{z}_n - \mathbf{z}^{(M-1)} + \mathbf{F}_n(s=1) \tag{93}
\]

#### 推导过程

由公式 (80)（\(\mathbf{A}_r = r\mathbf{I} - \mathbf{A}\)），解出：

\[
\mathbf{A} = r\mathbf{I} - \mathbf{A}_r
\]

代入公式 (92)：

\[
\dot{\mathbf{z}}_n = (r\mathbf{I} - \mathbf{A}_r)\mathbf{z}_n + \mathbf{F}_n(s=1) = r\mathbf{z}_n - \mathbf{A}_r\mathbf{z}_n + \mathbf{F}_n(s=1)
\]

再利用公式 (90)（\(\mathbf{A}_r^1\mathbf{z}_n = \mathbf{z}^{(M-1)}\)，即 \(k=1\) 的情形）：

\[
\mathbf{A}_r\mathbf{z}_n = \mathbf{z}^{(M-1)}
\]

代入，得：

\[
\dot{\mathbf{z}}_n = r\mathbf{z}_n - \mathbf{z}^{(M-1)} + \mathbf{F}_n(s=1)
\]

这正是公式 (93)。

#### 优越性

公式 (93) 用**辅助向量** \(\mathbf{z}^{(M-1)}\)（在计算最后一个子步时已得到）替换了原来的**矩阵-向量乘积** \(\mathbf{A}_r\mathbf{z}_n\)，将加速度的计算降为纯向量运算（\(O(n)\)），效率极高。外力项 \(\mathbf{F}_n(s=1)\) 是外力的取值，计算代价也很低。

---

### 公式 (94)：除以 \(r^2\) 后的子步方程

\[
\left(\mathbf{M} + \frac{\Delta t}{r}\mathbf{C} + \left(\frac{\Delta t}{r}\right)^2\mathbf{K}\right) \mathbf{z}_1^{(k)} = \frac{1}{r}\mathbf{M}\mathbf{z}_1^{(k-1)} - \left(\frac{\Delta t}{r}\right)^2\mathbf{K}\mathbf{z}_2^{(k-1)} \tag{94}
\]

#### 推导

公式 (94) 是将公式 (88) 两端同除以 \(r^2\) 的结果：

\[
\frac{1}{r^2}(r^2\mathbf{M} + r\Delta t\mathbf{C} + \Delta t^2\mathbf{K})\mathbf{z}_1^{(k)} = \frac{1}{r^2}\left(r\mathbf{M}\mathbf{z}_1^{(k-1)} - \Delta t^2\mathbf{K}\mathbf{z}_2^{(k-1)}\right)
\]

化简即得公式 (94)。

#### 目的

公式 (94) 的目的是将有效刚度矩阵的形式与 Bathe 复合法进行对比，以便识别两者之间的参数对应关系（见公式 (95)）。

---

### 公式 (95)：时间劈裂比 \(\gamma\) 与根 \(r\) 的关系

\[
\gamma = \frac{2}{r} \tag{95}
\]

#### 推导与对比

Bathe 复合时间积分法（文中参考文献 [33]）使用两个子步，步长分别为 \(\gamma\Delta t\) 和 \((1-\gamma)\Delta t\)（其中 \(\gamma \in (0,1)\)）。其第一子步（梯形法则，步长 \(\gamma\Delta t\)）的有效刚度矩阵为：

\[
\mathbf{K}_{\text{eff}}^{\text{Bathe}} = \frac{4}{\gamma^2\Delta t^2}\mathbf{M} + \frac{2}{\gamma\Delta t}\mathbf{C} + \mathbf{K}
\]

将其与公式 (94) 的有效刚度矩阵 \(\mathbf{M} + \frac{\Delta t}{r}\mathbf{C} + \left(\frac{\Delta t}{r}\right)^2\mathbf{K}\)（乘以 \(\mathbf{K}\) 的系数统一后比较）对比，令两者系数比例匹配：

\[
\frac{2}{\gamma\Delta t} \longleftrightarrow \frac{\Delta t}{r}, \quad \text{即} \quad \frac{2}{\gamma} = \frac{\Delta t^2}{(\Delta t/r)\Delta t} = r
\]

因此 \(\gamma = 2/r\)，即公式 (95)。

#### 意义

公式 (95) 建立了本文框架（参数 \(r\)）与 Bathe 方法（参数 \(\gamma\)）之间的精确映射：**本文的根 \(r\) 与 Bathe 方法的时间劈裂比 \(\gamma\) 之间满足简单的倒数关系** \(\gamma r = 2\)。这说明本文方法在 \(M=2\) 时本质上与 Bathe 方法处于同一框架下，只是参数化方式不同。

---

### 公式 (96)：\(M=2\) 时与 \(\rho_\infty\)-Bathe 方法的劈裂比等价

\[
\gamma = \frac{2}{2 + \sqrt{2 + 2\rho_{\infty}}} = \frac{2 - \sqrt{2 + 2\rho_{\infty}}}{1 - \rho_{\infty}} \tag{96}
\]

#### 推导

将第 3 节公式 (60)（\(M=2\) 时的根解析解）：

\[
r = 2 + \sqrt{2 + 2\rho_\infty}
\]

代入公式 (95)（\(\gamma = 2/r\)）：

\[
\gamma = \frac{2}{2 + \sqrt{2 + 2\rho_\infty}}
\]

这是公式 (96) 的第一个等号。对其进行有理化（分子分母同乘以共轭 \(2-\sqrt{2+2\rho_\infty}\)）：

\[
\gamma = \frac{2(2-\sqrt{2+2\rho_\infty})}{(2+\sqrt{2+2\rho_\infty})(2-\sqrt{2+2\rho_\infty})} = \frac{2(2-\sqrt{2+2\rho_\infty})}{4-(2+2\rho_\infty)} = \frac{2(2-\sqrt{2+2\rho_\infty})}{2-2\rho_\infty} = \frac{2-\sqrt{2+2\rho_\infty}}{1-\rho_\infty}
\]

这是公式 (96) 的第二个等号，与文献 [33] 中 \(\rho_\infty\)-Bathe 方法的劈裂比完全吻合。

#### 特殊情形验证

- **\(\rho_\infty = 0\)**：\(\gamma = \frac{2}{2+\sqrt{2}} = \frac{2}{2+1.414} \approx 0.586 = 1 - \frac{1}{\sqrt{2}}\)——最大数值耗散，对应 L-稳定的 Bathe 方法；
- **\(\rho_\infty = 1\)**：分母 \(1-\rho_\infty = 0\)，需用公式 (96) 第一形式：\(\gamma = \frac{2}{2+\sqrt{4}} = \frac{2}{4} = 0.5\)——无数值耗散，对应 Bathe 的经典方法（梯形+后向 Euler）；
- **\(\rho_\infty = 0.5\)**：\(\gamma = \frac{2}{2+\sqrt{3}} = 2/(3.732) \approx 0.536\)——适中的数值耗散。

#### 重要推论：算法的等价性

公式 (96) 连同其 \((M+1)\)-格式版本（\(r=3-\sqrt{3}\)，\(\gamma = 2/(3-\sqrt{3}) = 1+\sqrt{3}/3\)，\(\rho_\infty = \sqrt{3}-1 \approx 0.732\)），证明了**本文的二子步格式与 Bathe \(\rho_\infty\)-方法在数值上完全等价**，只是从不同的理论出发点（有理近似 vs. 经典复合法）达到了同一算法。这为本文方法提供了独立的理论验证，也说明 Bathe 方法可视为本文框架的一个特例。

---

## 第 4 节小结：算法流程

综合公式 (80)–(96)，完整的时间步进算法（第 \(n-1\) 步到第 \(n\) 步）流程如下：

**预处理（只做一次）**：
1. 由第 3 节公式 (42) 或 (67) 确定根 \(r\)；
2. 由公式 (40) 计算系数 \(p_i\)（\(i=0,\ldots,M\)）；
3. 由公式 (81a) 计算系数 \(p_{ri}\)；
4. 组装并分解有效刚度矩阵 \(\mathbf{K}_{\text{eff}} = r^2\mathbf{M} + r\Delta t\mathbf{C} + \Delta t^2\mathbf{K}\)（公式 (88) 左端）。

**每个时间步**：
1. 由公式 (84) 和 (91) 计算初始辅助向量 \(\mathbf{z}^{(0)}\)（无矩阵运算）；
2. 对 \(k=1,2,\ldots,M\)：
   - 由公式 (88) 求解 \(\mathbf{z}_1^{(k)}\)（前/后代换）；
   - 由公式 (89) 计算 \(\mathbf{z}_2^{(k)}\)（向量运算）；
3. 令 \(\mathbf{z}_n = \mathbf{z}^{(M)}\)，得到时间步末的状态向量（位移与速度）；
4. 由公式 (93) 计算加速度 \(\dot{\mathbf{z}}_n\)（向量运算）；
5. 保存辅助变量 \(\mathbf{z}^{(0)}, \mathbf{z}^{(1)}, \ldots, \mathbf{z}^{(M-1)}\) 供下一步使用（公式 (91)）。

---

## 5. 数值算例

第 5 节通过一系列标准算例验证第 4 节算法的精度、耗散特性和计算效率。本节共涉及 3 个关键定量公式：公式 (97)（外力函数）、公式 (98)（\(L_2\) 误差范数）和公式 (99)（CFL 数）。

---

### 5.1 单自由度系统

#### 公式 (97)：单自由度系统的外力函数

\[
f_1(t) = 10\cos\!\left(\frac{2\sqrt{5}}{5}t\right) + 70\sin\!\left(2\sqrt{10}\,t\right) \tag{97}
\]

#### 含义与背景

公式 (97) 给出的是一个**双频谐波外力**，由两个频率不同的谐波分量叠加而成：

- **分量 1**：\(10\cos\!\left(\frac{2\sqrt{5}}{5}t\right)\)，角频率 \(\omega_{f1} = \frac{2\sqrt{5}}{5} \approx 0.894\,\mathrm{rad/s}\)，振幅 10；
- **分量 2**：\(70\sin(2\sqrt{10}\,t)\)，角频率 \(\omega_{f2} = 2\sqrt{10} \approx 6.325\,\mathrm{rad/s}\)，振幅 70。

参数设置说明：
- 自然频率 \(\omega = 2\pi \approx 6.28\,\mathrm{rad/s}\)；
- 阻尼比 \(\xi=0.05\)（轻阻尼系统）；
- 初始位移 \(u_0=2.0\,\mathrm{m}\)，初始速度 \(\dot{u}_0 = \pi/3\,\mathrm{m/s}\)。

选取此外力的目的是同时测试方案对**低频外力（\(\omega_{f1} < \omega\)）**和**高频外力（\(\omega_{f2} \approx \omega\)，接近共振）**的响应精度，使得该算例既包含瞬态响应（来自初始条件）又包含稳态响应（来自外力激励），能全面考察时间积分方案的精度。

#### 解析解的存在性

由于运动方程为线性常系数常微分方程，且外力为谐波函数，可以用叠加原理求得精确解析解。精确解由以下三部分叠加而成：
1. 齐次方程对应初始条件的瞬态解；
2. 频率 \(\omega_{f1}\) 的特解（稳态响应）；
3. 频率 \(\omega_{f2}\) 的特解（稳态响应）。

有了精确解，就可以通过公式 (98) 计算数值解的误差。

---

#### 公式 (98)：\(L_2\) 范数误差

\[
\epsilon_{L_2} = \frac{\int_{t=0}^{t_{\mathrm{sim}}} (u_{\mathrm{exact}}(t) - u_{\mathrm{numerical}}(t))^2\,\mathrm{d}t}{\int_{t=0}^{t_{\mathrm{sim}}} u_{\mathrm{exact}}(t)^2\,\mathrm{d}t} \times 100\% \tag{98}
\]

#### 定义与含义

公式 (98) 定义的是**相对 \(L_2\) 范数误差**（以百分比表示）：

- **分子** \(\int_0^{t_{\mathrm{sim}}} (u_{\mathrm{exact}}-u_{\mathrm{numerical}})^2\,\mathrm{d}t\)：精确解与数值解之差的平方在整个模拟区间上的积分，衡量全局误差的大小；
- **分母** \(\int_0^{t_{\mathrm{sim}}} u_{\mathrm{exact}}^2\,\mathrm{d}t\)：精确解的平方积分，作为归一化因子（使误差具有无量纲性并可跨问题比较）；
- **乘以 100%**：将误差以百分比表示。

#### 数学背景

\(L_2\) 范数是泛函分析中最常用的函数范数之一：

\[
\|f\|_{L_2} = \sqrt{\int_0^{T} f(t)^2\,\mathrm{d}t}
\]

公式 (98) 等价于：

\[
\epsilon_{L_2} = \left(\frac{\|u_{\mathrm{exact}} - u_{\mathrm{numerical}}\|_{L_2}}{\|u_{\mathrm{exact}}\|_{L_2}}\right)^2 \times 100\%
\]

注意此处是平方比（而非范数比），这使得精度阶数 \(p\) 与误差 \(\epsilon_{L_2}\) 的关系为 \(\epsilon_{L_2} \propto \Delta t^{2p}\)（若范数比定义则为 \(\Delta t^p\)）。因此在双对数坐标（\(\log\epsilon_{L_2}\) vs. \(\log\Delta t\)）图中，斜率为 \(2p\)，即：**\(M\) 阶格式的斜率为 \(2M\)**，\((M+1)\)-格式的斜率为 \(2(M+1)\)。

#### 数值积分

实际计算中，精确解 \(u_{\mathrm{exact}}(t)\) 已知，数值解 \(u_{\mathrm{numerical}}(t_j)\) 在离散时间点 \(t_j = j\Delta t\) 处已知（分段常值或线性插值）。两个积分均用离散求和近似（梯形公式或辛普森规则）。

---

### 5.3 一维双材料杆波传播问题

#### 公式 (99)：CFL 数

\[
\mathrm{CFL} = c_2\,\frac{\Delta t}{\Delta x} \tag{99}
\]

#### 定义

**CFL 数**（Courant-Friedrichs-Lewy 数）是波传播问题中最重要的无量纲时间步长参数，定义为：

\[
\mathrm{CFL} = \frac{\text{波速} \times \text{时间步长}}{\text{空间步长}} = \frac{c_2 \cdot \Delta t}{\Delta x}
\]

其中：
- \(c_2 = 20\sqrt{2}\,\mathrm{m/s}\) 为右侧材料段的波速；
- \(\Delta t\) 为时间步长；
- \(\Delta x\) 为有限元单元尺寸。

#### 物理意义

CFL 数度量的是**波在一个时间步内传播的距离与单元尺寸之比**：
- \(\mathrm{CFL} = 1\)：一个时间步内波恰好传播一个单元——对显式格式而言是稳定性上限；
- \(\mathrm{CFL} > 1\)：一个时间步内波传播超过一个单元——显式格式不稳定，但**隐式格式无条件稳定**，允许 \(\mathrm{CFL}\gg 1\)；
- **计算效率**：CFL 数越大，时间步长越大，在同等精度下需要的时间步数越少，计算效率越高。

#### 在本文中的作用

本文讨论的是隐式格式，不受稳定性约束，CFL 数的主要作用是衡量精度性能：

- 对于低阶格式（如 \(M=2\)，二阶），CFL 数太大会导致明显的数值频散，通常需要较小的 CFL（约 1–2）；
- 对于高阶格式（如 \(M=4,5,6\)），数值频散特性更好，可以使用更大的 CFL（5–16），从而在相同精度下减少时间步数；
- **速度提升估计**：对比两种方案的效率，可用 \(\frac{\mathrm{CFL}_M / \mathrm{CFL}_2}{M/2}\) 估算相对于 \(M=2\) 格式的速度提升因子。

#### 多材料问题中的 CFL 定义

本文中，双材料杆有两段，波速不同（\(c_1 = 40\sqrt{5}\,\mathrm{m/s}\), \(c_2 = 20\sqrt{2}\,\mathrm{m/s}\)）。公式 (99) 统一用**较小**波速 \(c_2\) 定义 CFL，这是因为 \(c_2 < c_1\)，用较小波速定义的 CFL 更为保守（若用 \(c_1\) 定义，同样的 \(\Delta t\) 对应更大的 CFL，可能掩盖左侧材料中的数值问题）。

---

## 第 5 节数值结论总结

通过公式 (97)–(99) 定义的算例框架，文中验证了以下关键结论（数值实验结果，不涉及新公式）：

| 算例 | 主要结论 |
|------|------|
| SDOF（§5.1，公式(97)(98)）| \(M\)-格式按 \(2M\) 阶（双对数坐标）收敛；\(M=2\) 与 Bathe 方法数值完全等价 |
| 三自由度刚系统（§5.2）| \(\rho_\infty=0\)（最大耗散）推荐用于刚性系统；\(M\) 越大收敛越快；\(M=5\) 基本达到参考解 |
| 双材料杆波传播（§5.3，公式(99)）| \(M\) 越大允许的 CFL 越大（\(M=2\)：CFL≈1.2；\(M=6\)：CFL≈16）；速度提升因子 2.7–4.4 |
| 非规则网格矩形体（§5.4）| 高阶格式对不规则网格更鲁棒；实测速度提升与理论估计吻合 |
| Lamb 波问题（§5.5）| \(M=3,4\) 结果无高频振荡；速度提升 1.64、2.01（实测）vs 1.67、2.0（估计）|
| 开孔墙冲击（§5.6）| 加速度响应中 \(M=2\) 有强高频振荡，\(M=3,4\) 有效抑制；速度提升 1.49、1.83 |

---

## 公式总览与内在联系

### 第 4 节公式的推导链

```
公式(80)：A_r = rI - A
     ├─→ 公式(81a)(81b)：P(A), Q(A) 改写为 A_r 的多项式
     │         ↓
     │    公式(82)：A_r^M z_n = P_r z_{n-1} + 外力项
     │         ↓
     │    公式(83)(84)：引入 z^(0)..z^(M)，建立递推关系
     │         ↓
     │    公式(85)：M 个子步方程 A_r z^(k) = z^(k-1)
     │         ↓
     │    公式(86)：(rI-A) z^(k) = z^(k-1)
     │         ↓
     │    公式(87)：对 z^(k) 上下分块
     │         ↓
     │    公式(88)：隐式求解 z_1^(k)（核心方程）
     │    公式(89)：显式更新 z_2^(k)（向量运算）
     │
     ├─→ 公式(90)：A_r^k z_n = z^(M-k)（辅助变量意义）
     │         ↓
     │    公式(91)：P_r z_n 的高效计算（无矩阵运算）
     │
     └─→ 公式(92)：加速度的状态方程表达
               ↓（利用公式(90)消去矩阵-向量乘积）
          公式(93)：加速度的高效计算公式

公式(94)：公式(88) 除以 r^2 的标准化形式
     ↓
公式(95)：等价关系 γ = 2/r（连接 Bathe 方法）
     ↓（代入公式(60)）
公式(96)：与 ρ∞-Bathe 方法的劈裂比完全等价
```

### 第 5 节公式的角色

| 公式 | 角色 |
|------|------|
| (97) | 算例参数：定义 SDOF 算例的外力（双频谐波），使精确解可求，可用 (98) 评估精度 |
| (98) | 精度度量：\(L_2\) 相对误差范数（百分比），在双对数图中斜率为 \(2M\)，直接体现格式的精度阶数 |
| (99) | 步长度量：CFL 数，是连接时间步长 \(\Delta t\)、空间步长 \(\Delta x\) 和波速 \(c\) 的无量纲参数；大 CFL = 高效率 |

### 跨章节公式的对应关系

| 第 3 节（格式构造）| 第 4 节（算法实现）|
|------|------|
| 公式(42)/(67)：确定根 \(r\) | 公式(80)：定义 \(\mathbf{A}_r\) 用于分解子步 |
| 公式(40)：系数 \(p_i\)（以 \(\mathbf{A}\) 为基）| 公式(81a)：系数 \(p_{ri}\)（以 \(\mathbf{A}_r\) 为基）|
| 公式(44)/(69)：\(\mathbf{R} = \mathbf{P}/\mathbf{Q}\) | 公式(82)：\(\mathbf{Q}_r\mathbf{z}_n = \mathbf{P}_r\mathbf{z}_{n-1}+\cdots\) |
| 公式(60)：\(r=2+\sqrt{2+2\rho_\infty}\)（M=2）| 公式(96)：\(\gamma = 2/r = \gamma_{\text{Bathe}}\)（等价性证明）|

---

> **注**：本节所有符号与文献原文保持一致。\(\mathbf{A}\) 为公式 (8) 定义的 \(2n\times 2n\) 状态空间矩阵；\(\mathbf{z}\) 为 \(2n\) 维状态向量（上半速度、下半位移）；\(r\) 为第 3 节确定的实数根；\(\Delta t\) 为时间步长；\(n\) 为有限元模型自由度数；\(\mathbf{M}\)、\(\mathbf{C}\)、\(\mathbf{K}\) 分别为质量、阻尼、刚度矩阵；\(c_2\) 为右侧材料波速；\(\Delta x\) 为单元尺寸。
