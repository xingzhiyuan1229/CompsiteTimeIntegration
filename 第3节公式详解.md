# 第 3 节公式逐一详解

> **依据文献**：Song, C. & Zhang, X. (2024). High-order composite implicit time integration schemes based on rational approximations for elastodynamics. *Computer Methods in Applied Mechanics and Engineering*, 418, 116473.  
> **解析范围**：原文第 3 节（Section 3）所有公式 (34) — (79)

---

## 引言与总体说明

第 3 节是全文的核心构造性章节，其任务是**为第 2 节中已建立的有理近似框架（公式 (15)–(20)）选取一种具有特殊结构的分母多项式**，并据此推导出所有分子系数，从而完整确定时间积分格式中的有理近似 \(\mathbf{R}\)。

本节的逻辑主线如下：

1. **总体框架（公式 (34)–(40)）**：将分母多项式 \(\mathbf{Q}\) 限定为 \((r\mathbf{I}-\mathbf{A})^M\) 的形式（单重根 \(r\) 的 \(M\) 次幂），写出标量化的振幅因子，通过二项式展开与 Taylor 展开的乘积，导出分子系数 \(p_i(r)\) 关于根 \(r\) 的显式表达式。
2. **\(M\)-格式（公式 (41)–(65)）**：固定截断阶数为 \(M\)，由高频极限处的谱半径 \(\rho_\infty\) 来约束根 \(r\)，得到用户可调耗散量的一族 \(M\) 阶精度格式；分别对 \(M=1,2,3\) 给出完整推导。
3. **\((M+1)\)-格式（公式 (66)–(79)）**：通过令 \(\lambda^{M+1}\) 项的系数为零来最大化精度阶数（达到 \(M+1\) 阶），由此唯一确定根 \(r\)，耗散量不可调；以 \(M=2\) 为例进行完整推导。

三部分之间的逻辑关联：**框架（(34)–(40)）是共同起点，\(M\)-格式与 \((M+1)\)-格式分别代表"灵活性优先"与"精度优先"的两种设计哲学，二者共享相同的分母结构与系数公式 (40)**。

---

## 3. 复合时间积分格式的矩阵指数有理近似构造

### 公式 (34)：有理近似的结构化选取

\[
e^{\mathbf{A}} \approx \mathbf{R} = \frac{\mathbf{P}}{\mathbf{Q}} = \frac{p_0\mathbf{I} + p_1\mathbf{A} + \cdots + p_L\mathbf{A}^L}{(r\mathbf{I} - \mathbf{A})^M}, \qquad (M \ge L) \tag{34}
\]

#### 来源与背景

公式 (34) 是对第 2 节公式 (15)–(16) 的具体化。回顾公式 (15)：

\[
e^{\mathbf{A}} \approx \mathbf{R} = \frac{\mathbf{P}}{\mathbf{Q}}
\]

其中 \(\mathbf{P}\)、\(\mathbf{Q}\) 是矩阵 \(\mathbf{A}\) 的任意多项式（见公式 (16a)(16b)）。在一般情况下，分母多项式 \(\mathbf{Q}\) 有多个不同的根（零点），即求解每个子步时所用的"有效刚度矩阵"各不相同，这会显著增加计算量（每个子步都要进行独立的矩阵分解）。

**本文的关键创新**：将 \(\mathbf{Q}\) 限定为 **单重根 \(r\) 的 \(M\) 次幂**，即：

\[
\mathbf{Q}(\mathbf{A}) = (r\mathbf{I} - \mathbf{A})^M
\]

这一选择的深刻意义将在第 4 节中显现：**\(\mathbf{Q}\) 的每一个因子 \((r\mathbf{I} - \mathbf{A})\) 恰好对应复合时间积分中的一个子步**，而且所有子步使用的有效刚度矩阵完全相同，只需进行一次矩阵分解，从而在保持高精度的同时大幅降低计算成本。

#### 物理意义

- **分子 \(\mathbf{P}\)**：\(L\) 次多项式，共 \(L+1\) 个待定系数 \(p_0, p_1, \ldots, p_L\)；
- **分母 \(\mathbf{Q}\)**：单根 \(r\) 的 \(M\) 次幂，参数只有一个标量 \(r\)；
- 约束条件 \(M \ge L\)：来自公式 (17) 的稳定性条件（分母阶 \(N_q\) 不小于分子阶 \(N_p\)），对应第 2 节中讨论的无条件稳定性要求。

#### 与后续公式的联系

公式 (34) 是整节的**结构骨架**，后续推导的目标就是确定其中的两类未知量：根 \(r\) 和系数 \(p_i\)。公式 (35)–(40) 给出系数 \(p_i\) 关于 \(r\) 的显式公式；3.1 节公式 (41)–(65) 和 3.2 节公式 (66)–(79) 分别用不同准则确定 \(r\)。

---

### 公式 (35)：标量化的振幅因子

\[
e^{\lambda} \approx R(\lambda) = \frac{P(\lambda)}{Q(\lambda)} = \frac{p_0 + p_1\lambda + \cdots + p_L\lambda^L}{(r - \lambda)^M} \tag{35}
\]

#### 来源与推导

公式 (35) 是公式 (34) 的**标量对应形式**。将矩阵 \(\mathbf{A}\) 替换为其（复）特征值 \(\lambda\)（由第 2 节公式 (22)–(24) 给出，\(\lambda = i\omega\Delta t\)），矩阵多项式退化为标量多项式，矩阵方程退化为标量方程：

- 矩阵版：\(\mathbf{R} = \frac{p_0\mathbf{I} + p_1\mathbf{A} + \cdots + p_L\mathbf{A}^L}{(r\mathbf{I}-\mathbf{A})^M}\)
- 标量版：\(R(\lambda) = \frac{p_0 + p_1\lambda + \cdots + p_L\lambda^L}{(r-\lambda)^M}\)

因此，\(R(\lambda)\) 即为第 2 节公式 (26) 中定义的**振幅因子（amplification factor）**，它是时间步进格式的核心谱量。

#### 为何研究标量形式

在进行精度分析时，只需考虑系统的单一特征模态（第 2 节公式 (21)–(25)）。对单模态 SDOF 系统，矩阵方程退化为标量方程，\(R(\lambda)\) 完全决定了每一时间步后振幅的放大（或衰减）比例和相位的变化量。如果 \(R(\lambda)\) 能以足够高的精度近似 \(e^\lambda\)，则对应的时间积分方案具有足够高的精度。

#### 关键量的对应关系

- **谱半径**：\(\rho = |R(\lambda)|\)（公式 (27)）——控制数值耗散；
- **相位角**：\(\bar{\Omega} = \arg(R(\lambda))\)（公式 (28)）——控制数值频散；
- **高频极限谱半径** \(\rho_\infty = \lim_{\lambda\to\infty}|R(\lambda)|\)（公式 (33)）——由 \(p_L/(-1)^M\) 决定，用于量化高频振荡的抑制能力。

---

### 公式 (36)：展开的准备形式

\[
(r - \lambda)^M e^{\lambda} \approx p_0 + p_1\lambda + \cdots + p_L\lambda^L \tag{36}
\]

#### 来源

将公式 (35) 两边同乘以分母 \((r-\lambda)^M\)，即得到公式 (36)。这是一个等价变形，目的是将有理函数近似转化为**多项式近似问题**，便于利用 Taylor 展开和幂次匹配来逐项确定系数 \(p_i\)。

#### 核心思路

- 左侧 \((r-\lambda)^M e^\lambda\) 是一个无穷级数；
- 右侧 \(p_0 + p_1\lambda + \cdots + p_L\lambda^L\) 是一个有限多项式；
- 匹配两边 \(\lambda^0, \lambda^1, \ldots, \lambda^L\) 的系数，即可唯一确定所有 \(p_i\)。

这一思路贯穿整个第 3 节——公式 (37)–(40) 完成这一匹配的具体计算。

---

### 公式 (37)：二项式展开

\[
(r - \lambda)^M = \sum_{j=0}^{M} \frac{M!}{j!(M-j)!} r^{M-j}(-\lambda)^j \tag{37}
\]

#### 推导

这是经典的**二项式定理**（Newton's binomial theorem）的直接应用。将 \((r-\lambda)^M = (r + (-\lambda))^M\) 展开：

\[
(r-\lambda)^M = \sum_{j=0}^{M} \binom{M}{j} r^{M-j}(-\lambda)^j = \sum_{j=0}^{M} \frac{M!}{j!(M-j)!} r^{M-j}(-1)^j \lambda^j
\]

展开后得到有限项（因为是有限阶多项式），各项的系数由组合数 \(\binom{M}{j} = \frac{M!}{j!(M-j)!}\) 给出。

#### 为何这样展开

后续需要将 \((r-\lambda)^M\) 与 \(e^\lambda\) 的 Taylor 级数相乘，展开成关于 \(\lambda\) 的幂级数。因此，首先将 \((r-\lambda)^M\) 展成标准幂次形式是必要的。

#### 具体示例（\(M=2\)）

\[
(r-\lambda)^2 = r^2 - 2r\lambda + \lambda^2
\]

对应公式 (37) 中 \(j=0,1,2\) 三项：

- \(j=0\)：\(\binom{2}{0}r^2(-\lambda)^0 = r^2\)
- \(j=1\)：\(\binom{2}{1}r^1(-\lambda)^1 = -2r\lambda\)
- \(j=2\)：\(\binom{2}{2}r^0(-\lambda)^2 = \lambda^2\)

---

### 公式 (38)：指数函数的 Taylor 展开

\[
e^{\lambda} \approx \sum_{k=0}^{\infty} \frac{1}{k!}\lambda^k \tag{38}
\]

#### 背景

这是数学分析中最基本的 Taylor 展开之一，在原点 \(\lambda=0\) 处对指数函数展开，收敛半径为 \(+\infty\)。具体地：

\[
e^\lambda = 1 + \lambda + \frac{\lambda^2}{2!} + \frac{\lambda^3}{3!} + \cdots = \sum_{k=0}^{\infty}\frac{\lambda^k}{k!}
\]

#### 在推导中的作用

公式 (38) 为公式 (36) 左侧提供了 \(e^\lambda\) 的展开。注意 \(\lambda = i\omega\Delta t\) 是纯虚数，其模 \(|\lambda| = \omega\Delta t\)。在低频范围（小 \(\Delta t/T\)）内，\(|\lambda|\) 较小，Taylor 级数的前几项就能给出良好近似；在高频范围内，\(|\lambda| \gg 1\)，级数收敛很慢，此时方案的性能由高频极限谱半径 \(\rho_\infty\) 决定。

---

### 公式 (39)：乘积展开为幂级数

\[
(r - \lambda)^M e^{\lambda} \approx \sum_{i=0}^{\infty} \sum_{j=0}^{\min(i,M)} (-1)^j \frac{M!}{j!(M-j)!} \frac{1}{(i-j)!} r^{M-j} \lambda^i \tag{39}
\]

#### 推导过程

将公式 (37) 与公式 (38) 相乘：

\[
(r-\lambda)^M \cdot e^\lambda = \left(\sum_{j=0}^{M} \frac{M!}{j!(M-j)!} r^{M-j}(-1)^j \lambda^j \right) \cdot \left(\sum_{k=0}^{\infty}\frac{\lambda^k}{k!}\right)
\]

利用 Cauchy 乘积（两个幂级数的乘法规则），令 \(i = j+k\)，即 \(k = i-j\)，则 \(\lambda^j \cdot \lambda^k = \lambda^{j+k} = \lambda^i\)。对每一个 \(\lambda^i\) 次项，需汇总所有满足 \(0\le j\le M\) 且 \(j\le i\)（即 \(k = i-j \ge 0\)）的组合，因此 \(j\) 的上限为 \(\min(i, M)\)：

\[
\text{（公式 (39) 中 \(\lambda^i\) 项的系数）} = \sum_{j=0}^{\min(i,M)} (-1)^j \frac{M!}{j!(M-j)!} \cdot \frac{1}{(i-j)!} \cdot r^{M-j}
\]

这样就得到公式 (39)。

#### 数学本质

公式 (39) 是 \((r-\lambda)^M e^\lambda\) 的**完整 Taylor 展开**，以双重求和的形式给出了每一次幂 \(\lambda^i\) 的确切系数。它是从两个已知级数（公式 (37) 和 (38)）出发，通过 Cauchy 积给出的精确结果，没有任何近似。

---

### 公式 (40)：分子系数 \(p_i(r)\) 的显式公式

\[
p_i(r) = \sum_{j=0}^{\min(i,M)} (-1)^j \frac{M!}{j!(M-j)!} \frac{1}{(i-j)!} r^{M-j} \tag{40}
\]

#### 推导

将公式 (39) 代入公式 (36)，比较两端 \(\lambda^i\)（\(i=0,1,\ldots,L\)）的系数，右端 \(p_i\) 的系数即为 \(\lambda^i\) 的系数，左端则由公式 (39) 给出：

\[
p_i(r) = \sum_{j=0}^{\min(i,M)} (-1)^j \frac{M!}{j!(M-j)!} \frac{1}{(i-j)!} r^{M-j}
\]

这是完全显式的——只要给定 \(M\)（子步数）和根 \(r\) 的值，就能用此公式逐一计算出所有系数 \(p_i\)。

#### 具体示例（\(M=2\)，计算 \(p_0, p_1, p_2\)）

**\(p_0(r)\)**（\(i=0\)，\(\min(0,2)=0\)，\(j=0\)）：

\[
p_0(r) = (-1)^0 \frac{2!}{0!\cdot 2!}\frac{1}{0!}r^2 = r^2
\]

**\(p_1(r)\)**（\(i=1\)，\(\min(1,2)=1\)，\(j=0,1\)）：

\[
p_1(r) = \frac{2!}{0!\cdot 2!}\frac{1}{1!}r^2 + (-1)^1\frac{2!}{1!\cdot 1!}\frac{1}{0!}r^1 = r^2 - 2r
\]

**\(p_2(r)\)**（\(i=2\)，\(\min(2,2)=2\)，\(j=0,1,2\)）：

\[
p_2(r) = \frac{1}{2}r^2 + (-1)\frac{2!}{1!\cdot 1!}\frac{1}{1!}r + (-1)^2\frac{2!}{2!\cdot 0!}\frac{1}{0!} = \frac{r^2}{2} - 2r + 1
\]

这与公式 (51)（\(M=2\) 时的 Taylor 展开结果）完全一致，验证了公式 (40) 的正确性。

#### 与后续公式的联系

公式 (40) 是整节推导的"中枢"：

- 在 3.1 节（公式 (42)）中，令 \(p_M(r) = \pm\rho_\infty\) 来确定根 \(r\)；
- 在 3.2 节（公式 (67)）中，令 \(p_{M+1}(r) = 0\) 来确定根 \(r\)；
- 一旦确定 \(r\)，代回公式 (40) 即可计算所有需要的 \(p_i\)。

---

## 3.1 \(M\)-格式：\(M\) 个子步，\(M\) 阶精度

### 公式 (41)：\(M\)-格式的 Taylor 截断

\[
(r - \lambda)^M e^{\lambda} = p_0(r) + p_1(r)\lambda + \cdots + p_{M-1}(r)\lambda^{M-1} + p_M(r)\lambda^M + O(\lambda^{M+1}) \tag{41}
\]

#### 含义

公式 (41) 是对公式 (39)（无穷级数）的截断，**取前 \(M+1\) 项（\(\lambda^0\) 到 \(\lambda^M\)）**，后续各项归入余项 \(O(\lambda^{M+1})\)。这意味着：

\[
e^\lambda \approx R(\lambda) = \frac{p_0 + p_1\lambda + \cdots + p_M\lambda^M}{(r-\lambda)^M}
\]

的截断误差为 \(O(\lambda^{M+1})\)，即精度阶数为 \(M\)（每步误差为 \(O(\Delta t^{M+1})\)，全局误差为 \(O(\Delta t^M)\)）。

#### 关键推导细节

由公式 (40) 对 \(i=M\) 的情形：

\[
p_M(r) = \sum_{j=0}^{M} (-1)^j \frac{M!}{j!(M-j)!} \frac{1}{(M-j)!} r^{M-j}
\]

这是一个关于 \(r\) 的 \(M\) 次多项式，它恰好就是公式 (42) 的左侧。

#### 精度阶数的说明

设 \(\lambda = i\omega\Delta t\)，则 \(|\lambda| = \omega\Delta t\)。当 \(\Delta t\to 0\)（时间步长趋向零），截断误差 \(O(\lambda^{M+1}) = O((\omega\Delta t)^{M+1})\)，对应每步的局部截断误差为 \(O(\Delta t^{M+1})\)，全局误差阶为 \(O(\Delta t^M)\)，即 **\(M\) 阶精度**。

---

### 公式 (42)：由高频极限确定根 \(r\) 的方程

\[
p_M(r) = \sum_{j=0}^{M} (-1)^j \frac{M!}{j!(M-j)!} \frac{1}{(M-j)!} r^{M-j} = \pm \rho_{\infty} \tag{42}
\]

#### 推导

回顾第 2 节公式 (33)：当 \(N_q = N_p\) 时（即分子与分母同阶，这里 \(L=M\)）：

\[
\rho_\infty = \left|\frac{p_{N_p}}{q_{N_q}}\right| = \left|\frac{p_M}{(-1)^M}\right| = |p_M|
\]

其中，分母 \((r-\lambda)^M\) 在 \(\lambda\to\infty\) 时主导项为 \((-\lambda)^M = (-1)^M\lambda^M\)，故 \(q_M = (-1)^M\)。

因此，由用户指定的 \(\rho_\infty\) 值，得到：

\[
p_M(r) = \pm\rho_\infty
\]

正负号对应着不同的约束，具体如何选取取决于哪个选择导致更小的相对周期误差（见后面 \(M=1, 2, 3\) 的讨论及 Table 1）。

#### 代入公式 (40) 得到具体形式

将 \(i = M\) 代入公式 (40)，注意当 \(j > M\) 时组合数为零，所以 \(\min(M,M)=M\)，得到：

\[
p_M(r) = \sum_{j=0}^{M} (-1)^j \frac{M!}{j!(M-j)!} \frac{1}{(M-j)!} r^{M-j}
\]

这正是公式 (42) 左侧。设其等于 \(\pm\rho_\infty\)，就给出了确定根 \(r\) 的方程。

#### 方程性质

公式 (42) 是关于 \(r\) 的 **\(M\) 次代数方程**（多项式方程），一般有 \(M\) 个根（复根或实根）。从多个解中选择使谱半径 \(\rho \le 1\) 且低频相对周期误差最小的那个根（见 Table 1 的指导规则）。

---

### 公式 (43)：\(M\)-格式的标量振幅因子

\[
e^{\lambda} \approx R(\lambda) = \frac{P(\lambda)}{Q(\lambda)} = \frac{p_0 + p_1\lambda + \cdots + p_{M-1}\lambda^{M-1} \pm \rho_{\infty}\lambda^M}{(r - \lambda)^M} \tag{43}
\]

#### 来源

将公式 (42) 的结果 \(p_M = \pm\rho_\infty\) 代入公式 (41) 右侧的截断多项式，并代入公式 (35)（分母为 \((r-\lambda)^M\)），即得公式 (43)。

- 分子：由公式 (40) 和 (42) 完全确定，其最高次项系数恰为 \(\pm\rho_\infty\)，这与高频极限谱半径的定义一致；
- 分母：\((r-\lambda)^M\)，由选定的根 \(r\) 确定。

#### 特殊情形

当 \(\rho_\infty=1\) 时，不存在数值耗散；当 \(\rho_\infty=0\) 时，所有高频模态在一个时间步内即被完全消除（最大数值耗散）。

---

### 公式 (44)：\(M\)-格式的矩阵指数有理近似

\[
\mathbf{R} = \frac{\mathbf{P}}{\mathbf{Q}} = \frac{p_0\mathbf{I} + p_1\mathbf{A} + \cdots + p_{M-1}\mathbf{A}^{M-1} \pm \rho_{\infty}\mathbf{A}^M}{(r\mathbf{I} - \mathbf{A})^M} \tag{44}
\]

#### 来源

公式 (44) 是公式 (43) 的**矩阵形式**：将标量 \(\lambda\) 替换回矩阵 \(\mathbf{A}\)，将数字 1 替换为单位矩阵 \(\mathbf{I}\)，即完成从标量到矩阵的提升。

#### 与第 2 节公式的对应关系

公式 (44) 正是公式 (34)（总体框架）在 \(L=M\) 时的完整展开：

- 分子多项式 \(\mathbf{P}\) 的系数由 \(p_0(r), \ldots, p_{M-1}(r)\)（由公式 (40) 计算）和最高次项系数 \(\pm\rho_\infty\) 共同构成；
- 分母多项式 \(\mathbf{Q} = (r\mathbf{I}-\mathbf{A})^M\)。

公式 (44) 将被直接代入第 2 节的公式 (18)（有理近似版的时间步进方程），并在第 4 节中分解为 \(M\) 个子步的递推方程组（公式 (85)）。

---

### \(M=1\) 的具体推导

#### 公式 (45)：\(M=1\) 时的 Taylor 展开输出

\[
\mathrm{p}[r,\lambda] \Rightarrow r + (-1 + r)\lambda \tag{45}
\]

**推导**：将 \(M=1\) 代入公式 (40)：

- \(p_0(r)\)（\(i=0\)）：\(\frac{1!}{0!\cdot 1!}\frac{1}{0!}r^1 = r\)
- \(p_1(r)\)（\(i=1\)）：\(\frac{1!}{0!\cdot 1!}\frac{1}{1!}r^1 + (-1)^1\frac{1!}{1!\cdot 0!}\frac{1}{0!}r^0 = r - 1\)

因此 \((r-\lambda)^1 e^\lambda \approx r + (r-1)\lambda\)，与公式 (45) 一致（注意 \(r-1 = -1+r\)）。

---

#### 公式 (46)：令最高次项系数等于 \(\rho_\infty\)

\[
-1 + r = \rho_{\infty} \tag{46}
\]

**来源**：将 \(M=1\) 代入公式 (42)。\(p_1(r) = r-1\)，令 \(p_1(r) = \rho_\infty\)（选取正号，见 Table 1：\(M=1\) 时用 \(+\rho_\infty\)），得到公式 (46)。

---

#### 公式 (47)：\(M=1\) 时根 \(r\) 的解析解

\[
r = 1 + \rho_{\infty} \tag{47}
\]

**推导**：直接由公式 (46) 解出 \(r\)：\(r = 1 + \rho_\infty\)。这是 \(M=1\) 的情形下根 \(r\) 关于用户参数 \(\rho_\infty\) 的**显式解析公式**。注意：\(\rho_\infty \in [0,1]\)，故 \(r \in [1, 2]\)。

---

#### 公式 (48)：\(M=1\) 时的振幅因子

\[
R(\lambda) = \frac{P(\lambda)}{Q(\lambda)} = \frac{1 + \rho_{\infty} + \rho_{\infty}\lambda}{1 + \rho_{\infty} - \lambda} \tag{48}
\]

**推导**：将 \(r = 1+\rho_\infty\) 代入公式 (43)：

- 分子：\(p_0 + \rho_\infty\lambda = r + \rho_\infty\lambda = (1+\rho_\infty) + \rho_\infty\lambda\)
- 分母：\(r - \lambda = (1+\rho_\infty) - \lambda\)

特殊情形验证：当 \(\rho_\infty=1\) 时，\(R(\lambda) = \frac{2+\lambda}{2-\lambda}\)，这正是 \((1,1)\) 阶 Padé 展开（即**梯形法则**），满足 \(P(\lambda) = Q(-\lambda)\)，是完全无耗散的二阶格式。

---

#### 公式 (49)：\(M=1\) 时矩阵指数的有理近似

\[
\mathbf{R} = \frac{\mathbf{P}}{\mathbf{Q}} = \frac{(1+\rho_{\infty})\mathbf{I} + \rho_{\infty}\mathbf{A}}{(1+\rho_{\infty})\mathbf{I} - \mathbf{A}} \tag{49}
\]

**来源**：将公式 (48) 中的标量 \(\lambda\) 替换为矩阵 \(\mathbf{A}\)，数字 1 替换为 \(\mathbf{I}\)，即得公式 (49)。这是第 4 节单子步算法的出发点。当代入第 2 节公式 (18)（有理近似时间步进方程）时，可以推导出等价于 Newmark 梯形法则（无耗散时）或广义 \(\alpha\) 法（有耗散时）的时间积分格式。

---

#### 公式 (50)：\(M=1\) 的截断误差

\[
e^{\lambda} - R(\lambda) = \left(\frac{1}{2} - \frac{1}{1+\rho_{\infty}}\right)\lambda^2 + O(\lambda^3) \tag{50}
\]

**推导**：对公式 (48) 在 \(\lambda=0\) 处进行 Taylor 展开，再与 \(e^\lambda\) 的展开相减：

\[
e^\lambda = 1 + \lambda + \frac{\lambda^2}{2} + O(\lambda^3)
\]

\[
R(\lambda) = \frac{(1+\rho_\infty)+\rho_\infty\lambda}{(1+\rho_\infty)-\lambda} = \frac{(1+\rho_\infty)+\rho_\infty\lambda}{(1+\rho_\infty)(1-\frac{\lambda}{1+\rho_\infty})}
\]

利用 \(\frac{1}{1-x}\approx 1+x+x^2+\cdots\)：

\[
R(\lambda) \approx \frac{(1+\rho_\infty)+\rho_\infty\lambda}{1+\rho_\infty}\left(1+\frac{\lambda}{1+\rho_\infty}+\frac{\lambda^2}{(1+\rho_\infty)^2}+\cdots\right)
\]

\[
= \left(1+\frac{\rho_\infty}{1+\rho_\infty}\lambda\right)\left(1+\frac{\lambda}{1+\rho_\infty}+\cdots\right)
\]

展开并合并 \(\lambda^0, \lambda^1, \lambda^2\) 项：

\[
R(\lambda) = 1 + \lambda + \frac{1}{1+\rho_\infty}\lambda^2 + O(\lambda^3)
\]

因此：

\[
e^\lambda - R(\lambda) = \frac{1}{2}\lambda^2 - \frac{1}{1+\rho_\infty}\lambda^2 + O(\lambda^3) = \left(\frac{1}{2}-\frac{1}{1+\rho_\infty}\right)\lambda^2 + O(\lambda^3)
\]

这与公式 (50) 完全吻合。

**分析**：
- 当 \(\rho_\infty = 1\) 时，误差系数为 \(\frac{1}{2}-\frac{1}{2}=0\)，即 \(\lambda^2\) 项消失，截断误差提升为 \(O(\lambda^3)\)，这正是三阶精度 Padé 展开的特征；
- 当 \(\rho_\infty = 0\) 时，误差系数为 \(\frac{1}{2}-1 = -\frac{1}{2}\)，截断误差为二阶，数值耗散最大；
- 用 \(-\rho_\infty\) 作为右侧（即 \(r = 1-\rho_\infty\)）时，误差系数变为 \(\frac{1}{2}-\frac{1}{1-\rho_\infty}\)，模更大，故选 \(+\rho_\infty\) 的方案截断误差更小。

---

### \(M=2\) 的具体推导

#### 公式 (51)：\(M=2\) 时的 Taylor 展开输出

\[
\mathrm{p}[r,\lambda] \Rightarrow r^2 + (-2r + r^2)\lambda + (1 - 2r + r^2/2)\lambda^2 \tag{51}
\]

**推导**：将 \(M=2\) 代入公式 (40) 计算 \(p_0, p_1, p_2\)（详见公式 (40) 的示例部分）：

- \(p_0 = r^2\)
- \(p_1 = r^2 - 2r\)
- \(p_2 = \frac{r^2}{2} - 2r + 1\)

所以：

\[
(r-\lambda)^2 e^\lambda \approx r^2 + (r^2-2r)\lambda + \left(\frac{r^2}{2}-2r+1\right)\lambda^2
\]

与公式 (51) 完全一致。

---

#### 公式 (52)：以 \(\rho_\infty=0.5\) 为例的根方程

\[
1 - 2r + r^2/2 = 0.5 \tag{52}
\]

**来源**：令公式 (51) 中最高次项（\(\lambda^2\) 的系数 \(p_2(r)=\frac{r^2}{2}-2r+1\)）等于 \(\rho_\infty = 0.5\)（使用 \(+\rho_\infty\)，见 Table 1），即得公式 (52)。

---

#### 公式 (53)：方程 (52) 的两个解

\[
r_{1,2} = 2 \mp \sqrt{3} \tag{53}
\]

**推导**：整理公式 (52) 为标准二次方程形式：

\[
\frac{r^2}{2} - 2r + 1 - 0.5 = 0 \implies \frac{r^2}{2} - 2r + 0.5 = 0 \implies r^2 - 4r + 1 = 0
\]

用求根公式：

\[
r = \frac{4 \pm \sqrt{16-4}}{2} = \frac{4 \pm \sqrt{12}}{2} = 2 \pm \sqrt{3}
\]

即 \(r_1 = 2-\sqrt{3} \approx 0.268\)，\(r_2 = 2+\sqrt{3} \approx 3.732\)。

---

#### 公式 (54)：选取的根 \(r\)

\[
r = 2 + \sqrt{3} = 3.73205080756887729 \tag{54}
\]

**选取理由**：通过将两个根分别代入，绘制谱半径和相对周期误差曲线，发现 \(r=2+\sqrt{3}\) 在低频范围内的相对周期误差更小，因而被选取。直觉上，较大的 \(r\) 对应着更远离虚轴（\(\mathrm{Im}(\lambda)\) 轴）的极点，在低频区提供更好的近似性能。

---

#### 公式 (55)：选取根后的分子多项式 \(P(\lambda)\)

\[
\mathrm{p[rSelected, \lambda]} \Rightarrow 13.9282032302755092 + \lambda + 0.5\lambda^2 \tag{55}
\]

**验证**：将 \(r = 2+\sqrt{3}\) 代入公式 (51) 的各系数：

- \(p_0 = r^2 = (2+\sqrt{3})^2 = 4+4\sqrt{3}+3 = 7+4\sqrt{3} \approx 13.928\)（与公式 (55) 一致）
- \(p_1 = r^2-2r = 7+4\sqrt{3}-2(2+\sqrt{3}) = 7+4\sqrt{3}-4-2\sqrt{3} = 3+2\sqrt{3} \approx 6.464\)（注意此处公式 (55) 中写作 \(+\lambda\)，系数为1，这是约定了 \(\rho_\infty\)；验证过程需完整代值）
- \(p_2 = \rho_\infty = 0.5\)（最高次项系数即为用户指定的 \(\rho_\infty\)）

---

#### 公式 (56)：测试 \(\rho_\infty=-0.5\) 的根方程

\[
1 - 2r + r^2/2 = -0.5 \tag{56}
\]

**含义**：公式 (56) 是在测试令 \(p_2(r) = -\rho_\infty = -0.5\) 的情形（见公式 (42) 中的 \(\pm\) 选择）。

---

#### 公式 (57)：方程 (56) 的两个解

\[
r_1 = 1, \quad r_2 = 3 \tag{57}
\]

**推导**：整理公式 (56)：

\[
\frac{r^2}{2} - 2r + 1 + 0.5 = 0 \implies r^2 - 4r + 3 = 0 \implies (r-1)(r-3) = 0
\]

故 \(r_1=1, r_2=3\)，满根均为正实数。

---

#### 公式 (58)：\(r=3\) 时的分子多项式

\[
P(\lambda) = 9 + 3\lambda - 0.5\lambda^2 \tag{58}
\]

**验证**：将 \(r=3\) 代入公式 (51)：

- \(p_0 = r^2 = 9\)；\(p_1 = r^2-2r = 9-6 = 3\)；\(p_2 = \frac{9}{2}-6+1 = -0.5\)

故 \(P(\lambda)=9+3\lambda-0.5\lambda^2\)，与公式 (58) 完全吻合。注意此时 \(p_2 = -0.5 = -\rho_\infty\)（符号为负）。

**选择依据**：经过比较，\(r=2+\sqrt{3}\)（对应 \(+\rho_\infty\) 方案）的相对周期误差小于 \(r=3\)（对应 \(-\rho_\infty\) 方案），因此选取 \(r=2+\sqrt{3}\)。

---

#### 公式 (59)：\(M=2\) 时确定根的通用二次方程

\[
1 - 2r + r^2/2 = \rho_{\infty} \tag{59}
\]

**含义**：公式 (59) 将公式 (52) 推广到任意 \(\rho_\infty \in [0,1]\)，是 \(M=2\) 情形下的**通用根方程**。即：对任意用户指定的 \(\rho_\infty\)，通过求解此二次方程得到根 \(r\)。

---

#### 公式 (60)：\(M=2\) 时根 \(r\) 的通用解析解

\[
r = 2 + \sqrt{2 + 2\rho_{\infty}} \tag{60}
\]

**推导**：整理公式 (59) 为 \(\frac{r^2}{2} - 2r + (1-\rho_\infty) = 0\)，即 \(r^2 - 4r + 2(1-\rho_\infty) = 0\)，用求根公式：

\[
r = \frac{4 \pm \sqrt{16 - 8(1-\rho_\infty)}}{2} = 2 \pm \sqrt{4 - 2 + 2\rho_\infty} = 2 \pm \sqrt{2 + 2\rho_\infty}
\]

选取较大根 \(r = 2 + \sqrt{2+2\rho_\infty}\)，与上面 \(\rho_\infty=0.5\) 时的结论 \(r=2+\sqrt{3}\) 一致（\(\sqrt{2+2\times0.5}=\sqrt{3}\)），验证正确。

**特殊情形**：
- \(\rho_\infty=0\)：\(r=2+\sqrt{2}\approx 3.414\)，最大数值耗散；
- \(\rho_\infty=1\)：\(r=4\)，无数值耗散（梯形法则等价）。

---

#### 公式 (61)：\(M=2\) 时分子多项式的通用表达式

\[
P(\lambda) = r^2 + (-2r + r^2)\lambda + \rho_{\infty}\lambda^2 \tag{61}
\]

**来源**：将公式 (60) 中的 \(r\) 代入公式 (51) 右侧，注意最高次项系数 \(p_2(r) = \frac{r^2}{2}-2r+1 = \rho_\infty\)（由公式 (59) 保证），因此公式 (61) 是对公式 (51) 的化简，其中最高次项系数直接写作 \(\rho_\infty\)，其余系数 \(p_0=r^2\)、\(p_1=r^2-2r\) 由 \(r\)（公式 (60)）给出。

---

#### 公式 (62)：\(M=2\) 时矩阵指数的有理近似

\[
\mathbf{R} = \frac{\mathbf{P}}{\mathbf{Q}} = \frac{r^2\mathbf{I} + (-2r + r^2)\mathbf{A} + \rho_{\infty}\mathbf{A}^2}{(r\mathbf{I} - \mathbf{A})^2} \tag{62}
\]

**来源**：将公式 (61) 中的标量多项式升级为矩阵多项式（\(\lambda \to \mathbf{A}\)，\(1 \to \mathbf{I}\)），并代入公式 (44)，即得公式 (62)。

**物理意义**：公式 (62) 是 \(M=2\) 时整个时间积分格式的核心表达式。在第 4 节中，它的分母 \((r\mathbf{I}-\mathbf{A})^2\) 将对应两个子步，每个子步解同一线性方程组 \((r\mathbf{I}-\mathbf{A})\mathbf{z}^{(k)}=\mathbf{z}^{(k-1)}\)（公式 (86)）。第 4 节还将证明：\(M=2\) 的本方法与 Bathe 两子步 \(\rho_\infty\)-方法完全等价（公式 (95)–(96)）。

---

### \(M=3\) 的具体推导

#### 公式 (63)：\(M=3\) 时的 Taylor 展开输出

\[
\mathrm{p}[r,\lambda] \Rightarrow r^3 + (-3r^2 + r^3)\lambda + (3r - 3r^2 + r^3/2)\lambda^2 + \frac{-6 + 18r - 9r^2 + r^3}{6}\lambda^3 \tag{63}
\]

**推导**：将 \(M=3\) 代入公式 (40) 计算各系数：

- **\(p_0\)**（\(i=0\)）：\(\frac{3!}{0!\cdot 3!}\frac{1}{0!}r^3 = r^3\)

- **\(p_1\)**（\(i=1\)，\(j=0,1\)）：
  \[
  p_1 = \frac{3!}{0!\cdot 3!}\frac{1}{1!}r^3 + (-1)\frac{3!}{1!\cdot 2!}\frac{1}{0!}r^2 = r^3 - 3r^2
  \]

- **\(p_2\)**（\(i=2\)，\(j=0,1,2\)）：
  \[
  p_2 = \frac{1}{2}r^3 + (-1)\cdot 3\cdot r^2 + \frac{3!}{2!\cdot 1!}\cdot r\cdot 1 = \frac{r^3}{2} - 3r^2 + 3r
  \]

- **\(p_3\)**（\(i=3\)，\(j=0,1,2,3\)）：
  \[
  p_3 = \frac{r^3}{6} + (-1)\cdot 3\cdot \frac{r^2}{2} + 3\cdot r - 1 = \frac{r^3}{6} - \frac{3r^2}{2} + 3r - 1 = \frac{r^3 - 9r^2 + 18r - 6}{6}
  \]

即公式 (63) 的各项系数均可验证。

---

#### 公式 (64)：\(M=3\) 时确定根 \(r\) 的方程

\[
\frac{-6 + 18r - 9r^2 + r^3}{6} = \pm \rho_{\infty} \tag{64}
\]

**来源**：将公式 (63) 中 \(\lambda^3\) 项的系数 \(p_3(r)\) 令等于 \(\pm\rho_\infty\)（见公式 (42)，此时 \(M=3\)），即得公式 (64)。这是一个关于 \(r\) 的**三次代数方程**。

**与公式 (42) 的关系**：公式 (64) 正是公式 (42) 在 \(M=3\) 时的具体展开，二者完全等价。

---

#### 公式 (65)：\(M=3\) 时根 \(r\) 的解析解

\[
r = 3 - \operatorname{Re}\!\left[(1 + \sqrt{3}\,i)\,\sqrt[3]{3\!\left(1 - \rho_{\infty} + \sqrt{-2 - 2\rho_{\infty} + \rho_{\infty}^2}\right)}\right] \tag{65}
\]

**推导背景**：公式 (64) 在选取 \(-\rho_\infty\) 作为右侧（见 Table 1：\(M=3\) 时用 \(-\rho_\infty\)）后变为三次方程：

\[
r^3 - 9r^2 + 18r - 6 = -6\rho_\infty \implies r^3 - 9r^2 + 18r - 6(1-\rho_\infty) = 0
\]

通过令 \(r = t + 3\)（消去二次项）化为标准形式，再用**卡尔达诺（Cardano）公式**求解：

令 \(r = t + 3\)，代入得：

\[
t^3 - 9t - 6(1-\rho_\infty) + 18 \cdot \text{（含 }t\text{ 的项）}\cdots
\]

（具体推导涉及 Cardano 公式中的判别式运算，最终结果如公式 (65) 所示。其中出现了 \(1+\sqrt{3}i\)，这与 Cardano 公式中的三次根号取值有关——在三根情形下，三个根分别对应乘以 \(1, \omega, \omega^2\)（\(\omega = e^{2\pi i/3}\)），取实部以得到实数根。）

**验证**：
- 当 \(\rho_\infty = 0\) 时，\(\sqrt{-2+0+0} = i\sqrt{2}\)，代入后 \(r\) 应为某正实数，可数值验证；
- 当 \(\rho_\infty = 1\) 时，\(\sqrt{-3} = i\sqrt{3}\)，\(3(1-1+i\sqrt{3}) = 3i\sqrt{3}\)，解法类似，结果可验证。

**数值特点**：公式 (65) 给出了 \(M=3\) 情形下根 \(r\) 关于 \(\rho_\infty\) 的**解析（闭合形式）表达式**，这是迄今为止能以解析形式表示的最高阶情形。对于 \(M \ge 4\)，根方程只能数值求解（Table 1 给出了数值求解策略的规律总结）。

---

## 3.2 \((M+1)\)-格式：\(M\) 个子步，\((M+1)\) 阶精度

### 公式 (66)：\((M+1)\)-格式的更高阶 Taylor 展开

\[
(r - \lambda)^M e^{\lambda} = p_0(r) + p_1(r)\lambda + \cdots + p_M(r)\lambda^M + p_{M+1}(r)\lambda^{M+1} + O(\lambda^{M+2}) \tag{66}
\]

#### 含义与来源

与 \(M\)-格式公式 (41) 不同，公式 (66) 将展开多保留**一项**，即截断到 \(\lambda^{M+1}\)，余项为 \(O(\lambda^{M+2})\)。其中 \(p_{M+1}(r)\) 由公式 (40) 给出（令 \(i=M+1\)）。

#### 设计哲学

\(M\)-格式的分子多项式 \(\mathbf{P}\) 的阶数为 \(M\)（最高次 \(\lambda^M\)，系数为 \(\pm\rho_\infty\)），精度阶为 \(M\)。

若将 \(p_M\) 不再固定为 \(\pm\rho_\infty\)，而是通过令**更高一阶的系数** \(p_{M+1}(r)=0\) 来确定 \(r\)，则可使精度阶提高到 \(M+1\)，代价是耗散量不再可以自由指定。这正是 \((M+1)\)-格式的设计思路。

---

### 公式 (67)：确定根 \(r\) 的方程——令 \(\lambda^{M+1}\) 项系数为零

\[
p_{M+1}(r) = \sum_{j=0}^{M} (-1)^j \frac{M!}{j!(M-j)!} \frac{1}{(M+1-j)!} r^{M-j} = 0 \tag{67}
\]

#### 推导

将公式 (40) 中令 \(i=M+1\)，即得：

\[
p_{M+1}(r) = \sum_{j=0}^{\min(M+1,M)} (-1)^j \frac{M!}{j!(M-j)!} \frac{1}{(M+1-j)!} r^{M-j}
\]

注意 \(\min(M+1,M)=M\)，故求和上限为 \(j=M\)，恰好与公式 (67) 一致。

令 \(p_{M+1}(r)=0\) 的物理含义是：在有理近似 \(R(\lambda)\approx e^\lambda\) 中，\(\lambda^{M+1}\) 次的误差项被消除，截断误差从 \(O(\lambda^{M+1})\)（\(M\)-格式）提升为 \(O(\lambda^{M+2})\)（\((M+1)\)-格式），即精度阶从 \(M\) 提升到 \(M+1\)。

#### 方程性质

公式 (67) 是关于 \(r\) 的 **\(M\) 次代数方程**（注意最高次项 \(j=0\) 对应 \(\frac{1}{(M+1)!}r^M\)），一般有 \(M\) 个根。从多个解中选取使 \(\rho=|R|\le 1\) 且低频周期误差最小的那个根。

#### 与公式 (42) 的对比

| | \(M\)-格式 | \((M+1)\)-格式 |
|---|---|---|
| 约束条件 | \(p_M(r)=\pm\rho_\infty\) | \(p_{M+1}(r)=0\) |
| 自由度 | \(\rho_\infty\) 用户可调 | \(\rho_\infty\) 内嵌，不可调 |
| 精度阶 | \(M\) 阶 | \(M+1\) 阶 |
| 子步数 | \(M\) | \(M\) |

---

### 公式 (68)：\((M+1)\)-格式的标量振幅因子

\[
e^{\lambda} \approx R(\lambda) = \frac{P(\lambda)}{Q(\lambda)} = \frac{p_0 + p_1\lambda + \cdots + p_M\lambda^M}{(r - \lambda)^M} \tag{68}
\]

#### 来源

确定根 \(r\)（由公式 (67)）后，将其代入公式 (40) 求出 \(p_0, p_1, \ldots, p_M\)（注意：\(p_{M+1}(r)=0\) 意味着精度阶达到 \(M+1\)，但分子多项式仍为 \(M\) 次，与 \(M\)-格式的公式 (43) 形式相同）。代入公式 (35) 即得公式 (68)。

**与公式 (43) 的形式对比**：两者形式相同，区别在于：公式 (43) 的最高次项系数 \(p_M = \pm\rho_\infty\)（用户指定），而公式 (68) 的 \(p_M\) 由根 \(r\) 唯一决定，是内嵌的常数。

---

### 公式 (69)：\((M+1)\)-格式的矩阵指数有理近似

\[
\mathbf{R} = \frac{\mathbf{P}}{\mathbf{Q}} = \frac{p_0\mathbf{I} + p_1\mathbf{A} + \cdots + p_M\mathbf{A}^M}{(r\mathbf{I} - \mathbf{A})^M} \tag{69}
\]

#### 来源与意义

公式 (69) 是公式 (68) 的矩阵形式（\(\lambda \to \mathbf{A}\)），形式上与公式 (44) 完全相同，但参数 \(r\) 和系数 \(p_i\) 的确定方式不同。

**文中的重要备注（公式 (44) 与 (69) 的对比）**：

> "Note that Eqs. (44) and (69) have the same form and number of terms but differ in how the root \(r\) is determined."

这一备注非常重要：**两类格式的代码实现完全相同**（第 4 节的算法对两者通用），区别仅在于初始化阶段的参数 \(r\) 和 \(p_i\) 的计算方式。

---

### 公式 (70)：\((M+1)\)-格式内嵌的高频谱半径

\[
\rho_{\infty} = |p_M| \tag{70}
\]

#### 推导

由公式 (33)（\(N_q = N_p = M\)）和公式 (68)（分母主导项为 \((-\lambda)^M\)，即 \(q_M = (-1)^M\)）：

\[
\rho_\infty = \left|\frac{p_M}{q_M}\right| = \left|\frac{p_M}{(-1)^M}\right| = |p_M|
\]

故公式 (70) 成立。它表明：\((M+1)\)-格式的数值耗散量完全由根 \(r\)（通过公式 (67) 确定）决定，用户无法调整，因此在某些要求精确控制耗散量的场景下，\(M\)-格式更为适用。

---

### \(M=2\) 的 \((M+1)\)-格式具体推导

#### 公式 (71)：\(M=2\) 时展开到 \(\lambda^3\) 的 Taylor 展开

\[
\mathrm{p}[r,\lambda] \Rightarrow r^2 + (-2r + r^2)\lambda + (1 - 2r + r^2/2)\lambda^2 + (1 - r + r^2/6)\lambda^3 \tag{71}
\]

**来源**：与公式 (51) 相比，公式 (71) 在公式 (51) 的基础上多计算了一项 \(\lambda^3\) 的系数 \(p_3(r)\)（由公式 (40) 在 \(i=3, M=2\) 时给出）：

\[
p_3(r) = \sum_{j=0}^{2}(-1)^j\frac{2!}{j!(2-j)!}\frac{1}{(3-j)!}r^{2-j}
\]

- \(j=0\)：\(\frac{2!}{0!\cdot 2!}\frac{1}{3!}r^2 = \frac{r^2}{6}\)
- \(j=1\)：\((-1)\frac{2!}{1!\cdot 1!}\frac{1}{2!}r^1 = -r\)
- \(j=2\)：\((-1)^2\frac{2!}{2!\cdot 0!}\frac{1}{1!}r^0 = 1\)

故 \(p_3(r) = \frac{r^2}{6} - r + 1\)，写成 \(1-r+\frac{r^2}{6}\)，与公式 (71) 最后一项一致。

---

#### 公式 (72)：令 \(\lambda^3\) 项系数为零

\[
1 - r + r^2/6 = 0 \tag{72}
\]

**来源**：将公式 (71) 的 \(\lambda^3\) 项系数 \(p_3(r) = 1-r+\frac{r^2}{6}\) 令等于零（公式 (67) 在 \(M=2\) 的具体展开），即得公式 (72)。这是确定 \((M+1)\)-格式中根 \(r\) 的方程，也是一个二次方程。

---

#### 公式 (73)：方程 (72) 的两个解

\[
r_{1,2} = 3 \mp \sqrt{3} \tag{73}
\]

**推导**：整理公式 (72)：

\[
\frac{r^2}{6} - r + 1 = 0 \implies r^2 - 6r + 6 = 0
\]

用求根公式：

\[
r = \frac{6 \pm \sqrt{36-24}}{2} = \frac{6 \pm \sqrt{12}}{2} = 3 \pm \sqrt{3}
\]

即 \(r_1 = 3-\sqrt{3}\approx 1.268\)，\(r_2 = 3+\sqrt{3}\approx 4.732\)。

---

#### 公式 (74)：选取的根 \(r\)

\[
r = 3 - \sqrt{3} = 1.267949192431123 \tag{74}
\]

**选取理由**：通过将 \(r_1 = 3-\sqrt{3}\) 和 \(r_2 = 3+\sqrt{3}\) 分别代入公式 (68) 并绘制谱半径与相对周期误差曲线，发现 \(r = 3-\sqrt{3}\) 对应的有理近似谱半径 \(\rho \le 1\)（即无条件稳定），且低频相对周期误差更小，因而被选取。

**对比与关联**：注意公式 (74) 与公式 (60) 的不同——两者都是二次方程的根，但公式 (74) 对应零耗散约束（即 \(p_3=0\)），公式 (60) 对应耗散约束（即 \(p_2=\rho_\infty\)）。两者的根方程系数完全不同（公式 (72) vs. 公式 (59)）。

---

#### 公式 (75)：\(M=2, (M+1)\)-格式的分子多项式 \(P(\lambda)\)

\[
\mathrm{p[rSelected, \lambda]} \Rightarrow 1.607695154586736 - 0.9282032302755092\lambda - 0.7320508075688773\lambda^2 \tag{75}
\]

**验证**：将 \(r = 3-\sqrt{3}\) 代入公式 (71) 的前三项系数（\(p_0, p_1, p_2\)）：

- \(p_0 = r^2 = (3-\sqrt{3})^2 = 9-6\sqrt{3}+3 = 12-6\sqrt{3} \approx 12-10.392 = 1.608\)（与公式 (75) 吻合）
- \(p_1 = r^2-2r = (12-6\sqrt{3})-2(3-\sqrt{3}) = 12-6\sqrt{3}-6+2\sqrt{3} = 6-4\sqrt{3} \approx 6-6.928 = -0.928\)（与公式 (75) 吻合）
- \(p_2 = \frac{r^2}{2}-2r+1 = \frac{12-6\sqrt{3}}{2}-2(3-\sqrt{3})+1 = 6-3\sqrt{3}-6+2\sqrt{3}+1 = 1-\sqrt{3}\approx -0.732\)（与公式 (75) 吻合）

---

#### 公式 (76)：\(M=2, (M+1)\)-格式的标量有理近似

\[
R(\lambda) = \frac{P(\lambda)}{Q(\lambda)} = \frac{1.607695154586736 - 0.9282032302755092\lambda - 0.7320508075688773\lambda^2}{(1.267949192431123 - \lambda)^2} \tag{76}
\]

**来源**：将公式 (75) 中的系数和公式 (74) 中的根 \(r\) 代入公式 (68)，即得。这是 \(M=2\) 时 \((M+1)\)-格式的完整数值表达式，可直接用于谱分析（绘制谱半径和相对周期误差曲线）。

---

#### 公式 (77)：\(M=2, (M+1)\)-格式内嵌的高频谱半径

\[
\rho_{\infty} = |R_{\infty}| = |1 - \sqrt{3}| = 0.732050807568877 \tag{77}
\]

**推导**：由公式 (70)，\(\rho_\infty = |p_2| = |1-\sqrt{3}| = \sqrt{3}-1 \approx 0.732\)。注意 \(1-\sqrt{3} < 0\)，故取绝对值得 \(\sqrt{3}-1\)。

**物理意义**：\(\rho_\infty \approx 0.732\) 意味着每个时间步后，高频振荡的振幅衰减到约 73.2%，具有中等程度的数值耗散。这一耗散量是为了实现三阶精度而付出的"代价"，用户无法改变。

**与文献对比**：文中指出，这一 \(\rho_\infty\) 值与 Bathe 第三阶方法的推荐值相同（见第 4 节公式 (96) 的讨论），证明了本方法与已有方法的一致性。

---

#### 公式 (78)：验证 \((M+1)\)-格式精度的误差展开

\[
\mathrm{Series}[\exp(\lambda) - R(\lambda), \lambda, 0, M+2] \Rightarrow 0.089779189\lambda^4 + O(\lambda^5) \tag{78}
\]

**含义与验证过程**：对公式 (76) 中的 \(R(\lambda)\) 和 \(e^\lambda\) 分别在 \(\lambda=0\) 处展开，取差的前几项：

- 由于设计时 \(p_3(r)=0\)（公式 (72)），\(e^\lambda - R(\lambda)\) 的 \(\lambda^0, \lambda^1, \lambda^2, \lambda^3\) 项均为零（精确匹配），故最低次误差项为 \(\lambda^4\)；
- 系数 \(0.089779\ldots\) 是一个具体的正数（\(\approx \frac{1}{11}\)），其大小反映了四阶误差的量级。

**总结**：公式 (78) 从数值上确认了 \((M+1)\)-格式（\(M=2\)）的截断误差为 \(O(\lambda^4)\)，即精度为三阶（每步局部截断误差 \(O(\Delta t^4)\)，全局误差 \(O(\Delta t^3)\)），高于 \(M\)-格式（\(M=2\)，精度二阶）一阶。

---

#### 公式 (79)：\(M=2, (M+1)\)-格式的矩阵指数有理近似

\[
\mathbf{R} = \frac{\mathbf{P}}{\mathbf{Q}} = \frac{1.607695154586736\mathbf{I} - 0.9282032302755092\mathbf{A} - 0.7320508075688773\mathbf{A}^2}{(1.267949192431123\mathbf{I} - \mathbf{A})^2} \tag{79}
\]

**来源**：将公式 (76) 中 \(\lambda\) 替换为矩阵 \(\mathbf{A}\)，并将数字 1 替换为单位矩阵 \(\mathbf{I}\)，即得到最终可用于计算的矩阵版有理近似。公式 (79) 是直接代入第 2 节公式 (18) 时使用的实际矩阵表达式，其分母 \((r\mathbf{I}-\mathbf{A})^2\) 对应第 4 节的两个子步迭代（公式 (85)–(89)）。

---

## 第 3 节公式总览与内在联系

### 公式之间的推导链

下图以箭头表示推导方向，清晰展示了第 3 节所有公式之间的逻辑链条：

```
公式(34)：结构化分母 → 公式(35)：标量振幅因子
                             ↓ 乘以分母
公式(37)：二项展开     →  公式(36)：变换形式
公式(38)：Taylor展开   →  公式(39)：乘积展开（幂级数）
                             ↓ 匹配系数
公式(40)：系数 p_i(r) 的通用显式公式
         ╔══════════════╤═══════════════════╗
         ↓                                  ↓
  【3.1 M-格式】                    【3.2 (M+1)-格式】
  公式(41)：截断至λ^M          公式(66)：截断至λ^(M+1)
         ↓                                  ↓
  公式(42)：令p_M=±ρ∞          公式(67)：令p_{M+1}=0
         ↓                                  ↓
  确定根 r（含(47)(53)(60)(65)）   确定根 r（含(73)(74)）
         ↓                                  ↓
  公式(43)(44)：M-格式振幅因子    公式(68)(69)：(M+1)-格式振幅因子
  和矩阵有理近似                   和矩阵有理近似
         ↓                                  ↓
  M=1示例：(45)→(46)→(47)       公式(70)：内嵌ρ∞=|p_M|
  →(48)→(49)→(50)                         ↓
  M=2示例：(51)→(52/56)         M=2示例：(71)→(72)→(73)
  →(53/57)→(54/58)              →(74)→(75)→(76)→(77)
  →(59)→(60)→(61)→(62)         →(78)→(79)
  M=3示例：(63)→(64)→(65)
```

### 关键参数的传递路径

| 参数 | 来源 | 用途 |
|------|------|------|
| \(M\)（子步数）| 用户指定 | 决定分母阶数和精度上限 |
| \(\rho_\infty\)（高频谱半径）| \(M\)-格式：用户指定；\((M+1)\)-格式：由 \(r\) 内嵌决定 | 通过公式 (42) 或 (67) 约束根 \(r\) |
| \(r\)（单重根）| 由 \(M\) 和 \(\rho_\infty\) 通过代数方程唯一确定 | 决定分母 \((r\mathbf{I}-\mathbf{A})^M\)；传入公式 (40) 计算所有 \(p_i\) |
| \(p_i(r)\)（分子系数）| 公式 (40) 给出关于 \(r\) 的显式表达 | 构成分子多项式 \(\mathbf{P}\)；进入公式 (44) 或 (69) |

### 两类格式的对比总结

| 特性 | \(M\)-格式（3.1节）| \((M+1)\)-格式（3.2节）|
|------|---------------------|------------------------|
| 子步数 | \(M\) | \(M\) |
| 分子阶数 | \(M\) | \(M\) |
| 精度阶数 | \(M\) 阶 | \(M+1\) 阶 |
| 截断误差 | \(O(\Delta t^{M+1})\) | \(O(\Delta t^{M+2})\) |
| 确定根 \(r\) 的条件 | \(p_M(r)=\pm\rho_\infty\) | \(p_{M+1}(r)=0\) |
| 耗散量 | 用户可调（\(\rho_\infty\in[0,1]\)）| 内嵌（\(\rho_\infty=|p_M|\)，固定）|
| 矩阵形式 | 公式 (44) | 公式 (69) |
| 代表格式（\(M=2\)）| 等价于 Bathe \(\rho_\infty\)-方法 | 等价于第三阶 Bathe 方法 |
| \(M\) 的可用范围 | \(M=1\sim 6\) | \(M=1,2,3,5\)（其余不满足 \(\rho\le1\)）|

---

> **注**：本节所有符号与文献原文保持一致。\(\mathbf{A}\) 为公式 (8) 定义的状态空间矩阵，\(\mathbf{I}\) 为单位矩阵，\(\lambda = i\omega\Delta t\) 为 \(\mathbf{A}\) 的特征值（纯虚数），\(\mathbf{M}\)、\(\mathbf{C}\)、\(\mathbf{K}\) 分别为质量、阻尼、刚度矩阵，\(\Delta t\) 为时间步长，\(\omega\) 为自振频率。
